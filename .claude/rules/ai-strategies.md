# AI 戦略（Human-like AI 拡張）

`katrain/core/ai.py` の戦略クラスを触る前に読む。パラメータの具体値は `ai-parameters.md`、
フィルタ実装の詳細とチェックリストは `ai-humanstyle.md`。

## 改修の全体像

Human-like AI（9段）モードの拡張。悪手フィルタ（スコアベースのフィルタリング）に加え、力戦派（Fighting）・攻城（Siege）・狩猟（Hunt）・狩猟一致率低減（HuntDivergence）・AI一致率低減（Divergence）・地合い勝ち（Jigo）等の戦略モードを追加・改修。力戦派には複雑化モード `complex`（切りボーナス＋リード適応の損失予算ゲートで盤面を紛れさせる）を追加。力戦派の human/complex の悪手フィルタ閾値は **9路と13/19路・序盤と中盤以降で独立した GUI スライダー**（`fighting_human_*_max_loss` / `complexity_*_max_loss_9`、解決は共有純関数 `_fighting_loss_thresholds`）で、既定値は従来のハードコード値と同一。`fighting_max_loss` は scoreloss 専用で human/complex には効かない。Jigo には序盤星打ち強制オプション `jigo_force_sanrensei`（19路のみ・黒=三連星/白=2連星）を追加。さらに Jigo には9路専用の独立戦略 `ai:jigo9`（持碁（9路））を追加（既存 `ai:jigo` は19/13路専用に整理）し、9路 deception の phase 境界・target を5スライダーで調整可能にした（phase3前倒しで挽回を間に合わせる）。星打ち布石ロジックは `ai.py` の共有ヘルパー `_compute_star_opening_targets` に集約し HumanStyle の2連星と共用。さらに**9路専用の独立戦略 `ai:parity9`（一致率追随（9路））**を追加（**終局レポートの AI 最善手一致率を絶対目標 `parity9_target_rate` まで下げつつ勝ちきる**。2026-08-08 に設計を入れ替えた＝実測の経緯と却下案は `.claude/rules/ai-parameters.md` の「改修 2026-08-08」に集約。要点は4つ。(1) **候補プールは通常解析 `cn.candidate_moves`**。Stage2 は `scoreLead` をクリーンに保つため `wideRootNoise=0` で撃っており、9路では非最善手を 69〜86visits でしか読まないので損失が一貫して 1.3〜1.8目楽観的に出る（改修前の低い一致率は損失の過小評価で買っていた）。損失は `relativePointsLost`＝**GameNode 側で既に打つ側視点に符号済み**なので `sign` を掛けない。`PARITY9_MIN_VISITS`(10) 未満は候補に入れない。(2) 解禁は**レートゲート** `parity9_rate_gate`＝「この手も一致させたら実効目標を超えるか」の先読み型で、実効目標は `parity9_target_rate` の**絶対値のみ**（相手の一致率では動かさない＝ユーザー要件の条件は「勝てない場合」で、それは勝率フロアが処理するため二重適用だった。実測で30判断中5回、安全性と無関係にゲートを閉じていた）。旧・一致数差ゲートは 0-0 で必ず閉じるため**盤面で最も外し賃の安い白の初手**を落としており、相手が最善手に一度も一致しない対局では追随目標が原理的に達成不能だった。(3) 安全条件は**着手後の勝率フロア** `parity9_min_winrate` **ただ1つ**（候補の `winrate` は探索値＝相手の最善応手込み）。スコア予算 `lead − keep_margin` は撤去した＝`scoreLead` は komi 込みなので「lead > 0」は「勝率 > 50%」と同義で、**9路 komi 7 の黒は開始時点で lead が負**（実測 黒番実戦 depth 0〜8 で −0.22〜−0.04）＝予算 0 で早期 return し、勝率フロアが評価されないまま「no budget」とログに出る**黒白非対称**だった。hp を見ない足切り `parity9_has_admissible` を挟むことで「外すと決まるまで humanSL を撃たない」順序は維持している。なお**序盤を塞いでいるのは予算ではなく勝率フロア**（黒の序盤は勝率50%ちょうどなので、勝率を下げたくないなら原理的に外せない）。(4) 選択は**最安バンド内の humanPolicy 最大**（`parity9_cost_slack`）。「予算内で humanPolicy 最大」だと毎回予算を使い切るが、実測では高価な外しほど humanPolicy も低いので安さを第1基準にするとコストと不自然さが同時に下がる。pass は外し候補から除外するので area scoring のダメ処理は KataGo 最善手のまま。ヨセ（`parity9_endgame_move` 手数以降 **かつ** 未確定点 `|ownership| < 0.5` が `parity9_unsettled_max` 以下）に入ったら sticky になるが、**丸ごとロックはしない** — cap を `parity9_yose_max_loss`（既定 0.1目）まで絞るだけで、ダメ詰めや1目ヨセの**手順入れ替え（目数が動かない）**は許す（0 で従来の完全固定）。長い9路の実戦ではヨセこそ外し賃が最も安く、ロックすると31判断中16手＝52%が最善手に固定されて一致率の最大のボトルネックになる。ownership はユーザーのローカル設定が `_enable_ownership: false` なのでクエリで明示要求する。全ゲートが「最善手を打つ」に倒れるフェイルセーフ）。さらに**9路専用の独立戦略 `ai:enigma9`（難解（9路））**を追加（2026-08-10・spec `2026-08-10-enigma9-strategy-design.md`。序盤〜中盤は損失上限 `enigma9_max_loss`〈候補値の天井1.8目＝**2目以上の損失手は打たない**〉と着手後勝率フロア `enigma9_min_winrate` の内側で「**相手が正しく応じることが最も難しい手**」へ積極的に外し、相手の研究した定跡・手筋を無効化する。難解さは候補ごとに子局面をプローブ〈クリーン500visits + humanSL 9段 humanPolicy 8visits・全並列発行〉して `net = E〈9段の応手分布で重みづけた相手の期待損失〉+ 十分な応手〈損失0.3以下〉の見つけにくさ + 自手の意外さ − 検証済み損失`（自手の意外さは応手が自明な手〈find_hp > 0.85〉で線形減衰＝E≈0 なのに自手の珍しさだけで目を払う手を塞ぐ・spec 追記11・2026-09-01） で測り、最善手も同じ尺度でスコアして `enigma9_net_margin`〈既定0〉以上勝る挑戦者だけ外す。**候補プールは visits>=1 まで広げ、採否の損失・勝率は子局面プローブの検証値で確定する**〈9路の通常解析は visits が1〜3手に集中し visits>=10 のプールは0〜1手に痩せる＝実測 move 8 で74手中2手。浅い候補の生 loss は打つ側に楽観的なので「生loss>cap」の足切りは安全側、偽に安い1visitの蜃気楼は検証capが落とす＝実測 生0.15→検証0.29。**1visitの生lossが悲観側に壊れて足切りされる手だけは救えない**＝既知の限界〉。攻め合い1手差の積極形成は専用検出なし＝相手の並みの応手が大損する局面は E が構造的に高く出て、安全ゲート〈cap+勝率フロア、どちらも相手最善応手込みの探索値〉が「間違えなければ勝てる場合のみ」を担保。**勝勢時はヨセ前に限り消費モード**〈`enigma9_large_lead_max_loss` 既定5.0・`enigma9_spending_plan`〉＝budget = lead−target が max_loss を超える間、cap をこの値まで緩和し **net の損失項を cost_weight = max_loss/budget に割引**する（cap を広げるだけでは E 典型0.2〜2目の中で3〜5目の勝負手が net で必ず負けて帯が使われない。初戦実戦ログ game_20260810_193156〈AI白番＝depth奇数〉でリード+6〜+38 の中盤後半が cap 固定のため admissible=0 連続＝強制最善手になり「一致率が異常に高い」というユーザー報告が起点。budget→max_loss で cost_weight は連続的に1へ戻り、毎手消費で lead は target+max_loss 近傍へ収束＝2目差勝ちへ余剰を難解さに変換。実測: 復元SGF move 21〈lead+12.6〉で admissible 1→15・F3〈vloss 2.16・E 1.38・応手発見率5.9%・wr 99.8%〉へ外し、互角局面は Spend 非発火で不変〉。ヨセ〈手数AND未確定点・sticky・parity9と同一判定〉は**net から「自手の意外さ」own_rare を外し**〈`enigma9_own_rarity_weight`＝ヨセ重み0・spec 追記6・2026-08-22。ヨセは E も reply_rare も横並びに潰れるので own_rare が順位をそのまま決め「予算内で最も humanPolicy の低い手」が機械的に選ばれていた＝実測 校正局 move 53 で 最善 E9〈hp 95.7%〉ではなく J8〈hp 0.1%・意外さ +0.996〉を 0.27目払って採用。own_rare は研究した定跡を外すための 序盤〜中盤の道具で、一本道のヨセでは「人間が打たない手」にしかならない＝jigo の `jigo_endgame_humanstyle` と同じ理由。ヨセの外しは E と応手の見つけにくさ＝本物の罠だけで正当化する。非ヨセ手番はビット同一〉、lead − `enigma9_target_score`(2) の余剰だけを外し予算にし、余剰が無ければ最善手で**2目勝ち〜持碁**を確保、劣勢は最善手で粘るだけ＝勝てない碁は僅差で負けてよい。全分岐が「最善手を打つ」に倒れるフェイルセーフ。実測: 校正局黒25判断バッチで Top1 56%〈=44%外し〉・平均損失0.03目・Top5 96%、序盤 move 2 は F7〈own_hp 10%・応手の見つけやすさ17%・検証損失0.29目〉へ外し、ヨセ+4.4は予算内候補なしで最善、劣勢白は子クエリ0本で即最善）。難解には**13路版の独立戦略 `ai:enigma13`（難解（13路））**もある（2026-08-10・spec 追記2。実装は Enigma9Strategy をクラス属性〈`BOARD_LEN`/`KEY_PREFIX`/`LABEL`/`SETTING_DEFAULTS`・sticky フラグ `game._enigma13_endgame`〉でパラメータ化したサブクラスで `generate_move` は非オーバーライド＝選択則・消費モード・ヨセ予算・フェイルセーフは9路版と同一、既定値だけ13路へスケール: max_loss 1.5〈候補天井3.0＝「2目未満」は挽回が難しい9路向けの締め方で、13路は悪手フィルタ比3.3→5.6でスケール〉・large_lead_max_loss 8.0〈jigo の13/19路既定と同値〉・endgame_move 75・unsettled_max 16〈≒169点の10%〉。**13路の実戦校正は未実施**）。13/19路には**局所性オプション** `enigma{13,19}_locality_stddev` / `_locality_slack`（2026-08-25・既定 OFF・9路にも既定 OFF で露出・spec `2026-08-25-enigma-locality-design.md`）がある＝13路実戦ログでは外した手の 69% が最善手からチェビシェフ距離 4 以上で、序盤は E≈0 のまま own_rare だけで別の隅へ飛ぶ（追記6 のヨセと同じ「own_rare が順位を決める」構造の序盤版）ため、own_rare を相手の直前手／KataGo 最善手の近傍で減衰させ、net の同点帯では最も近い手を採る。明確に優る遠い罠は帯の外なので残る。さらに**序盤の HumanStyle 9段委譲** `enigma{9,13,19}[plus]_opening_humanstyle_moves`（2026-09-04・13/19路は既定 20 手・9路は既定 OFF）がある＝対局の手数がこの値未満の手番は難解の選択に入らず rank_9d としてそのまま打つ（jigo のヨセ委譲の鏡像・純関数 `enigma9_opening_handoff`・難解＋は `generate_move` 非オーバーライドなので継承）。起点はユーザー報告「13/19路の序盤 15〜25 手で珍しすぎる手を打ちすぎる」で、実測 2026-09-03（13路 29 局）の「序盤は E≈0 で own_rare だけが順位を決め、外しても相手の実損失 0.17 目＝騙せていない」と整合する。さらに**19路版の独立戦略 `ai:enigma19`（難解（19路））**もある（2026-08-12・spec 追記4。13路版と同じ方式＝Enigma9Strategy のクラス属性差し替えサブクラス〈sticky フラグ `game._enigma19_endgame`〉で `generate_move` は非オーバーライド、既定値だけ19路へスケール: max_loss 2.0〈候補天井4.0＝悪手フィルタは13路と同じ5.6だが盤が広く挽回機会が多いので一段開ける。5.6までは開けず「難解だが悪手ではない」帯に留める〉・large_lead_max_loss 8.0〈jigo の13/19路既定と同値〉・endgame_move 150〈jigo の19路ヨセ委譲既定・deception phase3 開始と同じ〉・unsettled_max 36〈≒361点の10%〉。**19路の実戦校正は未実施**）。難解（9/13/19路共通）には**持碁〜2目以内の負けを狙うオプション** `enigma{9,13,19}_aim_jigo`（bool・既定OFF）がある（2026-08-14・spec 追記5。優先度 持碁 > 2目以内の負け > それ超の負け。ON で target を `ENIGMA9_JIGO_TARGET`(-1.0) に固定＝許容帯 [-2,0] の中心でパリティ非依存（整数コミの偶数目差 0/±2 も半目コミの ±0.5/±1.5 も両隣が帯内。0 狙いは検証ノイズ ±0.3 の上振れがそのまま勝ち＝帯の外）、`target_score` は無視。**勝率フロアは無効化**＝意図して勝率50%を割りに行くモードでは勝率で安全を測れない（ヨセで lead が僅かに負＝設計どおりの局面ほど探索値の勝率が急落し正当な外しを全部ブロックする）ので、安全条件は「検証済み損失 <= cap <= lead−target＝着手後も target を割らない」の1本＝中盤 cap もヨセ予算と同じ式で頭打ち（純関数 `enigma9_aim_cap`）。lead <= target なら最善手で維持/挽回＝「2目超の負けを避ける」と「持碁へ寄せる」を同じ1本で実装。勝勢の削りは既存の消費モードを target=-1 で流用＝1手の損失は `large_lead_max_loss` で頭打ち・選択は難解さ net 最大なので**大差でもバレる大損失手は打たない**（そのぶん削り切れない大差は僅差勝ちで終わりうる）。OFF 時は cap・min_wr・target とも従来値＝解析条件も採用判断もビット同一）。難解（9/13/19路共通）は**着手時間を短縮済み**（2026-08-11・精度不変・spec 追記3。実測13路ウォームの内訳は 通常解析0.6〜0.75秒・親humanSL 0.05〜0.27秒・プローブバッチ×8 1.2〜1.44秒＝支配項。(1) 親 humanSL クエリを 8visits 化し `_probe_children(parent_hp=True)` で子局面プローブと同一バッチ並列発行〈humanPolicy は root NN 出力で visits 非依存だが**別プロセス間では ±0.05〜0.09 揺れて上位10手の順位も入れ替わる**＝TensorRT バッチ非決定性。1000v 同士でも同幅なので 8visits 化は既存分散に上乗せなし、を別プロセス A/B で確認〉、(2) **着手後の先読み**＝着手を返す直前に相手の有力応手 top-5〈humanSL 9d の応手分布順＝追記9・2026-08-27。旧 KataGo本命1+humanSL2 は13路監視対局148局面の実測で的中 50%→humanSL順 top-5 62%〉の局面と、上位2手はその解析の top-8 候補の子プローブも、使い捨て複製ゲーム経由・優先度 `PRIORITY_ENIGMA_PONDER`(-50)・価値順のレーン（`PonderLanes`・同時3本＝KataGo は priority で並べ替えるだけで同時実行を絞れず、全部同時に出すと有力な応手から温まり切らない）で先回り発行し **NN キャッシュだけ温めて結果は捨てる**〈判定影響ゼロ。発火は自分=AI かつ 相手=人間のときだけ＝`_ponder_applies`。残骸は Game.play の `_cancel_enigma_ponder`〈相手の着手が入った瞬間に gen を進めて回収。**terminate だけではワーカーの sim 構築約0.1秒より速い応手に空振りする**ため、gen は `_start_ponder` の同期部で捕獲してワーカーに渡す。この掃除が無いと実クエリが温めと GPU を取り合い 1.6→4.4 秒に伸びた実測あり〉＋ generate 冒頭の保険 `_cancel_ponder` の2段〉、(3) per-move 時間ログ `着手決定に X.X 秒`〈OUTPUT_INFO・tsumego と同形式〉。実測: generate 8局面×2run 平均 1.04→0.89 秒、先読み**的中時**の次手番は 通常解析+generate 0.92〜1.09 → **0.35〜0.48 秒**〈プローブバッチ 0.45〜0.64 → 0.08〜0.11 秒〉、外れは従来どおり）。さらに**監視モードでは先読みを一本化し自ノード解析を後回しにする**（2026-08-27・精度不変・spec 追記9。監視スレッドは新規対局をまたいで生きるのに `Game` を作り直すと `board_watch_active`/`prefetch_replies` が初期値に戻るバグで、2局目からヨセの Probe 省略〈追記7〉も応手先読みも黙って死んでいた＝`apply_game_watch_flags` を `_do_new_game` からも呼ぶ。逆に1局目は ponder 3本＋先読み 5本＋自ノード解析の root 9本が横並びで走り約2.8秒の考慮時間で何も温まり切らず的中 28%＝ponder が発火した着手では `_maybe_board_watch_prefetch` は撃たない。自ノードの 2000visits 解析は監視モードで ponder が armed なら fast だけにし、フルは温めの後に ponder が発行〈`_enigma_ponder_defers_own_analysis`〉。詳細と実測は `.claude/rules/ai-parameters.md`）。さらに**終局帯では難解さの選択をせず 9d の終局処理に倣う**（2026-08-28・spec 追記10。area scoring では終局後も自陣埋めが 0 目損で候補に並び KataGo 最善手が pass にならないため、旧実装〈`best_gtp == "pass"` のときだけパス〉は「最も難解な 0 目損の埋め手」を打ち続けた＝実測 game_20260828_064054 move 57: pass 3位・損失 0.07・0 目損の候補 25 手 → H1。**pass が候補に居て損失 < 0.5**〈`_AREA_PASS_MARGIN`＝盤上に 0.5 目以上の手が無い〉の手番だけ、(1) 相手の直前パス → パス〈pass の損失が 0.5 以上でも ownership を1本撃ち全点 |o| >= 0.9 ならパス＝chinese ルールは未取り込みの死石を生きと数えるので pass が 126 目損に出るが終局すれば ownership どおり。未決着ならば最善手〉、(2) humanSL 9d 8visits の最上位が pass → パス〈他5戦略の `_area_scoring_should_pass` と同じ判定〉、(3) 9d 最上位の盤上の手が KataGo 候補で損失 < 0.5 → その手〈ダメ・死石の取り込み・ツギ〉、(4) 最善手。帯の外はビット同一。「PV に pass が現れたらパス」は相手のパスを拾うので却下〈同 065952 move 80: PV に pass が並ぶのに自分の pass は 126 目損＝chinese ルールでは死石の取り込みが残っている〉。改修後の実測は 064054@57 → J3〈9d 36.1% > pass 19.9%〉、065952@82 → J8〈9d 52.8%・pass 0%〉、帯の外は不変）。さらに**盤面監視モードのヨセは追加クエリを削って即応する**（2026-08-23・精度不変・spec 追記7。実測 9路監視対局 `game_20260823_024656`: ヨセ前 0.0〜0.4秒 に対しヨセ 0.7〜1.7秒で、支配項はヨセ判定クエリ Probe〈2000visits・ownership=True・wRN=0〉の **1.1秒**＝ownerMap の有無が違うので既存の応手先読みでは温まらない。(1) cap は `min(max_loss, lead−target)` なので余剰が `max_loss + ENIGMA9_FAST_YOSE_MARGIN`(0.5) 以上＝**飽和帯では cap が lead によらず max_loss** ＝ `enigma9_yose_probe_skippable` が真の手番だけ Probe を省き通常解析 root の scoreLead〈wRN=0.04〉で決める＝**採用判断はビット同一のままクエリ0本**。適用は ヨセ sticky 後 × `game.board_watch_active` の AND で、**接戦〈余剰がそれ未満＝lead がそのまま予算になる帯〉は従来どおり Probe を撃つ**＝目差が予算を決める局面の精度は落とさない。実測14手番は lead 5.77〜34.48・cap 全部1.60＝全部が飽和側。(2) 接戦で残る Probe は `game.board_watch_probe_warm` を立てて監視モードの応手先読み `_board_watch_prefetch_worker` に同条件〈ownership=True・wRN=0・visits既定〉の温めを1本足させる〈結果は捨てる・的中率 top-5 68.7%〉。ponder でなく先読み側に置いたのは、接戦のヨセが `cap <= MIN_BUDGET` の早期 return に倒れて ponder が発火しないため）。さらに Jigo/Jigo9 には**終盤ヨセの9段委譲オプション** `jigo_endgame_humanstyle` を追加（ヨセで target に合わせる手抜きは1手の目数がほぼ確定していて手順も一本道なぶん相手から見て露骨なので、指定手数〈`jigo_endgame_move` 19路150 / `jigo_endgame_move_13` 13路85 / `jigo9_endgame_move` 9路30〉以降かつ目差が **ユーザー設定の `target_score`**〈deception の eff_target ではない＝phase1/2 の eff_target は負なので即委譲してしまう〉以上になった手番から HumanStyle 9段へ丸ごと委譲する。判定は Stage1 発行前・目差は前手のキャッシュ＝1手ラグ。**劣勢のうちは jigo を継続**＝`lead < target_score` の jigo は target 最接近手＝実質最善手なので手抜きは起きず deception の挽回も取りこぼさない。一度委譲したら戻らない sticky）。

難解 / 難解＋（9/13/19路共通）には**序盤の賭け罠オプション** `<prefix>_gamble_until_move` / `_gamble_min_winrate` / `_gamble_min_delta_e` がある（2026-09-17・既定 OFF＝ビット同一・spec `2026-09-17-enigma-gamble-design.md`）。窓（手数 < until_move）× ヨセ前 × 消費モードでない手番で、「E を最善手より min_delta_e(0.5) 目以上多く買い・十分な応手の hp が 0.25 以下・正しく応じられても勝率が min_winrate(35%) を割らない」挑戦者を net 比較を飛ばして打つ（`enigma9_gamble_pick`＝順位は ΔE − 0.5·vloss）。cap は動かさず、正しく応じられたら素直に劣勢を受け入れて従来の選択で逆転を狙う＝演出用のオプション。起点のユーザー要望は「応じられたら 35%・応じ損ねたら優勢」だったが、実測（難解＋13路 18 局のログ再集計）で**その在庫は序盤にほぼ無い**（着手後勝率 30〜45% の 293 候補で E>=1.0 は 1 手。E>=2 の大きい罠は全部 着手後も勝率 50% 以上＝既に優勢な局面のもので消費モードが打っている）ので、「応じ損ねたらほぼ互角に戻る」本物の罠まで緩めた版を入れた（約 0.9 回/局・平均 vloss 0.89・ΔE 0.71）。rarity 項を資格にも順位にも使わないのは追記12 の失敗モード（珍しさだけに払う外し）を避けるため。13路序盤の勝率勾配は約 19%/目＝35% は互角から 0.8〜1.0 目損で、現行 cap と勝率フロアは元々これを許している（止めていたのは net 比較）。**実戦校正は未実施**。

難解 / 難解＋（9/13/19路共通）には**捨て身の罠オプション** `<prefix>_overdraft_deficit` / `_overdraft_answered_max` / `_overdraft_min_fooled_lead` / `_overdraft_probes` がある（2026-09-18・既定 OFF＝ビット同一・spec `2026-09-18-enigma-overdraft-design.md`）。ヨセ前 × 勝勢の消費モード（`cost_weight < 1`）の手番で、リードの予算（lead − target）を超えて払う罠＝「正しく応じられた後のリードが [−D, min(target, answered_max))・十分な応手の hp が 0.25 以下・引っかかった場合（十分でない応手の humanSL 重みつき平均 `enigma9_fooled_punish`）のリードが下限以上・1 手の損失は large_lead_max_loss の 1.5 倍まで」を、通常上限の外側から追加プローブで探し、資格があれば net 比較と勝率フロアを飛ばして `lead_after + E` 最大の手を打つ（`enigma9_overdraft_pick`）。序盤の賭け罠（`cost_weight >= 1`）とは排他。正しく応じられたら素直に劣勢になり、消費モードが切れて実質最善手で打つ＝「リスクを取って、応じられたら負ける」演出用のオプション。過去の2案（2026-09-03 の接戦での cap 緩和・賭け罠の文字どおり版）は在庫なしだったが、**勝勢の局面で予算の外まで払う本案は在庫がある**（13路 18 局の反実仮想: 必ずマイナス・下限 2 目・6 プローブで 0.82 回/局・55% の局で 1 回以上。リード 3.5 目以下は 126 手番で 0 件）。ただし**モデル自身の見積もりで毎回 3〜4 目の期待値の損**（E − vloss 中央値 −4.25）で、「引っかかれば現リードを維持」まで満たす手はほぼ無い。プローブ前に罠を見分ける特徴は無い＝発動回数はプローブ数に比例。**実戦校正は未実施**（9路はほぼ発動しない見込み・19路は未計測）。

難解には**改良版「難解＋」`ai:enigma9plus` / `ai:enigma13plus` / `ai:enigma19plus`**がある（2026-09-02・spec 追記12〜12-3。
実装は `EnigmaPlusMixin` を基底の難解に先置きしたサブクラス）。差分は2つ。(1) **罠探索の拡張**＝子局面プローブの挑戦者を「安い順 7 手」
＋「残りの候補から loss の範囲で等間隔に `probe_extra`(4) 手」にする（`_shortlist`・spec 追記12-2。従来は cap 5 目の
手番でも 2 目より高い手を調べておらず、E>=2 の罠は高い帯に多い＝vloss<0.3 で 6%・4目超で 21%。代金の上限は
不変。先読み wave2 も同じ規則で温めるので即着手は保たれる）。(2) **ΔE 床**＝挑戦者を選択関数に渡す前に
「E − 最善手の E >= `min_delta_e`(0.2) または vloss <= `cheap_loss`(0.3)」に絞る（`_filter_challengers`）。
どちらも基底は no-op＝既存の難解 9/13/19 路はビット同一。起点は 13路7局の実測: E は較正済み（実損失 ≈ 1.06×E）だが、
`net = E + rarity − cost_weight·vloss` の rarity 項と消費モードの cost_weight の組合せで
「最善手より E の低い手を珍しさだけで 3〜7 目払って打つ」外しが 200 手中 20 手・29.6 目あった
（realized gain −0.52 目。ΔE>=0.2 側は +0.79 目）。`cheap_loss` の免除は序盤の安い定跡外し（E が全候補
0.05 前後）を残すため。変わらない手番はビット同一なのでシャッフルは起きない。`min_delta_e` 0 で
難解と同一。13路は実対局 難解13局 vs 難解＋14局（後半15局は交互）で 相手の実損失/手 2.25→3.23（+1.0・z 1.9〜2.7）・>=2目の失着 41%→54%・vloss/手 0.93→0.85・E/手 1.66→1.84（z 0.7＝有意でない）＝**同じ代金で相手の実損失が約 1 目/手増える**を約 2σ で確認（spec 追記12-5）。9路は交互 6 vs 5 局で利点なし（追記12-4）。
9/19 路版は同じ既定値で展開＝**未校正**（追記12-3）。

13路には**相手より低い一致率で勝つ専用戦略 `ai:mimic13`（擬態（13路））**がある（2026-09-17・spec `2026-09-17-mimic13-strategy-design.md`。実装は `Mimic13Strategy(Enigma13Strategy)` が `_generate_move` を上書きし、難解の子局面プローブ・先読み・終局帯〈`_terminal_band_move` に抽出して共有〉を再利用）。不一致1回の値段を `price = max(0, vloss) − (E − E_best)`〈難解の net から rarity 項を外したものと同値。own_rare は「humanPolicy の低い手ほど加点」で人間らしさと逆向き、reply_rare は E を与件にすると寄与ゼロ〉で測り、**リード連動の支払い上限 λ**〈`mimic_price_cap`: lead < −1 → 0／余剰なし → `free_loss` 0.3／余剰あり → min(`max_loss` 2.0, 余剰 × `spend_rate` 0.25)。余剰 = lead − `reserve` 3.0〉の内側で、**資格**〈自然＝hp >= max(`min_human_policy` 0.05, `natural_ratio` 0.2 × 第一感トップ)／罠＝ΔE >= `trap_min_delta_e` 0.5 かつ hp >= 0.005＝相手を騙す手は人間らしさを免除〉のある最安の手へ外す（`cost_slack` 0.3 以内の帯は hp 最大＝parity9 の「最安バンド内 humanPolicy 最大」と同型）。price が負＝期待値プラスの罠は余剰が無くても打つ＝**相手の悪手を誘って外しの原資を作る**。**支配ガード**: 最善手の hp >= `dominant_hp` 0.8 の手番は罠も含めて外さない（親 humanSL 8visits を先に1本撃つのでプローブ0本）。ヨセ（手数 >= 85 AND 未確定点 <= 16・sticky `game._mimic13_endgame`）は HumanStyle 9段へ丸ごと委譲し、`lead < reserve` の手番だけ最善手（85 は HumanStyle 自身の終局閾値＝それ未満だと委譲先が hp 重みのランダム選択になる）。一致率（`parity9_match_tally`）は毎手ログに出すだけで判定に使わない。設計の根拠は 13路難解ログ 18 局の在庫実測: 中盤 443 手番で 自然な非最善手が居る 67〜79%〈下限〉・タダ同然 or 期待値プラスの罠 約40%・最善手 hp>=0.8 が 22%、リードは典型 +3〜+35 ＝**律速は予算ではなく自然な代替手の在庫**。**実戦校正は未実施**（相手の一致率が未実測）。

9/13/19路には**勝ち優先で自分の一致率を絶対目標まで下げる戦略 `ai:veil9` / `ai:veil13` / `ai:veil19`（韜晦）**がある（2026-09-23・spec `2026-09-23-veil-strategy-design.md`。擬態の後継だが擬態は無変更）。実装は `Veil9Strategy(Enigma9Strategy)` が `_generate_move` を上書きし、難解の子局面プローブ・相手の直前パスの終局処理を再利用、ponder と HumanStyle 委譲は使わない。一致率は終局レポートと同一定義（`veil_tally`。合成木で `game_report` との完全一致をテストで固定）。**同値外し（free）は一致率に関係なく常に、損をする外し（paid）は一致率が目標を超えている間だけリードの余剰から**払い、最善手の hp >= dominant_hp（明らかな一手）は目標超のときだけ dominant_max_loss まで。候補は自然さの床を満たす手だけを子局面で検証し、最安帯の中で hp 最大。決着局面はプローブなしで即決、終局帯はダメ詰めの順番入れ替え（生 loss 0.10・接戦 0.05 まで。9段の締めの手が最善手でないときも同じ `veil_terminal_swap` の条件＝ゲート・visits・損失上限・接戦の累計上限を通す）。罠は `trap_mode`（既定 OFF・A/B 用）の上乗せ層で、値段は ΔE の半分だけ割り引き安全条件は割り引かない。全フェイルセーフと不変条件違反は最善手。毎手 `Decision: {json}` と `last_decision_info` と `game._veil_state[prefix]` の ledger を残す（自己対局ハーネスが読む）。既定値とハーネスでの境界線は `ai-parameters.md` と `docs/superpowers/specs/calibration-data/selfplay/veil13-campaign.md`。13路のハーネス段階1（20局 × 5アーム）では既定の自分の一致率 局平均 53.1%（相手 26.3%・20/20 勝ち）で目標 30% に届かず、攻めプリセットでも 42.2%（難解＋13路 49.7%・HumanStyle 9段 53.2%）。下限は予算ではなく構造（最善手を打つしかない手番 52.7%＝候補が最善手だけ 26.5%・自然な別の手なし 16.8%・安全な外しなし 8.9%・ほか 0.6%。割合は個別に四捨五入）で、既定値は据え置いた。接戦での勝ちの安全（spec §10.4 (a)）は段階3（AI 不利 4 目・相手 HumanStyle 9段・難解＋13路との 20 対・信頼度 0.95）で既定の設定について**可**＝20-0（難解＋ 17-2-1）・flip_moves 0.20/局（難解＋ 2.35/局・対の差の 95% 上限 −1.39 <= 0.1）。20 対のハーネス測定だけで、攻めプリセットは接戦で測っていない。段階1b・2・3b（2026-09-24〜25）では、安全条件を変えずに一致率を下げたのは loose（安全条件以外を全部ゆるめた設定）の 42.4%（同じ run の既定 49.0%）だけで、接戦ストレスでも 20-0。単独のつまみでは下がらず、投了なしでも上がらない（47.4%）。既定を loose にするかはユーザーの選択待ち。**実戦校正は未実施**。

## 関連 spec

戦略系 spec の一覧（実装済み／REJECT／未実装の別つき）は `docs/superpowers/specs/INDEX.md` の「AI 戦略」節にある。

**注意**: `2026-05-23-maze-strategy-9x9-design.md`（迷路）と `2026-05-24-ko-strategy-design.md`（コウ）は**未実装**で、`MazeStrategy` / `KoStrategy` はコードに存在しない。Jigo Stage2 の既定解析置換（`2026-04-14-...` / `2026-04-15-...`）は2度とも **REJECT**。
