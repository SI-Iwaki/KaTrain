# 自己対局ハーネスの 9路対応・定跡を知る相手・窓の指標と、序盤の研究外しの計測 実装計画（Plan B）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

日付: 2026-09-26
実行: worktree `C:\Users\iwaki\Documents\katrain-1.17.1.1\katrain-1.17.1.1\.claude\worktrees\veil-opening`・ブランチ `veil-opening`。
**Plan A（`docs/superpowers/plans/2026-09-26-veil-opening.md`＝序盤の研究外しの層）をすべて終えてから、同じブランチで始める。**
Task 1〜10 はサブエージェント駆動（順に1つずつ、実装 → レビュー → 修正）。C1〜C10 は計測のキャンペーンで、**コントローラ（メインセッション）が
KaTrain を止めてから、ユーザーに確かめて**走らせる。master へのマージは C10 の後。

**Goal:** 自己対局ハーネスを 9路で使えるようにし（実戦の復元・事後解析・校正の区間と目標・相手プール・投了の手数）、ユーザーの仮説を
模した「定跡を知る相手」と序盤の窓の指標を足し、序盤の研究外し（`<prefix>_open_moves`）のアームの設定を再開と合算で固定したうえで、
spec §16.3〜16.5 の段階（9路 9-0〜9-5・13路 13-1〜13-4）を走らせて、ユーザーが 9路の既定値と研究外しの採否を決められる材料を作る。

**Architecture:** 数値の判断は純関数（`selfplay_stats.py`: 区間の境目・校正目標の表・盤の手数の比例・定跡の出口）に置き、TDD で固定する。
盤サイズ別の値は表（`CALIB_TARGETS`・`CALIB_TARGET_GAMES`・`RECON_DIRS`）で持ち、13路の値は変えない（テストで凍結）。定跡を知る相手
`BookOpponent` はプールの humanSL の相手を包むだけ。研究外しのアームは `resolve_arm` が任せる先（難解＋の節）の指紋を持ち、
`execute_plan`（再開）と `load_records`（合算）で突き合わせる。run / calibrate は使った config の写しを出力ディレクトリに置き、再開は
その写しを読む。実戦ログの復元・事後解析・校正目標・窓の指標・調査の集計は `calibration-data/selfplay/` のスクリプト。

**Tech Stack:** Python 3.12・pytest（偽エンジン＋本物の `Game`／KataGo 不要）・標準ライブラリ・自己対局ハーネス
`python -m katrain_debug.selfplay`（KataGo v1.18.2 CUDA・b10c384・humanSL・max_visits 2500＝ユーザーのローカル設定）。

**Spec:** `docs/superpowers/specs/2026-09-23-veil-strategy-design.md` の **§16（正本）**と §15（序盤の研究外し・Plan A が実装）。
文脈は §3.1・§4・§5・§6.1（9路の列）・§11・§13.2・§14.2。ハーネスの設計は `docs/superpowers/specs/2026-09-23-selfplay-harness-design.md`
（§3 校正・§5 出力・§6 A/B）。

## Global Constraints

1. **ファイルの種類ごとの書き方**（black フック＝Edit / Write で `.py` を書くと PostToolUse が `black --line-length 120` をファイル全体に掛ける）:
   - **black 整形済み（CRLF）＝Edit ツールで直接変えてよい**: `katrain_debug/selfplay.py`・`selfplay_run.py`・`selfplay_game.py`・
     `selfplay_opponent.py`・`selfplay_stats.py`・`tests/test_selfplay_*.py`・`tests/selfplay_fakes.py`・
     `docs/superpowers/specs/calibration-data/selfplay/recon_logs.py`（2026-09-26 に `python -m black -l 120 --check` で確認済み）。
     この計画のコードは black 済み（フックは何も変えない）。変えた後に `file <path>` で CRLF のままか確かめる。
   - **black 未整形（CRLF）＝python のパッチスクリプトだけで変える**: `calibration-data/selfplay/offline_report.py`・`calib_targets.py`・
     `README.md`・`veil13-campaign.md`・`CLAUDE.md`・`docs/superpowers/specs/*.md`（spec・INDEX）。
     道具は `crlf_patch.py`（`patch(path, [(old, new, count)])`＝old/new は LF で書く。件数が合わなければ何も書かずに止まる。
     `append(path, text)`）。**コピー元: `C:\Users\iwaki\AppData\Local\Temp\claude\C--Users-iwaki--katrain\fa4754c0-234e-47cd-8f07-eff2747ac3ec\scratchpad\crlf_patch.py`**。
     自分のセッションの scratchpad ディレクトリ（システムプロンプトの「Scratchpad directory」。以下 `<scratchpad>`）にコピーして使う
     （`cp <コピー元> <scratchpad>/crlf_patch.py`）。コピー元が消えていたら Plan A の File Structure 節の全文で作り直す。
     パッチスクリプトは `<scratchpad>/patch_9x9_tN.py` に書き、**worktree のルートを cwd にして** `python <scratchpad>/patch_9x9_tN.py`。
     スクリプトの先頭で `sys.path.insert(0, r"<scratchpad の実パス>")` してから `from crlf_patch import patch, append`。
   - **新しいファイル**（`window_stats.py`・`enigma9_log_stats.py`・`opp_bot_match9.py`・`tests/test_selfplay_window.py`・
     `tests/test_selfplay_recon.py`）は Write ツールで書く（LF のままでよい。git の autocrlf が扱う）。`recon_logs.py` は Task 7 で Write で全体を
     書き換える（black 済みのファイルを black 済みの全文で置き換える）。`book9/` の2本は `cp` でコピーするだけ（中身を変えない）。
2. **Plan A の持ち物に触らない**: `katrain/core/ai.py`・`katrain/core/constants.py`・`katrain/config.json`・i18n・`katrain/gui/ai_help.py`・
   マニュアル・`.claude/rules/*.md`・`tests/test_ai_veil.py`・`selfplay_stats.py` の `_decision_metrics`（と Plan A が足す `_veil_unpaid`・
   `_opening_metrics`）。**難解（Enigma*）・擬態（Mimic13）・韜晦のコードは変えない**（この計画は ai.py から `VEIL_OPEN_DELEGATES` を
   import するだけ）。
3. **13路の今の振る舞いを変えない**: `CALIB_TARGETS_13` の値・校正用区間（13路 24 / 85 手）・13路の投了の手数（`recon/` の 18局）・
   `calib_targets.py` の 13路の既存の出力 32 行・コミット済みの `recon/`（上書きしない）。テストで凍結する。
4. KataGo・KaTrain・ハーネスを Task 1〜10 では起動しない（テストはすべて偽エンジン。`offline_report.py` は `summarize` だけ、
   `calib_targets.py` は合成の report で走らせる）。**`C:\Users\iwaki\.katrain\config.json`（ユーザー設定）は読むだけ**（C1 で写しを作る）。
5. 既存のテストを弱めない。この計画で書き換える既存のテストは次の 5 つだけで、どれも仕様の変更（9路の投了の手数が recon_9 から来る・
   9路の lead モデルは範囲が要る）に合わせるもの: `test_selfplay_run.py::TestSummaries::test_resign_lengths_are_the_real_game_lengths`・
   `test_selfplay_cli.py` の fixture `env`・`TestResignModel::test_length_model_is_the_default`・
   `TestResignModel::test_runs_with_different_resign_models_are_not_merged`・`TestMeasurementBaseline::test_run_json_paths_are_relative_to_the_repo`。
6. コミットは日本語の Conventional Commits、末尾に `Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>`
   （`git commit -m "<件名>" -m "<本文>" -m "Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>"`）。
7. テスト: 自分のタスクの focused テスト（下の各 Step）と `python -m pytest tests/test_selfplay_*.py -q -p no:cacheprovider`。
   全体スイートは Task 10 と C1 で流す（`--ignore=tests/test_ai.py`＝humanSL モデルが要る既知の対象外）。
8. **シェル**: `Run:` 行と ```bash のブロックはすべて **Bash ツール（Git Bash）** で実行する（glob・`tail`・`> file 2>&1` のため）。
   KaTrain の確認だけ PowerShell（C1）。数分を超える実行（C2 の事後解析・C4 の校正・C5〜C8 の段階）は Bash の `run_in_background: true` で
   出力をファイルへ落とし、`tail -3 <file>` で進み具合を見る（前面の実行は 600000 ms で打ち切られる）。
9. 行番号は 2026-09-26 のブランチ先頭（`76105598`・Plan A の前）で確かめた値。Plan A は `selfplay_stats.py` の `_decision_metrics`
   （466 行〜）を変えるので、それより後ろの行番号はずれる。Edit の old（とパッチの old）は文字列で当てる。
10. ツールの本文に Unicode エスケープ（バックスラッシュ＋u＋16進4桁）を書かない。非 ASCII は文字そのものを書く。

## Plan A から受け取るもの（共有インターフェース・名前は Plan A と同じ）

- 設定キー `f"{prefix}_open_moves"`（prefix は veil9 / veil13 / veil19・int・コードの既定は3盤とも 0）。`Veil9/13/19Strategy.SETTING_DEFAULTS`
  の forced の4キーの直後（最後）にある＝`resolve_arm` のコードの既定値の重ね（`effective_settings`）に入る。
- `katrain/core/ai.py` のモジュール定数 `VEIL_OPEN_DELEGATES = {9: AI_ENIGMA_9_PLUS, 13: AI_ENIGMA_13_PLUS, 19: AI_ENIGMA_19_PLUS}`
  （値は `"ai:enigma9plus"` など）。韜晦のクラスの盤は `BOARD_LEN`（9 / 13 / 19・ai.py の既存の属性）。
- 任せた手番の記録（moves.jsonl の列 `decision_<field>`）: `decision_tier` `"opening"`・`decision_kind` `"opening"` / `"best"`・
  `decision_open` `"played"` / `"invariant"` / `"gate"`・`decision_open_index`・`decision_open_src`・`decision_open_in_cands`・
  `decision_open_secs`・`decision_open_thoughts`・`decision_lead`・`decision_root_wr`。窓の外の手番と `open_moves` 0 は `decision_open*` の列が無い。
  既存の列 `decision_u`（緊急度）・`decision_kind` `"paid"`（支払う外し）も使う。
- 窓の中の手を打つのは `VeilOpenEnigma9PlusStrategy` など（ログの `[VeilOpenEnigma9PlusStrategy] ...` 行はハーネスの `logs/` に残る）。

## File Structure

| ファイル | 責務 | 変更 |
|---|---|---|
| `katrain_debug/selfplay_stats.py` | 純関数 | Task 1: `calib_bin_moves`・`CALIB_TARGETS`・`CALIB_TARGET_GAMES`・`calib_targets_for`。Task 2: `scale_moves`・`resign_lengths_from_summaries` の盤。Task 4: `BOOK_LOSS`・`book_exit_depth`。C3: `CALIB_TARGETS_9` |
| `katrain_debug/selfplay_run.py` | 実行計画・出力・再開・要約・校正 | Task 2: `RECON_DIRS`・`recon_dir_for`・投了の標本の読み先。Task 3: `calibration_result`・`pool_file_content`・`format_calibration_md` を盤別に。Task 4: `plan_conditions` に定跡のキー。Task 5a: `delegate_fingerprint`・再開と合算の任せる先の突き合わせ。Task 5b: `CONFIG_SNAPSHOT`・`file_sha256`・`OutputDir.snapshot_config` / `pinned_config` |
| `katrain_debug/selfplay.py` | CLI | Task 2: `resign_plan` の盤。Task 3: calibrate `--strategy NAME:k=v,...`。Task 4: `book_plan`・`--book-moves` / `--book-loss`。Task 5a: `_plan_warnings`（任せる先の note）。Task 5b: `_new_output`（写し）・`_resume`（写しを読む） |
| `katrain_debug/selfplay_game.py` | 1局・アーム | Task 4: `make_opponent` が `BookOpponent` で包む。Task 5a: `Arm.delegate`・`open_delegate`・`resolve_arm` |
| `katrain_debug/selfplay_opponent.py` | 相手ボット | Task 4: `BookOpponent` |
| `tests/test_selfplay_stats.py`・`test_selfplay_run.py`・`test_selfplay_cli.py`・`test_selfplay_opponent.py`・`test_selfplay_runner.py` | テスト | Task 1〜5b |
| `docs/superpowers/specs/calibration-data/selfplay/window_stats.py` + `tests/test_selfplay_window.py` | 窓の指標（ハーネスの実行ディレクトリ・`--recon` で実戦の事後解析） | Create（Task 6） |
| `docs/superpowers/specs/calibration-data/selfplay/recon_logs.py` + `tests/test_selfplay_recon.py` | 実戦ログの復元（`--log-dir`・`--out`・`--size`・`--strategy`・9路の直し） | Rewrite / Create（Task 7） |
| `calibration-data/selfplay/offline_report.py`・`calib_targets.py` | 事後解析・校正目標（盤別の区間・出力先・設定・目標の1行） | Modify（Task 8・パッチ） |
| `calibration-data/selfplay/enigma9_log_stats.py`・`opp_bot_match9.py`・`book9/book9.py`・`book9/book9deep.py` | §16.1 の調査の集計 | Create / Copy（Task 9） |
| `calibration-data/selfplay/README.md`・`CLAUDE.md`・`2026-09-23-selfplay-harness-design.md`・`INDEX.md` | 文書 | Task 9・10（パッチ） |
| `calibration-data/selfplay/recon_9/`・`opponent_pool_9.json`・`selfplay-calibration-results-<日付>-9x9.md`・`veil9-campaign.md`・`veil9-campaign/`・`veil13-campaign.md` | 計測のデータと記録 | C2〜C9 |

タスクの順: 1 → 2 → 3 → 4 → 5a → 5b → 6 → 7 → 8 → 9 → 10 → C1 → … → C10。Task 1〜10 は前のタスクのコードに依存する（2 は 1 の `scale_moves` を使わない
が同じファイルを触る・3 は 1 の `calib_targets_for`・8 は 1 の `calib_bin_moves`・5a は Plan A の `VEIL_OPEN_DELEGATES`・5b は 5a が足した
`delegate_fingerprint` の直前に書く）。

---

## Task 1: 校正用の区間と校正目標を盤サイズ別にする（TDD）

状態: 完了（`20dfce89`）。

**Files:**
- Modify: `katrain_debug/selfplay_stats.py`（`POOL_TOLERANCE`〈59 行〉の直後・`depth_bin`〈77-85 行〉の直後）
- Test: `tests/test_selfplay_stats.py`（`TestBoardConstants.test_calibration_bins_match_game_report_boundaries`〈27-32 行〉の直後）

**Interfaces:**
- Consumes: 既存の `CALIB_BINS`（37 行・割合 24/169・85/169）・`CALIB_TARGETS_13`（45 行）・`depth_bin(depth, size, bins=CALIB_BINS)`。
- Produces（Task 3・8・C3 が使う）:
  - `calib_bin_moves(size) -> tuple[int, int]`（13路 (24, 85)・9路 (12, 41)・19路 (52, 182)）
  - `CALIB_TARGETS: dict[int, dict]`（`{13: CALIB_TARGETS_13}`。C3 で 9 を足す）
  - `CALIB_TARGET_GAMES: dict[int, str]`（`{13: "16/18"}`）
  - `calib_targets_for(size) -> dict | None`

- [ ] **Step 1: 失敗するテストを書く**（`tests/test_selfplay_stats.py` の 32 行 `        assert S.depth_bin(11, 9) == "cal_opening" and S.depth_bin(12, 9) == "cal_middle"` の後に、空行1つを挟んで足す）

```python
    def test_calibration_bin_moves_by_board_size(self):
        """spec 2026-09-23-veil-strategy-design.md §16.2 手順3: 13路の境目は変えない・9路は 12 / 41 手。"""
        assert S.calib_bin_moves(13) == (24, 85)
        assert S.calib_bin_moves(9) == (12, 41)
        assert S.calib_bin_moves(19) == (52, 182)
        for size in (9, 13, 19):
            lo, hi = S.calib_bin_moves(size)
            for depth in range(1, size * size + 1):
                want = "cal_opening" if depth < lo else "cal_middle" if depth < hi else "cal_endgame"
                assert S.depth_bin(depth, size) == want

    def test_calibration_targets_by_board_size(self):
        assert S.calib_targets_for(13) is S.CALIB_TARGETS_13 and S.CALIB_TARGET_GAMES[13] == "16/18"
        assert S.calib_targets_for(19) is None  # 目標の無い盤は calibrate がプールを選ばない
        assert S.CALIB_TARGETS_13 == {  # 13路の目標は変えない
            "opp_mean": 0.231,
            "opp_sd": 0.065,
            "opp_loss": 1.77,
            "opp_ge2": 0.28,
            "opp_ge5": 0.10,
            "ai_mean": 0.533,
            "moves_median": 78.5,
            "bins": {
                "cal_opening": {"opp_top1": 0.267, "opp_loss": 0.56},
                "cal_middle": {"opp_top1": 0.182, "opp_loss": 2.43},
                "cal_endgame": {"opp_top1": 0.291, "opp_loss": 0.96},
            },
        }
```

- [ ] **Step 2: 失敗を確かめる**

Run: `python -m pytest tests/test_selfplay_stats.py -q -p no:cacheprovider -k "bin_moves or targets_by_board"`
Expected: 2 failed（`AttributeError: module 'katrain_debug.selfplay_stats' has no attribute 'calib_bin_moves'` と `... 'calib_targets_for'`）。

- [ ] **Step 3: 実装**（Edit ツール）

`selfplay_stats.py` の 59 行 `POOL_TOLERANCE = {"opp_mean": 0.01, "opp_sd": 0.02, "opp_loss": 0.25}  # プール選択のスコアの尺度` の直後に足す:

```python
# 盤サイズ → 校正目標（spec 2026-09-23-veil-strategy-design.md §16.2 手順4。9路は recon_9 から足す）。無い盤はプールを選ばない
CALIB_TARGETS = {13: CALIB_TARGETS_13}
# 盤サイズ → 目標の元の局数（AI 側 / 相手側）。calibration.md の「実戦（目標）」の行に出す
CALIB_TARGET_GAMES = {13: "16/18"}
```

`depth_bin` の最後（83-85 行）

```python
        if lo <= depth < hi:
            return name
    return None
```

の直後に足す（空行2つを挟む）:

```python
def calib_bin_moves(size):
    """校正用の区間の境目の手数 (lo, hi): 序盤 = 手数 < lo・中盤 = lo <= 手数 < hi・終盤 = hi <= 手数。

    game_report と同じ ceil(割合 × 盤面積)（割合は CALIB_BINS の 24/169・85/169）。13路 (24, 85)・9路 (12, 41)・19路 (52, 182)。
    """
    return tuple(math.ceil(f * size * size) for f in (CALIB_BINS[0][1][1], CALIB_BINS[2][1][0]))


def calib_targets_for(size):
    """盤サイズの校正目標（CALIB_TARGETS に無い盤は None＝calibrate はプールを選ばない）。"""
    return CALIB_TARGETS.get(size)
```

（`CALIB_BINS` は割合のまま変えない。`game_report` の `depth_filter` が割合を受け取り、`ceil(割合 × 盤面積)` で手数に直す＝9路は
すでに 12 / 41 手。盤サイズ別の値は `calib_bin_moves` で出して、スクリプトと窓の指標が同じ境目を使う。）

- [ ] **Step 4: 通ることを確かめる**

Run: `python -m pytest tests/test_selfplay_stats.py -q -p no:cacheprovider`
Expected: すべて PASS。

- [ ] **Step 5: コミット**

```bash
git add katrain_debug/selfplay_stats.py tests/test_selfplay_stats.py
git commit -m "feat(selfplay): 校正用の区間の境目と校正目標を盤サイズ別に引けるようにする" -m "spec §16.2 手順3・4。calib_bin_moves（13路 24/85・9路 12/41）と CALIB_TARGETS。13路の値はテストで凍結。" -m "Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>"
```

---

## Task 2: 投了の手数を盤サイズ別の実戦から読む（TDD）

状態: 完了（`bfb039c6`）。

**Files:**
- Modify: `katrain_debug/selfplay_stats.py`（`scale_moves_13`〈67-69 行〉・`resign_lengths_from_summaries`〈315-321 行〉）
- Modify: `katrain_debug/selfplay_run.py`（`RECON_DIR`〈31 行〉・`load_resign_pool` / `load_resign_lengths`〈65-72 行〉）
- Modify: `katrain_debug/selfplay.py`（`resign_plan`〈123-154 行〉）
- Test: `tests/test_selfplay_stats.py`・`tests/test_selfplay_run.py`・`tests/test_selfplay_cli.py`

**Interfaces:**
- Produces:
  - `S.scale_moves(moves, size, from_size=13) -> int`（同じ盤なら縮めない）・`S.scale_moves_13(moves, size)` は `scale_moves(moves, size, 13)`（値は今と同じ）
  - `S.resign_lengths_from_summaries(summaries, size)`: summary の `board_size`（無ければ 13）から `size` へ比例
  - `R.RECON_DIRS = {9: <SELFPLAY_DATA>/recon_9}`・`R.recon_dir_for(size) -> str`・`R.load_resign_pool(size, recon_dir=None)`・`R.load_resign_lengths(size, recon_dir=None)`
  - CLI: 9路の `--resign-model lead` は `--resign-lead LO:HI` が要る（無ければ `SystemExit("--resign-model lead on 9x9 needs --resign-lead LO:HI ...")`）

- [ ] **Step 1: 失敗するテストを書く**

(a) `tests/test_selfplay_stats.py` の 208 行 `        assert S.resign_lengths_from_summaries(summaries, 19) == [round(123 * 361 / 169), round(31 * 361 / 169)]` の後に、空行1つを挟んで足す:

```python
    def test_resign_lengths_keep_the_board_of_the_summary(self):
        """spec §16.2 手順5: 9路の実戦（recon_9・summary.board_size 9）は 9路で縮めない。"""
        summaries = [{"ai": "B", "n_moves": 48, "board_size": 9}, {"ai": "W", "n_moves": 31, "board_size": 9}]
        assert S.resign_lengths_from_summaries(summaries, 9) == [48, 31]
        assert S.resign_lengths_from_summaries(summaries, 13) == [round(48 * 169 / 81), round(31 * 169 / 81)]
        assert S.scale_moves(78, 13) == 78 and S.scale_moves(40, 9) == 19 and S.scale_moves_13(40, 9) == 19
```

(b) `tests/test_selfplay_run.py` の `TestSummaries.test_resign_lengths_are_the_real_game_lengths`（416-420 行）を置き換える:

置き換え前:
```python
    def test_resign_lengths_are_the_real_game_lengths(self):
        lens = R.load_resign_lengths(13)  # 実戦 13路 18局（recon/report_game_*.json の summary.n_moves）
        assert len(lens) == 18 and statistics.median(lens) == 78.5 and (min(lens), max(lens)) == (31, 128)
        assert R.load_resign_lengths(9) == [round(n * 81 / 169) for n in lens]
        assert R.load_resign_lengths(19) == [round(n * 361 / 169) for n in lens]
```
置き換え後:
```python
    def test_resign_lengths_are_the_real_game_lengths(self):
        lens = R.load_resign_lengths(13)  # 実戦 13路 18局（recon/report_game_*.json の summary.n_moves）
        assert len(lens) == 18 and statistics.median(lens) == 78.5 and (min(lens), max(lens)) == (31, 128)
        assert R.load_resign_lengths(9, R.RECON_DIR) == [round(n * 81 / 169) for n in lens]
        assert R.load_resign_lengths(19) == [round(n * 361 / 169) for n in lens]

    def test_recon_dir_by_board_size(self, tmp_path, monkeypatch):
        """spec §16.2 手順5: 9路の投了モデルは recon_9/（9路の実戦）を縮めずに読む。19路は 13路の recon/ を縮める。"""
        assert R.repo_relpath(R.recon_dir_for(9)) == "docs/superpowers/specs/calibration-data/selfplay/recon_9"
        assert R.recon_dir_for(13) == R.RECON_DIR and R.recon_dir_for(19) == R.RECON_DIR
        for i, n in enumerate((31, 48, 99)):
            summary = {"ai": "B", "n_moves": n, "final_score": 5.0, "board_size": 9}
            (tmp_path / f"report_game_{i}.json").write_text(json.dumps({"summary": summary}), encoding="utf-8")
        monkeypatch.setitem(R.RECON_DIRS, 9, str(tmp_path))
        assert R.load_resign_lengths(9) == [31, 48, 99]
```

(c) `tests/test_selfplay_cli.py`:
- fixture `env`（16-21 行）を置き換える（`from tests.selfplay_fakes import FakeEngine, make_stub` の行から）:

置き換え前:
```python
from tests.selfplay_fakes import FakeEngine, make_stub


@pytest.fixture
def env(tmp_path, monkeypatch):
    make_stub(tmp_path, **{"ai:policy": {}})  # tmp_path/config.json を書く
    monkeypatch.setattr(CLI, "start_engine", lambda stub: FakeEngine())
    monkeypatch.setattr(CLI, "katago_processes", lambda: [])
    return {"config": str(tmp_path / "config.json"), "out": str(tmp_path / "out"), "tmp": tmp_path}
```
置き換え後:
```python
from tests.selfplay_fakes import FakeEngine, make_stub

RECON_9_LENGTHS = (31, 48, 99)  # テストの 9路の実戦の手数（本物の recon_9/ は計測の準備で作る）


@pytest.fixture
def env(tmp_path, monkeypatch):
    make_stub(tmp_path, **{"ai:policy": {}})  # tmp_path/config.json を書く
    monkeypatch.setattr(CLI, "start_engine", lambda stub: FakeEngine())
    monkeypatch.setattr(CLI, "katago_processes", lambda: [])
    recon9 = tmp_path / "recon_9"
    recon9.mkdir()
    for i, n in enumerate(RECON_9_LENGTHS):
        summary = {"ai": "B", "n_moves": n, "final_score": 5.0, "board_size": 9}
        (recon9 / f"report_game_{i}.json").write_text(json.dumps({"summary": summary}), encoding="utf-8")
    monkeypatch.setitem(CLI.R.RECON_DIRS, 9, str(recon9))
    return {"config": str(tmp_path / "config.json"), "out": str(tmp_path / "out"), "tmp": tmp_path}
```
- `TestResignModel.test_length_model_is_the_default` の 161-162 行:

置き換え前:
```python
        lens = CLI.R.load_resign_lengths(9)  # 実戦 13路 18局の手数を 9路の盤面積に縮めたもの
        assert plan["resign"]["model"] == "length" and plan["resign"]["lengths"] == lens
```
置き換え後:
```python
        lens = CLI.R.load_resign_lengths(9)  # 9路の実戦の手数（recon_9・縮めない）
        assert lens == list(RECON_9_LENGTHS)
        assert plan["resign"]["model"] == "length" and plan["resign"]["lengths"] == lens
```
- `def test_resign_lead_applies_only_to_the_lead_model(self, env):`（175 行）の直前に足す:

```python
    def test_lead_model_on_9x9_needs_a_range(self, env):
        with pytest.raises(SystemExit, match="needs --resign-lead"):  # spec §16.2 手順5: 9路の実戦のリードの標本は無い
            CLI.main(_resign_args(env, "--resign-model", "lead"))

```
- `test_runs_with_different_resign_models_are_not_merged` の 186 行:

置き換え前:
```python
        CLI.main(_resign_args(env, "--resign-model", "lead", "--seed-base", "1002", "--label", "ext"))
```
置き換え後:
```python
        CLI.main(
            _resign_args(
                env, "--resign-model", "lead", "--resign-lead", "8:40", "--seed-base", "1002", "--label", "ext"
            )
        )
```
- `TestMeasurementBaseline.test_run_json_paths_are_relative_to_the_repo`（320-324 行）の先頭2行:

置き換え前:
```python
    def test_run_json_paths_are_relative_to_the_repo(self, env):
        CLI.main(_resign_args(env))
```
置き換え後:
```python
    def test_run_json_paths_are_relative_to_the_repo(self, env):
        args = _resign_args(env)
        args[args.index("--size") + 1] = "13"  # 13路の投了モデルは recon/（リポジトリの中）
        CLI.main(args)
```

- [ ] **Step 2: 失敗を確かめる**

Run: `python -m pytest tests/test_selfplay_stats.py tests/test_selfplay_run.py tests/test_selfplay_cli.py -q -p no:cacheprovider`
Expected: stats の新しいテストが `AssertionError: assert [23, 15] == [48, 31]`（`resign_lengths_from_summaries` は今もあり、9路の
summary を 13路の手数とみなして縮める＝最初の assert で落ちる）、run の `test_recon_dir_by_board_size` が
`AttributeError: ... 'recon_dir_for'`、CLI のテストは全件 ERROR（fixture の `CLI.R.RECON_DIRS` が無い＝`AttributeError`）。

- [ ] **Step 3: 実装**（Edit ツール）

(a) `selfplay_stats.py` 67-69 行を置き換える:

置き換え前:
```python
def scale_moves_13(moves, size):
    """13路の手数を盤面積で比例させる（9路 ×81/169・19路 ×361/169）。"""
    return round(moves * size * size / 169)
```
置き換え後:
```python
def scale_moves(moves, size, from_size=13):
    """from_size 路の手数を size 路の盤面積で比例させる（同じ盤なら縮めない）。"""
    if from_size == size:
        return moves
    return round(moves * size * size / (from_size * from_size))


def scale_moves_13(moves, size):
    """13路の手数を盤面積で比例させる（9路 ×81/169・19路 ×361/169）。"""
    return scale_moves(moves, size, 13)
```

(b) `resign_lengths_from_summaries` の docstring と本体（316-321 行）を置き換える:

置き換え前:
```python
    """実戦の report_game_*.json の summary 群 → 局の手数（summary.n_moves）の標本（length モデル）。

    実戦の手数の分布そのもの（13路 18局・中央値 78.5）なので短い局も除かない。13路以外の盤は開始手数と同じく
    盤面積で比例させる（scale_moves_13）。
    """
    return [scale_moves_13(s["n_moves"], size) for s in summaries if s.get("n_moves")]
```
置き換え後:
```python
    """実戦の report_game_*.json の summary 群 → 局の手数（summary.n_moves）の標本（length モデル）。

    実戦の手数の分布そのもの（13路 18局・中央値 78.5）なので短い局も除かない。summary の盤（board_size・無ければ
    13路）と size が違えば盤面積で比例させ、同じ盤なら縮めない（9路の recon_9 は 9路の実戦の手数そのまま）。
    """
    return [scale_moves(s["n_moves"], size, s.get("board_size", 13)) for s in summaries if s.get("n_moves")]
```

(c) `selfplay_run.py` 31 行 `RECON_DIR = os.path.join(SELFPLAY_DATA, "recon")` の直後に足す:

```python
# 盤サイズ → 投了モデル（length の手数 L）の元データ。無い盤は 13路の recon/ を盤面積で縮めて使う（spec §16.2 手順5）
RECON_DIRS = {9: os.path.join(SELFPLAY_DATA, "recon_9")}
```

(d) `selfplay_run.py` 65-72 行を置き換える:

置き換え前:
```python
def load_resign_pool(size, recon_dir=RECON_DIR):
    """lead モデルの投了閾値 R の標本（実戦の投了局の最終リード）。"""
    return S.resign_pool_from_summaries(_recon_summaries(recon_dir), size)


def load_resign_lengths(size, recon_dir=RECON_DIR):
    """length モデルの目標の手数 L の標本（実戦 13路 18局の手数を盤面積で比例）。"""
    return S.resign_lengths_from_summaries(_recon_summaries(recon_dir), size)
```
置き換え後:
```python
def recon_dir_for(size):
    """盤サイズの実戦の復元と事後解析のディレクトリ（9路は recon_9/＝難解＋9路・それ以外は recon/＝13路）。"""
    return RECON_DIRS.get(size, RECON_DIR)


def load_resign_pool(size, recon_dir=None):
    """lead モデルの投了閾値 R の標本（実戦の投了局の最終リード）。"""
    return S.resign_pool_from_summaries(_recon_summaries(recon_dir or recon_dir_for(size)), size)


def load_resign_lengths(size, recon_dir=None):
    """length モデルの目標の手数 L の標本（9路は recon_9 の手数そのまま・19路は 13路 18局を盤面積で比例）。"""
    return S.resign_lengths_from_summaries(_recon_summaries(recon_dir or recon_dir_for(size)), size)
```

(e) `selfplay.py` の `resign_plan`:
- docstring（124-128 行）を置き換える:

置き換え前:
```python
    """投了モデル（--resign-model）:
    - length（既定）: 局ごとに目標の手数 L を実戦 13路 18局の手数から引き、L 以降に AI が明らかに勝っていれば投了。
    - lead: 局ごとに閾値 R を実戦の投了局の最終リード（または --resign-lead LO:HI の一様）から引く。
    --no-resign ならどちらでもなく投了しない（model "none"）。
    """
```
置き換え後:
```python
    """投了モデル（--resign-model）:
    - length（既定）: 局ごとに目標の手数 L を実戦の手数（9路は recon_9 の難解＋9路・ほかは 13路 18局を盤面積で比例）
      から引き、L 以降に AI が明らかに勝っていれば投了。
    - lead: 局ごとに閾値 R を実戦の投了局の最終リード（または --resign-lead LO:HI の一様）から引く。9路は
      --resign-lead が要る（実戦の最終リードの標本は 13路だけ・spec §16.2 手順5）。
    --no-resign ならどちらでもなく投了しない（model "none"）。
    """
```
- 135 行 `    source = R.repo_relpath(R.RECON_DIR)` → `    source = R.repo_relpath(R.recon_dir_for(size))`
- 151 行 `    pool = R.load_resign_pool(size)` の直前に足す:

```python
    if size == 9:
        raise SystemExit("--resign-model lead on 9x9 needs --resign-lead LO:HI (no real-game lead pool for 9x9)")
```

- [ ] **Step 4: 通ることを確かめる**

Run: `python -m pytest tests/test_selfplay_*.py -q -p no:cacheprovider`
Expected: すべて PASS。

- [ ] **Step 5: コミット**

```bash
git add katrain_debug/selfplay_stats.py katrain_debug/selfplay_run.py katrain_debug/selfplay.py tests/test_selfplay_stats.py tests/test_selfplay_run.py tests/test_selfplay_cli.py
git commit -m "feat(selfplay): 9路の投了の手数を recon_9 の実戦から縮めずに読む" -m "spec §16.2 手順5。summary の board_size で比例を決め、9路の lead モデルは --resign-lead を要る形にする。" -m "Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>"
```

---

## Task 3: 校正（calibrate）を盤サイズ別にし、AI の設定の上書きを受ける（TDD）

状態: 完了（`23924612`）。

**Files:**
- Modify: `katrain_debug/selfplay_run.py`（`calibration_result`〈527-551 行〉・`pool_file_content`〈554-569 行〉・`format_calibration_md`〈572-630 行〉）
- Modify: `katrain_debug/selfplay.py`（`_plan_calibrate` 261 行・`cmd_calibrate` 316 行・`--strategy` 450 行）
- Test: `tests/test_selfplay_run.py`・`tests/test_selfplay_cli.py`

**Interfaces:**
- Consumes: Task 1 の `S.calib_targets_for`・`S.CALIB_TARGET_GAMES`。既存の `S.parse_arm(text) -> (name, strategy, items)`・`resolve_arm(stub, name, strategy, override_items)`・
  `S.selfplay_pool_choice(rank_stats, targets=..., k=3, tolerance=...)`。
- Produces:
  - `calibration_result(out, plan)` の dict に `"strategy_overrides": list[str]`・`"targets": dict | None`（盤の目標）・`"targets_games": str | None`。
    目標の無い盤は `choices` 空・`best` None・`harness_drift_ai` None。
  - `pool_file_content(cal)` に `"strategy_overrides"`。
  - `format_calibration_md(cal)`: 題に `（上書き k=v, ...）`・「実戦（目標）」の行の局数は `targets_games`・目標の無い盤は行を出さない。
  - CLI: `calibrate --strategy NAME[:key=val,...]`（ラベルの既定は `calib-<NAME>`）。

- [ ] **Step 1: 失敗するテストを書く**

(a) `tests/test_selfplay_run.py` の `def test_calibration_result_and_markdown_carry_integrity_totals(self, tmp_path):`（481 行）の直前に足す:

```python
    def test_calibration_targets_follow_the_board_size(self, tmp_path, monkeypatch):
        """spec §16.2 手順4: 目標・「実戦（目標）」の局数・ずれ（harness_drift_ai）は盤サイズの目標から。"""
        out = R.OutputDir(tmp_path / "cal")
        recs = [
            _cal_record(rank=rank, seed=i, opp_top1=0.1 + 0.05 * i)
            for i, rank in enumerate(("rank_8k", "rank_3k", "rank_1d"))
        ]
        with open(out.file("games.jsonl"), "w", encoding="utf-8") as f:
            f.writelines(json.dumps(r) + "\n" for r in recs)
        targets_9 = {**S.CALIB_TARGETS_13, "ai_mean": 0.6, "moves_median": 48}
        monkeypatch.setitem(S.CALIB_TARGETS, 9, targets_9)
        monkeypatch.setitem(S.CALIB_TARGET_GAMES, 9, "7/16")
        plan = {
            "size": 9,
            "arms": [{"name": "calib", "strategy": "enigma9plus", "override_items": ["enigma9plus_max_loss=1.6"]}],
            "opponent": {"tau": 1.0},
        }
        cal = R.calibration_result(out, plan)
        assert cal["targets"] is targets_9 and cal["best"]["ranks"] == ["rank_8k", "rank_3k", "rank_1d"]
        assert abs(cal["harness_drift_ai"] - (0.55 - 0.6)) < 1e-9
        assert cal["strategy_overrides"] == ["enigma9plus_max_loss=1.6"]
        md = R.format_calibration_md(cal)
        assert "| **実戦（目標）** | 7/16 |" in md
        assert "（9路・enigma9plus（上書き enigma9plus_max_loss=1.6）・tau 1.0）" in md
        assert R.pool_file_content(cal)["strategy_overrides"] == ["enigma9plus_max_loss=1.6"]
        cal19 = R.calibration_result(out, {**plan, "size": 19})  # 目標の無い盤: プールを選ばず md も書ける
        assert cal19["best"] is None and cal19["choices"] == [] and cal19["harness_drift_ai"] is None
        assert "実戦（目標）" not in R.format_calibration_md(cal19)

```

(b) `tests/test_selfplay_cli.py` の `    def test_invalid_calibrate_ranks_are_refused(self, env):`（269 行）の直前に足す:

```python
    def test_calibrate_strategy_overrides(self, env):
        """spec §16.2 手順4: calibrate --strategy NAME:key=v,... で AI の設定を上書きする（エンジンは起こさない）。"""
        args = _calibrate_args(env)
        args[args.index("--strategy") + 1] = "enigma9plus:enigma9plus_max_loss=1.6,enigma9plus_gamble_until_move=20"
        stub, plan = CLI._plan_calibrate(CLI.build_parser().parse_args(args))
        arm = plan["arms"][0]
        assert arm["strategy"] == "enigma9plus" and arm["name"] == "calib"
        assert arm["override_items"] == ["enigma9plus_max_loss=1.6", "enigma9plus_gamble_until_move=20"]
        assert arm["settings"]["enigma9plus_max_loss"] == 1.6 and arm["settings"]["enigma9plus_gamble_until_move"] == 20

    def test_calibrate_refuses_a_typo_in_the_strategy_overrides(self, env, monkeypatch):
        started = []
        monkeypatch.setattr(CLI, "start_engine", lambda stub: started.append(1) or FakeEngine())
        args = _calibrate_args(env)
        args[args.index("--strategy") + 1] = "enigma9plus:enigma9plus_max_los=1.6"
        with pytest.raises(SystemExit, match="unknown setting keys"):
            CLI.main(args)
        assert started == []

```

- [ ] **Step 2: 失敗を確かめる**

Run: `python -m pytest tests/test_selfplay_run.py tests/test_selfplay_cli.py -q -p no:cacheprovider -k "calibration_targets_follow or calibrate_strategy_overrides or typo_in_the_strategy"`
Expected: 3 failed（run: 9路で `choices` が空＝`cal["targets"] is targets_9` が偽／cli: `KeyError: "Unknown strategy 'enigma9plus:...'"`・
typo は `SystemExit: error: "Unknown strategy ..."` で `unknown setting keys` に合わない）。

- [ ] **Step 3: 実装**（Edit ツール）

(a) `selfplay_run.py` の `calibration_result`（529-531 行）:

置き換え前:
```python
    per_rank = S.calibration_rank_stats(records)
    choices = S.selfplay_pool_choice(per_rank) if plan["size"] == 13 and len(per_rank) >= 3 else []
    best = choices[0] if choices else None
```
置き換え後:
```python
    per_rank = S.calibration_rank_stats(records)
    targets = S.calib_targets_for(plan["size"])  # 目標の無い盤（19路）はプールを選ばない
    choices = S.selfplay_pool_choice(per_rank, targets) if targets is not None and len(per_rank) >= 3 else []
    best = choices[0] if choices else None
```
同じ関数の return の中（538-548 行）:

置き換え前:
```python
        "strategy": plan["arms"][0]["strategy"],
        "size": plan["size"],
        "tau": plan["opponent"]["tau"],
        "resign_model": resign_model_of_plan(plan.get("resign") or {}),
        "targets": S.CALIB_TARGETS_13,
        "per_rank": per_rank,
        "choices": choices[:5],
        "best": best,
        "harness_drift_ai": (
            None if best is None or best["own_mean"] is None else best["own_mean"] - S.CALIB_TARGETS_13["ai_mean"]
        ),
```
置き換え後:
```python
        "strategy": plan["arms"][0]["strategy"],
        "strategy_overrides": list(plan["arms"][0].get("override_items") or []),  # calibrate --strategy NAME:k=v,...
        "size": plan["size"],
        "tau": plan["opponent"]["tau"],
        "resign_model": resign_model_of_plan(plan.get("resign") or {}),
        "targets": targets,
        "targets_games": S.CALIB_TARGET_GAMES.get(plan["size"]),
        "per_rank": per_rank,
        "choices": choices[:5],
        "best": best,
        "harness_drift_ai": (
            None if best is None or best["own_mean"] is None else best["own_mean"] - targets["ai_mean"]
        ),
```
（`        "strategy": plan["arms"][0]["strategy"],` は `calibration_result` の中に1つだけ。`pool_file_content` の同名のキーは
`"strategy": cal["strategy"],` で別の文字列。）

(b) `pool_file_content`（561-562 行）:

置き換え前:
```python
        "strategy": cal["strategy"],
        "ranks": best["ranks"],
```
置き換え後:
```python
        "strategy": cal["strategy"],
        "strategy_overrides": cal.get("strategy_overrides") or [],
        "ranks": best["ranks"],
```

(c) `format_calibration_md` の先頭（572-577 行）:

置き換え前:
```python
def format_calibration_md(cal):
    t = cal["targets"]
    lines = [
        f"# 自己対局ハーネスの相手ボット校正（{cal['size']}路・{cal['strategy']}・tau {cal['tau']}）",
        "",
    ]
```
置き換え後:
```python
def format_calibration_md(cal):
    t = cal["targets"]
    overrides = cal.get("strategy_overrides") or []
    strategy = cal["strategy"] + (f"（上書き {', '.join(overrides)}）" if overrides else "")
    lines = [
        f"# 自己対局ハーネスの相手ボット校正（{cal['size']}路・{strategy}・tau {cal['tau']}）",
        "",
    ]
```
「実戦（目標）」の行（599-609 行）:

置き換え前:
```python
    b = t["bins"]
    lines += [
        f"| **実戦（目標）** | 16/18 | {fmt_pct(t['opp_mean'])} | {100 * t['opp_sd']:.1f}pt | {t['opp_loss']:.2f} | "
        f"{fmt_pct(t['opp_ge2'])} | {fmt_pct(t['opp_ge5'])} | "
        + " | ".join(
            f"{fmt_pct(b[n]['opp_top1'])} / {b[n]['opp_loss']:.2f}"
            for n in ("cal_opening", "cal_middle", "cal_endgame")
        )
        + f" | {fmt_pct(t['ai_mean'])} | {t['moves_median']} |",
        "",
```
置き換え後:
```python
    if t is not None:  # 目標の無い盤（19路）は行を出さない
        b = t["bins"]
        lines.append(
            f"| **実戦（目標）** | {cal.get('targets_games') or '-'} | {fmt_pct(t['opp_mean'])} | {100 * t['opp_sd']:.1f}pt | "
            f"{t['opp_loss']:.2f} | {fmt_pct(t['opp_ge2'])} | {fmt_pct(t['opp_ge5'])} | "
            + " | ".join(
                f"{fmt_pct(b[n]['opp_top1'])} / {b[n]['opp_loss']:.2f}"
                for n in ("cal_opening", "cal_middle", "cal_endgame")
            )
            + f" | {fmt_pct(t['ai_mean'])} | {t['moves_median']} |"
        )
    lines += [
        "",
```
（この後ろの `"## 3段位プールの候補 ...",` からの行はそのまま。13路の md は「16/18」が `targets_games` から来るだけで1文字も変わらない。）
ずれの行（626 行）:

置き換え前:
```python
        f"選んだプールでの AI 一致率 - 実戦の難解＋ 局平均 {fmt_pct(t['ai_mean'])} = "
```
置き換え後:
```python
        f"選んだプールでの AI 一致率 - 実戦の難解＋ 局平均 {fmt_pct(t['ai_mean'] if t else None)} = "
```

(d) `selfplay.py` の `_plan_calibrate`（260-261 行）:

置き換え前:
```python
    stub = make_stub(args.config)
    arm = resolve_arm(stub, "calib", args.strategy, [])
```
置き換え後:
```python
    stub = make_stub(args.config)
    _, strategy, items = S.parse_arm("calib=" + args.strategy)  # NAME[:key=val,...]＝AI の設定の上書き
    arm = resolve_arm(stub, "calib", strategy, items)
```
`cmd_calibrate` の 316 行:

置き換え前:
```python
        out = _new_output(args, stub, plan, args.label or f"calib-{args.strategy}")
```
置き換え後:
```python
        out = _new_output(args, stub, plan, args.label or f"calib-{plan['arms'][0]['strategy']}")
```
（`:` と `=` を含む文字列をディレクトリ名にしない＝Windows では `:` が使えない。）
パーサの 450 行:

置き換え前:
```python
    cal.add_argument("--strategy", default="enigma13plus", help="校正に使う戦略（runner の戦略名）")
```
置き換え後:
```python
    cal.add_argument(
        "--strategy",
        default="enigma13plus",
        metavar="NAME[:key=val,...]",
        help="校正に使う戦略（runner の戦略名）。:key=val,... で AI の設定を上書きする（9路は実戦の多数派の設定）",
    )
```

- [ ] **Step 4: 通ることを確かめる**

Run: `python -m pytest tests/test_selfplay_*.py -q -p no:cacheprovider`
Expected: すべて PASS（13路の既存の校正のテスト `test_calibration_markdown_and_pool_file` などは値が変わらない）。

- [ ] **Step 5: コミット**

```bash
git add katrain_debug/selfplay_run.py katrain_debug/selfplay.py tests/test_selfplay_run.py tests/test_selfplay_cli.py
git commit -m "feat(selfplay): 校正の目標とプールの選択を盤サイズ別にし、calibrate で AI の設定を上書きできるようにする" -m "spec §16.2 手順4。--strategy NAME:key=v,...・calibration.md の目標の局数と上書き・目標の無い盤はプールを選ばない。" -m "Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>"
```

---

## Task 4: 定跡を知る相手（`--book-moves B`・`--book-loss X`）（TDD）

状態: 完了（`e900546a`）。

**Files:**
- Modify: `katrain_debug/selfplay_stats.py`（`CALIB_TARGET_GAMES` の直後に `BOOK_LOSS`・`# ---- 投了・勝敗 ----`〈245 行〉の直前に `book_exit_depth`）
- Modify: `katrain_debug/selfplay_opponent.py`（末尾〈161 行の後〉に `BookOpponent`）
- Modify: `katrain_debug/selfplay_game.py`（import 25 行・`make_opponent` 220-225 行）
- Modify: `katrain_debug/selfplay_run.py`（`plan_conditions` 463 行）
- Modify: `katrain_debug/selfplay.py`（`opponent_plan` 92-120 行・パーサ 444 行の後）
- Test: `tests/test_selfplay_stats.py`・`tests/test_selfplay_opponent.py`・`tests/test_selfplay_run.py`・`tests/test_selfplay_cli.py`

**Interfaces:**
- Consumes: 既存の `HumanSLOpponent(profile, tau, seed, max_loss)`（`label`・`stats`・`play(game, waiter)`）・`Waiter.nodes(nodes, what)`・
  `GameNode.points_lost`・`candidate_moves`・`Move.from_gtp(gtp, player)`（`"pass"` も可）。
- Produces:
  - `S.BOOK_LOSS = 0.3`・`S.book_exit_depth(ai_losses: list[tuple[int, float | None]], max_loss) -> int | None`
  - `BookOpponent(inner, book_moves, book_loss=S.BOOK_LOSS)`: `label` = `f"book{B}@{X:g}+{inner.label}"`・`stats` = inner の stats ＋
    `book_played`・`book_exit_depth`（X を超えた AI の手の手数、または B）・`book_exit_reason`（`"ai_loss"` / `"limit"` / None）・`play(game, waiter)`
  - 計画の `opponent` に `book_moves`・`book_loss`（OFF は両方 None）。`plan_conditions` の相手のキーに入る（違う実行は summarize で合わせない）。
  - CLI: `run --book-moves B`（既定 0 = OFF）・`--book-loss X`（既定 0.3）。`--opponent strategy:...` と一緒には使えない。

- [ ] **Step 1: 失敗するテストを書く**

(a) `tests/test_selfplay_stats.py` の `    def test_ai_view(self):`（233 行）の直前に足す:

```python
    def test_book_exit_depth(self):
        """spec §16.2 手順6: AI の手の points_lost が X を超えた（または無い）最初の手の手数。"""
        assert S.book_exit_depth([(1, 0.1), (3, 0.3), (5, -0.2)], 0.3) is None
        assert S.book_exit_depth([(1, 0.1), (3, 0.31), (5, 2.0)], 0.3) == 3
        assert S.book_exit_depth([(2, None)], 0.3) == 2
        assert S.book_exit_depth([], 0.3) is None and S.BOOK_LOSS == 0.3

```

(b) `tests/test_selfplay_opponent.py`: 13 行の import を
`from katrain_debug.selfplay_opponent import BookOpponent, GameAborted, HumanSLOpponent, Waiter` に変え、ファイルの末尾（157 行の後）に足す:

```python


def _book_position(nodes, depth, next_player, best="E5"):
    """BookOpponent の単体テスト用の局面（stub のノード）。nodes: [(手数, 打った色, points_lost)]（root は足す）。"""
    root = types.SimpleNamespace(depth=0, player="W", points_lost=None)
    path = [root] + [types.SimpleNamespace(depth=d, player=p, points_lost=pl) for d, p, pl in nodes]
    cn = types.SimpleNamespace(
        depth=depth, next_player=next_player, nodes_from_root=path, candidate_moves=[{"move": best}]
    )
    played = []

    def play(move):
        played.append((move.player, move.gtp()))
        return "book-node"

    return types.SimpleNamespace(current_node=cn, play=play), played


class _Inner:
    label = "humanSL:rank_3d"

    def __init__(self):
        self.stats = {"moves": 0, "fallbacks": 0, "humansl_errors": 0}
        self.calls = 0

    def play(self, game, waiter):
        self.calls += 1
        return "inner-node"


_NO_WAIT = types.SimpleNamespace(nodes=lambda nodes, what: None)


class TestBookOpponent:
    """定跡を知る相手（spec 2026-09-23-veil-strategy-design.md §16.2 手順6）。"""

    def test_plays_the_best_move_while_the_ai_stays_in_the_book(self):
        inner = _Inner()
        opp = BookOpponent(inner, book_moves=12, book_loss=0.3)
        # AI は黒（相手＝白の手番）。相手自身の損失（2.0）は見ない
        game, played = _book_position([(1, "B", 0.1), (2, "W", 2.0), (3, "B", 0.3)], depth=3, next_player="W")
        assert opp.play(game, _NO_WAIT) == "book-node" and played == [("W", "E5")] and inner.calls == 0
        assert opp.stats == {
            "moves": 0,
            "fallbacks": 0,
            "humansl_errors": 0,
            "book_played": 1,
            "book_exit_depth": None,
            "book_exit_reason": None,
        }
        assert opp.label == "book12@0.3+humanSL:rank_3d"

    def test_one_ai_move_over_the_limit_leaves_the_book_for_the_rest_of_the_game(self):
        inner = _Inner()
        opp = BookOpponent(inner, book_moves=12, book_loss=0.3)
        game, played = _book_position([(1, "B", 0.31)], depth=1, next_player="W")
        assert opp.play(game, _NO_WAIT) == "inner-node" and played == []
        game, played = _book_position([(1, "B", 0.31), (2, "W", 0.0), (3, "B", 0.0)], depth=3, next_player="W")
        assert opp.play(game, _NO_WAIT) == "inner-node" and played == [] and inner.calls == 2
        assert (opp.stats["book_exit_depth"], opp.stats["book_exit_reason"]) == (1, "ai_loss")

    def test_a_move_without_points_lost_leaves_the_book(self):
        opp = BookOpponent(_Inner(), book_moves=12)
        game, _ = _book_position([(1, "W", None)], depth=1, next_player="B")  # AI は白
        assert opp.play(game, _NO_WAIT) == "inner-node" and opp.stats["book_exit_reason"] == "ai_loss"

    def test_stops_at_the_book_length(self):
        opp = BookOpponent(_Inner(), book_moves=4)
        game, played = _book_position([(1, "B", 0.0), (2, "W", 0.0), (3, "B", 0.0)], depth=3, next_player="W")
        assert opp.play(game, _NO_WAIT) == "book-node"  # 手数 3 < 4
        game, played = _book_position([(d, "BW"[(d - 1) % 2], 0.0) for d in range(1, 6)], depth=5, next_player="W")
        assert opp.play(game, _NO_WAIT) == "inner-node" and played == []
        assert (opp.stats["book_played"], opp.stats["book_exit_depth"], opp.stats["book_exit_reason"]) == (
            1,
            4,
            "limit",
        )

    def test_wraps_the_humansl_opponent_on_a_real_game(self, tmp_path):
        engine = FakeEngine(hp_fn=_hp({(8, 8): 1.0}))  # humanSL は右上（J9）・KataGo の最善手は左下（A1）から
        game = new_game(make_stub(tmp_path), engine)
        opp = BookOpponent(HumanSLOpponent("rank_3k", seed=1), book_moves=1)
        waiter = Waiter(engine, timeout=5)
        first = opp.play(game, waiter)  # 手数 0 < 1: KataGo の最善手
        assert first.move.gtp() == "A1" and first.move.player == "B"
        game.play(Move.from_gtp("B1", player="W"))
        third = opp.play(game, waiter)  # 手数 2 >= 1: 包んだ humanSL
        assert third.move.coords == (8, 8)
        assert opp.stats["book_played"] == 1 and opp.stats["moves"] == 1 and opp.stats["book_exit_reason"] == "limit"
```

(c) `tests/test_selfplay_run.py` の `    def test_calibration_markdown_and_pool_file(self, tmp_path):`（441 行）の直前に足す:

```python
    def test_book_opponent_is_a_game_condition(self):
        base = {"size": 9, "komi": 7.0, "resign": {"model": "length"}}
        plain = {**base, "opponent": {"kind": "humansl", "book_moves": None, "book_loss": None}}
        old = {**base, "opponent": {"kind": "humansl"}}  # 定跡を知る相手より前の run.json
        book = {**base, "opponent": {"kind": "humansl", "book_moves": 24, "book_loss": 0.3}}
        assert R.plan_conditions(plain) == R.plan_conditions(old)
        assert R.plan_conditions(plain) != R.plan_conditions(book)

```

(d) `tests/test_selfplay_cli.py` の `class TestResignModel:`（157 行）の直前に足す:

```python
class TestBookOpponent:
    def test_book_opponent_wraps_the_pool_opponent(self, env):
        CLI.main(_run_args(env, "--arm", "A=default", "--book-moves", "4"))
        plan, recs = _run_files(_only_dir(env["out"]))
        assert plan["opponent"]["book_moves"] == 4 and plan["opponent"]["book_loss"] == 0.3
        for r in recs:  # 偽エンジンは損失 0＝定跡のまま手数 4 まで（相手の手番は2回）、その後は humanSL
            assert r["opponent"] == "book4@0.3+humanSL:rank_3k"
            stats = r["opponent_stats"]
            assert (stats["book_played"], stats["book_exit_depth"], stats["book_exit_reason"]) == (2, 4, "limit")

    def test_book_is_off_by_default(self, env):
        CLI.main(_run_args(env, "--arm", "A=default"))
        plan, recs = _run_files(_only_dir(env["out"]))
        assert plan["opponent"]["book_moves"] is None and plan["opponent"]["book_loss"] is None
        assert all(r["opponent"] == "humanSL:rank_3k" and "book_played" not in r["opponent_stats"] for r in recs)

    def test_book_needs_the_humansl_opponent(self, env):
        with pytest.raises(SystemExit, match="--book-moves wraps the humansl opponent only"):
            CLI.main(_run_args(env, "--arm", "A=default", "--opponent", "strategy:default", "--book-moves", "4"))


```

- [ ] **Step 2: 失敗を確かめる**

Run: `python -m pytest tests/test_selfplay_stats.py tests/test_selfplay_opponent.py tests/test_selfplay_run.py tests/test_selfplay_cli.py -q -p no:cacheprovider --continue-on-collection-errors`
（`--continue-on-collection-errors` が無いと、`test_selfplay_opponent.py` の収集の ERROR で全体が `Interrupted: 1 error during collection` で止まり、
ほかの失敗が出ない。）
Expected: `5 failed, ... 1 error`。ERROR は `test_selfplay_opponent.py` の収集（`ImportError: cannot import name 'BookOpponent'`）、failed は stats の
`test_book_exit_depth`（`AttributeError: ... 'book_exit_depth'`）・run の `test_book_opponent_is_a_game_condition`（AssertionError＝`!=` が偽）・CLI の
`TestBookOpponent` の3件（`--book-moves` を渡す2件は argparse の `SystemExit: 2`〈`unrecognized arguments: --book-moves 4`〉＝
`test_book_needs_the_humansl_opponent` は `Regex pattern did not match`・`test_book_is_off_by_default` は `KeyError: 'book_moves'`）。

- [ ] **Step 3: 実装**（Edit ツール）

(a) `selfplay_stats.py`: Task 1 で足した `CALIB_TARGET_GAMES = {13: "16/18"}` の行の直後に足す:

```python
BOOK_LOSS = 0.3  # 定跡を知る相手の既定の X（AI の1手の損失の上限＝引き分けを保つ帯。spec §16.2 手順6）
```

`# ---- 投了・勝敗 ----`（245 行）の直前に足す:

```python
# ---- 定跡を知る相手（spec §16.2 手順6）----
def book_exit_depth(ai_losses, max_loss):
    """AI の手 [(手数, points_lost)]（時系列）のうち、points_lost が max_loss を超えた（または無い）最初の手の手数。

    すべて max_loss 以下なら None（まだ定跡の中）。
    """
    for depth, loss in ai_losses:
        if loss is None or loss > max_loss:
            return depth
    return None


```

(b) `selfplay_opponent.py` の末尾（`HumanSLOpponent.play` の最後の行 `        return node` の後）に、空行2つを挟んで足す:

```python
class BookOpponent:
    """定跡を知る相手（spec 2026-09-23-veil-strategy-design.md §16.2 手順6・`--book-moves B --book-loss X`）。

    相手の手番の局面の手数が B 未満で、それまでの AI の手がすべて points_lost <= X の間は、KataGo の最善手（通常解析の
    candidate_moves[0]）を打つ。AI が一度でも X を超える手（points_lost の無い手も）を打つか、手数が B に届いたら、その局の
    残りは包んだ相手（プールの humanSL）で打つ。ユーザーの仮説（こちらが定跡どおりだと高段の BOT が最善手を続ける）の
    モデル: 「定跡」は本より浅い読み（通常解析）の最善手で、「定跡の中」を AI の損失で決めるのはモデルの仮定。
    stats は包んだ相手の stats に book_played（最善手を打った回数）・book_exit_depth（定跡を外れた手数: X を超えた AI の手の
    手数、または B）・book_exit_reason（ai_loss / limit。B に届く前に終局したら None）を足したもの。
    """

    def __init__(self, inner, book_moves, book_loss=S.BOOK_LOSS):
        self.inner = inner
        self.book_moves = book_moves
        self.book_loss = book_loss
        self.book_played = 0
        self.exit_depth = None
        self.exit_reason = None

    @property
    def label(self):
        return f"book{self.book_moves}@{self.book_loss:g}+{self.inner.label}"

    @property
    def stats(self):
        return {
            **self.inner.stats,
            "book_played": self.book_played,
            "book_exit_depth": self.exit_depth,
            "book_exit_reason": self.exit_reason,
        }

    def play(self, game, waiter):
        cn = game.current_node
        if self.exit_reason is None and cn.depth >= self.book_moves:
            self.exit_depth, self.exit_reason = self.book_moves, "limit"
        if self.exit_reason is None:
            waiter.nodes(cn.nodes_from_root, "path analysis before the book opponent")
            ai = "W" if cn.next_player == "B" else "B"
            losses = [(n.depth, n.points_lost) for n in cn.nodes_from_root[1:] if n.player == ai]
            exit_depth = S.book_exit_depth(losses, self.book_loss)
            if exit_depth is not None:
                self.exit_depth, self.exit_reason = exit_depth, "ai_loss"
        if self.exit_reason is not None:
            return self.inner.play(game, waiter)
        cands = cn.candidate_moves
        best = cands[0]["move"] if cands else "pass"
        node = game.play(Move.from_gtp(best, player=cn.next_player))
        self.book_played += 1
        return node
```

(c) `selfplay_game.py` の 25 行を
`from katrain_debug.selfplay_opponent import BookOpponent, GameAborted, HumanSLOpponent, Waiter  # noqa: E402` にし、
`make_opponent`（220-225 行）を置き換える:

置き換え前:
```python
def make_opponent(opponent_plan, spec, opponent_arm=None):
    if opponent_plan["kind"] == "strategy":
        return StrategyOpponent(opponent_arm)
    return HumanSLOpponent(
        spec["rank"], tau=opponent_plan.get("tau", 1.0), seed=spec["opp_seed"], max_loss=opponent_plan.get("max_loss")
    )
```
置き換え後:
```python
def make_opponent(opponent_plan, spec, opponent_arm=None):
    if opponent_plan["kind"] == "strategy":
        return StrategyOpponent(opponent_arm)
    opponent = HumanSLOpponent(
        spec["rank"], tau=opponent_plan.get("tau", 1.0), seed=spec["opp_seed"], max_loss=opponent_plan.get("max_loss")
    )
    if opponent_plan.get("book_moves"):  # 定跡を知る相手（spec §16.2 手順6）: プールの humanSL を包む
        loss = opponent_plan.get("book_loss")
        return BookOpponent(opponent, opponent_plan["book_moves"], S.BOOK_LOSS if loss is None else loss)
    return opponent
```

(d) `selfplay_run.py` の `plan_conditions` の 463 行:

置き換え前:
```python
        "opponent": {k: opp.get(k) for k in ("kind", "ranks", "tau", "max_loss", "strategy", "override_items")},
```
置き換え後:
```python
        "opponent": {
            k: opp.get(k)
            for k in ("kind", "ranks", "tau", "max_loss", "strategy", "override_items", "book_moves", "book_loss")
        },
```

(e) `selfplay.py`: `opponent_plan`（92-95 行）の頭を置き換える:

置き換え前:
```python
def opponent_plan(args, size):
    """相手の設定。humansl は --ranks か --opp-pool（無ければ calibration-data/selfplay/opponent_pool_<size>.json）。"""
    if args.opponent != "humansl":
        if not args.opponent.startswith("strategy:"):
```
置き換え後:
```python
def book_plan(args):
    """定跡を知る相手（--book-moves B・--book-loss X）。B 0（既定）は OFF＝どちらも None（run.json と突き合わせのキー）。"""
    moves = getattr(args, "book_moves", 0) or 0
    if moves < 0:
        raise SystemExit("--book-moves must be >= 0")
    return {"book_moves": moves or None, "book_loss": args.book_loss if moves else None}


def opponent_plan(args, size):
    """相手の設定。humansl は --ranks か --opp-pool（無ければ calibration-data/selfplay/opponent_pool_<size>.json）。

    --book-moves B（> 0）なら humansl の相手を定跡を知る相手で包む（spec §16.2 手順6。strategy の相手には使えない）。
    """
    book = book_plan(args)
    if args.opponent != "humansl":
        if book["book_moves"]:
            raise SystemExit("--book-moves wraps the humansl opponent only (not --opponent strategy:...)")
        if not args.opponent.startswith("strategy:"):
```
同じ関数の return（118-120 行）:

置き換え前:
```python
        "pool_file": R.repo_relpath(pool_file),
        "pool": pool,
    }
```
置き換え後:
```python
        "pool_file": R.repo_relpath(pool_file),
        "pool": pool,
        **book,
    }
```
パーサの 444 行 `    run.add_argument("--komi-shift", type=float, default=0.0, help="AI 不利にずらすコミ（接戦ストレス層）")` の直後に足す:

```python
    run.add_argument(
        "--book-moves",
        type=int,
        default=0,
        metavar="B",
        help="定跡を知る相手: 手数 < B の間、AI の手がすべて --book-loss 以下なら KataGo の最善手を打つ（0 = OFF）",
    )
    run.add_argument(
        "--book-loss",
        type=float,
        default=S.BOOK_LOSS,
        metavar="X",
        help="定跡の中とみなす AI の1手の損失の上限（目・既定 0.3）",
    )
```

- [ ] **Step 4: 通ることを確かめる**

Run: `python -m pytest tests/test_selfplay_*.py -q -p no:cacheprovider`
Expected: すべて PASS。

- [ ] **Step 5: コミット**

```bash
git add katrain_debug tests/test_selfplay_stats.py tests/test_selfplay_opponent.py tests/test_selfplay_run.py tests/test_selfplay_cli.py
git commit -m "feat(selfplay): 定跡を知る相手（--book-moves / --book-loss）を足す" -m "spec §16.2 手順6。手数 < B で AI の手がすべて X 目以下の間は KataGo の最善手、外れたらプールの humanSL。run.json と突き合わせのキーに B・X。" -m "Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>"
```

---

## Task 5a: 研究外しの任せる先の指紋（再開と合算の突き合わせ）（TDD）

状態: 完了（`85994eb6`）。

**Files:**
- Modify: `katrain_debug/selfplay_game.py`（import 18 行・`Arm` 119-141 行・`resolve_arm` 144-172 行）
- Modify: `katrain_debug/selfplay_run.py`（`COMPARE_METRICS` 33 行の後・`execute_plan` 327-331 行・`load_records` 475-481 行）
- Modify: `katrain_debug/selfplay.py`（`_plan_warnings` 174-176 行）
- Test: `tests/test_selfplay_runner.py`・`tests/test_selfplay_run.py`・`tests/test_selfplay_cli.py`

**Interfaces:**
- Consumes: Plan A の `VEIL_OPEN_DELEGATES`・韜晦のクラスの `KEY_PREFIX` / `BOARD_LEN` / `SETTING_DEFAULTS["open_moves"]`。既存の `S.settings_fingerprint(mode, settings)`・`S.effective_settings`。
- Produces:
  - `open_delegate(stub, cls, effective) -> dict | None`（`{"strategy_key", "settings", "settings_source", "fingerprint"}`）
  - `Arm.delegate: dict | None`（`as_dict()` と run.json の各アームに入る）。**アームの `fingerprint` は今のまま**（settings の指紋＝古い run.json と互換）。
  - `R.delegate_fingerprint(arm_dict_or_Arm) -> str | None`（Task 5b はこの関数の直前に書く）
  - 再開（`execute_plan`）: 任せる先の指紋が run.json と違えば `SystemExit("arm <name>: the delegate of the opening window differs from run.json ...")`（1局も打たない）
  - 合算（`load_records`）: 同じ名前のアームの任せる先の指紋が違えば `SystemExit("arm <name>: the delegate of the opening window differs between the run directories ...")`
  - CLI: 計画時に `note: arm <name> (<strategy>): opening window delegated to <key> (<settings_source>, fingerprint <指紋>)` を出す。

- [ ] **Step 1: 失敗するテストを書く**

(a) `tests/test_selfplay_runner.py`: 17 行の import を
`from katrain.core.ai import VEIL_OPEN_DELEGATES, AIStrategy, AnalysisDiscardedException, generate_ai_move` にし、
`class TestNullGuardOnEffectiveSettings:`（117 行）の直前に足す:

```python
class TestOpenDelegate:
    """spec 2026-09-23-veil-strategy-design.md §16.2 手順9: open_moves > 0 の韜晦のアームは任せる先を持つ。"""

    def test_open_window_arm_carries_its_delegate(self, tmp_path):
        stub = make_stub(tmp_path, **{"ai:enigma9plus": {"enigma9plus_max_loss": 1.8}})
        arm = G.resolve_arm(stub, "open", "veil9", ["veil9_open_moves=12"])
        assert arm.delegate == {
            "strategy_key": "ai:enigma9plus",
            "settings": {"enigma9plus_max_loss": 1.8},
            "settings_source": "user config",
            "fingerprint": S.settings_fingerprint("ai:enigma9plus", {"enigma9plus_max_loss": 1.8}),
        }
        assert arm.as_dict()["delegate"] == arm.delegate

    @pytest.mark.parametrize(
        "strategy, items", [("veil9", []), ("veil9", ["veil9_open_moves=0"]), ("enigma9plus", []), ("default", [])]
    )
    def test_no_delegate_without_an_open_window(self, tmp_path, strategy, items):
        assert G.resolve_arm(make_stub(tmp_path), "A", strategy, items).delegate is None

    @pytest.mark.parametrize("strategy, size", [("veil9", 9), ("veil13", 13), ("veil19", 19)])
    def test_delegate_follows_the_board_size(self, tmp_path, strategy, size):
        arm = G.resolve_arm(make_stub(tmp_path), "A", strategy, [f"{strategy}_open_moves=30"])
        assert arm.delegate["strategy_key"] == VEIL_OPEN_DELEGATES[size]
        assert arm.delegate["settings"] == {} and arm.delegate["settings_source"].startswith("code defaults")

    def test_arm_overrides_do_not_touch_the_delegate(self, tmp_path):
        stub = make_stub(tmp_path, **{"ai:enigma9plus": {"enigma9plus_max_loss": 1.8}})
        a = G.resolve_arm(stub, "A", "veil9", ["veil9_open_moves=12"])
        b = G.resolve_arm(stub, "B", "veil9", ["veil9_open_moves=12", "veil9_reserve=4.0"])
        assert a.delegate == b.delegate and a.fingerprint != b.fingerprint


```

(b) `tests/test_selfplay_run.py`:
- `    def test_resume_drops_torn_last_lines_with_a_warning(self, tmp_path):`（138 行）の直前に足す:

```python
    def test_a_changed_delegate_section_stops_the_resume(self, tmp_path):
        """spec §16.2 手順9: 序盤の研究外しの任せる先（難解＋の節）が run.json と違えば1局も打たずに止まる。"""
        stub = make_stub(tmp_path, **{"ai:enigma9plus": {"enigma9plus_max_loss": 1.8}})
        arm = resolve_arm(stub, "open", "veil9", ["veil9_open_moves=12"])
        schedule = S.selfplay_schedule(2, ["rank_3k"], 1000)
        plan = R.make_plan("run", [arm], schedule, size=9, komi=7.0, max_moves=6, timeout=5)
        edited = make_stub(tmp_path, **{"ai:enigma9plus": {"enigma9plus_max_loss": 1.6}})  # 任せる先の節だけ変えた
        engine = FakeEngine()
        with pytest.raises(SystemExit, match="delegate of the opening window differs from run.json"):
            _run(plan, R.OutputDir(tmp_path / "run"), edited, engine)
        assert engine.requests == [] and engine.new_games == 0

```
- `    def test_summarize_refuses_runs_with_different_baselines(self, tmp_path):`（295 行）の直前に足す:

```python
    def test_summarize_refuses_runs_whose_delegates_differ(self, tmp_path):
        stub = make_stub(tmp_path, **{"ai:enigma9plus": {"enigma9plus_max_loss": 1.8}})
        arm = resolve_arm(stub, "open", "veil9", ["veil9_open_moves=12"])
        outs = []
        for label, seed_base in (("a", 1000), ("b", 1002)):
            plan = R.make_plan("run", [arm], S.selfplay_schedule(2, ["rank_3k"], seed_base), size=9, komi=7.0)
            plan = json.loads(json.dumps(plan, default=str))
            if label == "b":
                plan["arms"][0]["delegate"]["fingerprint"] = "0" * 16  # 任せる先の節を変えて打った延長
            out = R.OutputDir(tmp_path / label)
            out.write_json("run.json", plan)
            outs.append(out)
        with pytest.raises(SystemExit, match="delegate of the opening window differs between the run directories"):
            R.load_records(outs)
        assert R.load_records(outs[:1]) == []

```

(c) `tests/test_selfplay_cli.py` の `class TestReportSgf:`（327 行）の直前に足す:

```python
class TestOpenWindowNote:
    """spec §16.2 手順9: 任せる先は難解＋の節のまま（アームの上書きは効かない）なので、計画時に note で知らせる。"""

    def test_open_window_delegate_is_announced_at_plan_time(self, env, capsys):
        args = CLI.build_parser().parse_args(_run_args(env, "--arm", "A=veil9:veil9_open_moves=12"))
        _, plan = CLI._plan_run(args)  # エンジンは起こさない
        CLI._plan_warnings(plan)
        out = capsys.readouterr().out
        assert "note: arm A (veil9): opening window delegated to ai:enigma9plus (code defaults" in out
        assert plan["arms"][0]["delegate"]["strategy_key"] == "ai:enigma9plus"


```

- [ ] **Step 2: 失敗を確かめる**

Run: `python -m pytest tests/test_selfplay_runner.py tests/test_selfplay_run.py tests/test_selfplay_cli.py -q -p no:cacheprovider`
Expected: 12 failed。runner の `TestOpenDelegate` の 9 件が `AttributeError: 'Arm' object has no attribute 'delegate'`、run の
`test_a_changed_delegate_section_stops_the_resume` が `Failed: DID NOT RAISE <class 'SystemExit'>`（止まらずに偽エンジンで 2 局打ってから
落ちる＝数十秒かかることがある）・`test_summarize_refuses_runs_whose_delegates_differ` が `KeyError: 'delegate'`、CLI の
`test_open_window_delegate_is_announced_at_plan_time` が AssertionError（note の行が無い）。

- [ ] **Step 3: 実装**（Edit ツール）

(a) `selfplay_game.py` の 18 行を置き換える:

置き換え前:
```python
from katrain.core.ai import STRATEGY_REGISTRY, AnalysisDiscardedException, game_report  # noqa: E402
```
置き換え後:
```python
from katrain.core.ai import (  # noqa: E402
    STRATEGY_REGISTRY,
    VEIL_OPEN_DELEGATES,
    AnalysisDiscardedException,
    game_report,
)
```
`Arm` の docstring の最後（123-125 行）と属性（133-134 行）:

置き換え前:
```python
    effective_settings は戦略が実際に使う設定（コードの既定値に settings を重ねて数値を正規化・null ガード用）。
    fingerprint は settings の指紋のまま（run.json の再開・複数の実行の突き合わせと互換）。
    """
```
置き換え後:
```python
    effective_settings は戦略が実際に使う設定（コードの既定値に settings を重ねて数値を正規化・null ガード用）。
    fingerprint は settings の指紋のまま（run.json の再開・複数の実行の突き合わせと互換）。
    delegate は序盤の研究外し（`<KEY_PREFIX>_open_moves` > 0）の韜晦のアームだけ: 窓の中を任せる難解＋の戦略キー・設定・
    指紋（open_delegate）。再開と合算の突き合わせに入る（spec 2026-09-23-veil-strategy-design.md §16.2 手順9）。
    """
```
置き換え前:
```python
    settings_source: str
    effective_settings: dict = None
```
置き換え後:
```python
    settings_source: str
    effective_settings: dict = None
    delegate: dict = None
```
`def resolve_arm(stub, name, strategy, override_items):`（144 行）の直前に足す:

```python
def open_delegate(stub, cls, effective):
    """序盤の研究外しの任せる先（spec §15.3 手順5・§16.2 手順9）。`<KEY_PREFIX>_open_moves` > 0 の韜晦だけ dict、ほかは None。

    韜晦は窓の中を同じ盤サイズの難解＋（VEIL_OPEN_DELEGATES）に、ユーザー config の節（`ai/<戦略キー>`）のまま任せる
    ＝アームの上書きでは変わらない。指紋はその節の中身（節が無ければ {}＝難解＋のコードの既定値）。
    """
    prefix = getattr(cls, "KEY_PREFIX", None)
    size = getattr(cls, "BOARD_LEN", None)
    open_moves = (effective or {}).get(f"{prefix}_open_moves") if prefix else None
    if size not in VEIL_OPEN_DELEGATES or not open_moves or open_moves <= 0:
        return None
    key = VEIL_OPEN_DELEGATES[size]
    section = stub.config(f"ai/{key}")
    settings = dict(section or {})
    return {
        "strategy_key": key,
        "settings": settings,
        "settings_source": (
            "user config" if section is not None else "code defaults (mode missing from the user config)"
        ),
        "fingerprint": S.settings_fingerprint(key, settings),
    }


```
`resolve_arm` の最後（162-172 行）:

置き換え前:
```python
    settings = {**(user or {}), **overrides}
    return Arm(
        name=name,
        strategy=strategy,
        mode=mode,
        settings=settings,
        override_items=list(override_items),
        strategy_class=cls.__name__ if cls is not None else None,
        settings_source="user config" if user is not None else "code defaults (mode missing from the user config)",
        effective_settings=S.effective_settings(settings, prefix, defaults),
    )
```
置き換え後:
```python
    settings = {**(user or {}), **overrides}
    effective = S.effective_settings(settings, prefix, defaults)
    return Arm(
        name=name,
        strategy=strategy,
        mode=mode,
        settings=settings,
        override_items=list(override_items),
        strategy_class=cls.__name__ if cls is not None else None,
        settings_source="user config" if user is not None else "code defaults (mode missing from the user config)",
        effective_settings=effective,
        delegate=open_delegate(stub, cls, effective),
    )
```
（Enigma*・Mimic・HumanStyle などは `<KEY_PREFIX>_open_moves` を持たない＝`effective` にキーが無く None。韜晦で `open_moves` 0 も None。）

(b) `selfplay_run.py` の 33 行 `COMPARE_METRICS = (...)` の直後に（空行2つを挟んで）足す:

```python
def delegate_fingerprint(arm):
    """アーム（run.json の dict か Arm）の序盤の研究外しの任せる先の指紋（任せない・古い run.json は None）。"""
    delegate = arm.get("delegate") if isinstance(arm, dict) else arm.delegate
    return (delegate or {}).get("fingerprint")
```
`execute_plan` の 327-331 行:

置き換え前:
```python
        if arm.fingerprint != entry["fingerprint"]:
            raise SystemExit(
                f"arm {entry['name']}: resolved settings differ from run.json (config edited?). Start a new run."
            )
        arms[arm.name] = arm
```
置き換え後:
```python
        if arm.fingerprint != entry["fingerprint"]:
            raise SystemExit(
                f"arm {entry['name']}: resolved settings differ from run.json (config edited?). Start a new run."
            )
        if delegate_fingerprint(arm) != delegate_fingerprint(entry):  # 序盤の研究外しの任せる先（難解＋の節）
            raise SystemExit(
                f"arm {entry['name']}: the delegate of the opening window differs from run.json (config edited?). "
                "Start a new run."
            )
        arms[arm.name] = arm
```
`load_records` の 475-481 行:

置き換え前:
```python
    records, seen, prints, conditions, first = [], {}, {}, None, None
    for out in outs:
        if os.path.exists(out.file("run.json")):
            plan = out.read_json("run.json")
            for arm in plan["arms"]:
                if prints.setdefault(arm["name"], arm["fingerprint"]) != arm["fingerprint"]:
                    raise SystemExit(f"arm {arm['name']}: settings differ between the run directories ({out.path})")
```
置き換え後:
```python
    records, seen, prints, delegates, conditions, first = [], {}, {}, {}, None, None
    for out in outs:
        if os.path.exists(out.file("run.json")):
            plan = out.read_json("run.json")
            for arm in plan["arms"]:
                if prints.setdefault(arm["name"], arm["fingerprint"]) != arm["fingerprint"]:
                    raise SystemExit(f"arm {arm['name']}: settings differ between the run directories ({out.path})")
                if delegates.setdefault(arm["name"], delegate_fingerprint(arm)) != delegate_fingerprint(arm):
                    raise SystemExit(
                        f"arm {arm['name']}: the delegate of the opening window differs between the run directories "
                        f"({out.path})"
                    )
```

(c) `selfplay.py`: `_plan_warnings` の for 文（174-176 行）:

置き換え前:
```python
    for arm in plan["arms"]:  # spec §11: 戦略の節がユーザー config に無いと、GUI と違うコードの既定値で走る
        if arm["settings_source"] != "user config":
            safe_print(f"note: arm {arm['name']} ({arm['strategy']}): settings_source = {arm['settings_source']}")
```
置き換え後:
```python
    for arm in plan["arms"]:  # spec §11: 戦略の節がユーザー config に無いと、GUI と違うコードの既定値で走る
        if arm["settings_source"] != "user config":
            safe_print(f"note: arm {arm['name']} ({arm['strategy']}): settings_source = {arm['settings_source']}")
        delegate = arm.get("delegate")
        if delegate:  # 序盤の研究外しの窓は難解＋にユーザー config の節のまま任せる（アームの上書きは効かない）
            safe_print(
                f"note: arm {arm['name']} ({arm['strategy']}): opening window delegated to {delegate['strategy_key']} "
                f"({delegate['settings_source']}, fingerprint {delegate['fingerprint']})"
            )
```

- [ ] **Step 4: 通ることを確かめる**

Run: `python -m pytest tests/test_selfplay_*.py -q -p no:cacheprovider`
Expected: すべて PASS。

- [ ] **Step 5: コミット**

```bash
git add katrain_debug tests/test_selfplay_runner.py tests/test_selfplay_run.py tests/test_selfplay_cli.py
git commit -m "feat(selfplay): 研究外しの任せる先の指紋を run.json に持たせ、再開と合算で突き合わせる" -m "spec §16.2 手順9。open_moves > 0 の韜晦のアームは難解＋の節（戦略キー・設定・指紋）を delegate に持つ。計画時に note を出す。" -m "Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>"
```

---

## Task 5b: 設定の固定（実行ごとの config の写し・`--resume` はその写しを読む）（TDD）

状態: 完了（`7b780d46`）。

**Files:**
- Modify: `katrain_debug/selfplay_run.py`（import 8-13 行・`COMPARE_METRICS` 33 行の後〈Task 5a の `delegate_fingerprint` の前〉・`OutputDir.read_json`〈Plan B の前は 191-193 行。Task 5a と (a) の挿入で下にずれる＝文字列で当てる〉の後）
- Modify: `katrain_debug/selfplay.py`（`_new_output` と `_resume`＝Task 5a の後で 185-196 行〈Task 5a の前は 179-190 行〉）
- Test: `tests/test_selfplay_cli.py`

**Interfaces:**
- Produces:
  - `R.CONFIG_SNAPSHOT = "config-snapshot.json"`・`R.file_sha256(path) -> str`
  - `OutputDir.snapshot_config(config_path) -> {"config_snapshot", "config_sha256"}`・`OutputDir.pinned_config(plan) -> str`
  - CLI: run / calibrate は出力ディレクトリに `config-snapshot.json` を置き run.json に `config_snapshot`・`config_sha256`。`--resume` は写しを読む
    （中身が sha256 と違えば止まる・写しの無い古い実行は `config_path`）。

- [ ] **Step 1: 失敗するテストを書く**（`tests/test_selfplay_cli.py` の `class TestReportSgf:` の直前〈Task 5a が足した `TestOpenWindowNote` の後〉に足す）

```python
class TestConfigSnapshot:
    """spec §16.2 手順9: run は使った config の写しを置き、--resume はその写しを読む。"""

    def test_resume_reads_the_pinned_copy_not_the_live_config(self, env):
        CLI.main(_run_args(env, "--arm", "A=default"))
        run_dir = _only_dir(env["out"])
        plan = json.load(open(os.path.join(run_dir, "run.json"), encoding="utf-8"))
        snap = os.path.join(run_dir, "config-snapshot.json")
        assert plan["config_snapshot"] == "config-snapshot.json" and plan["config_sha256"] == CLI.R.file_sha256(snap)
        assert open(snap, "rb").read() == open(env["config"], "rb").read()
        games = os.path.join(run_dir, "games.jsonl")
        first = open(games, encoding="utf-8").read().splitlines()[0]
        open(games, "w", encoding="utf-8").write(first + "\n")  # 1局だけ終えて止まった実行
        cfg = json.loads(open(env["config"], encoding="utf-8").read())
        cfg["ai"]["ai:default"] = {"edited": 1}  # 止めている間にユーザー config を変えた
        open(env["config"], "w", encoding="utf-8").write(json.dumps(cfg))
        CLI.main(["run", "--resume", run_dir, "--boot", "100"])  # 写しを読むので設定は変わらない＝止まらない
        assert len(open(games, encoding="utf-8").read().splitlines()) == 2

    def test_an_edited_snapshot_stops_the_resume(self, env):
        CLI.main(_run_args(env, "--arm", "A=default"))
        run_dir = _only_dir(env["out"])
        with open(os.path.join(run_dir, "config-snapshot.json"), "a", encoding="utf-8") as f:
            f.write(" ")
        with pytest.raises(SystemExit, match="config snapshot was edited"):
            CLI.main(["run", "--resume", run_dir, "--boot", "100"])


```

- [ ] **Step 2: 失敗を確かめる**

Run: `python -m pytest tests/test_selfplay_cli.py -q -p no:cacheprovider -k TestConfigSnapshot`
Expected: 2 failed（`test_resume_reads_the_pinned_copy_not_the_live_config` が `KeyError: 'config_snapshot'`・`test_an_edited_snapshot_stops_the_resume` が
`Failed: DID NOT RAISE <class 'SystemExit'>`）。

- [ ] **Step 3: 実装**（Edit ツール）

(a) `selfplay_run.py` の import（8-13 行）:

置き換え前:
```python
import datetime
import glob
import json
import os
import subprocess
import sys
```
置き換え後:
```python
import datetime
import glob
import hashlib
import json
import os
import shutil
import subprocess
import sys
```
33 行 `COMPARE_METRICS = (...)` の直後（Task 5a の `def delegate_fingerprint(arm):` の前）に足す＝`COMPARE_METRICS` の行の次の行に `CONFIG_SNAPSHOT`、
その後に空行2つを挟んで `file_sha256`、さらに空行2つを挟んで既存の `def delegate_fingerprint(arm):`:

```python
CONFIG_SNAPSHOT = "config-snapshot.json"  # run / calibrate が出力ディレクトリに置く config の写し（spec §16.2 手順9）


def file_sha256(path):
    with open(path, "rb") as f:
        return hashlib.sha256(f.read()).hexdigest()
```
`OutputDir.read_json`（`    def read_json(self, name):` から `            return json.load(f)` までの3行・Plan B の前は 191-193 行）の直後に足す:

```python

    def snapshot_config(self, config_path):
        """使った config の写し（CONFIG_SNAPSHOT）を置き、run.json に足す {config_snapshot, config_sha256} を返す。"""
        shutil.copyfile(config_path, self.file(CONFIG_SNAPSHOT))
        return {"config_snapshot": CONFIG_SNAPSHOT, "config_sha256": file_sha256(self.file(CONFIG_SNAPSHOT))}

    def pinned_config(self, plan):
        """再開で読む config: 写し（中身が run.json の sha256 と違えば止まる）。写しの無い古い実行は plan の config_path。"""
        name = plan.get("config_snapshot")
        if not name:
            return plan["config_path"]
        path = self.file(name)
        if not os.path.exists(path):
            raise SystemExit(f"{path}: the config snapshot named in run.json is missing")
        if file_sha256(path) != plan.get("config_sha256"):
            raise SystemExit(f"{path}: the config snapshot was edited after the run started (sha256 differs)")
        return path
```

(b) `selfplay.py` の `_new_output` と `_resume`:

置き換え前:
```python
def _new_output(args, stub, plan, label):
    out = R.OutputDir.create(args.out_root, label)
    out.write_json("run.json", {**plan, **R.run_meta(stub)})
    safe_print(f"output: {out.path}")
    return out


def _resume(args):
    out = R.OutputDir(args.resume)
    plan = out.read_json("run.json")
    safe_print(f"resume: {out.path}")
    return out, plan, make_stub(plan["config_path"])
```
置き換え後:
```python
def _new_output(args, stub, plan, label):
    """出力ディレクトリ・使った config の写し（config-snapshot.json）・run.json。--resume はこの写しを読む
    （止めている間にユーザー config を変えても、再開した局の設定は変わらない・spec §16.2 手順9）。"""
    out = R.OutputDir.create(args.out_root, label)
    snapshot = out.snapshot_config(plan["config_path"])
    out.write_json("run.json", {**plan, **snapshot, **R.run_meta(stub)})
    safe_print(f"output: {out.path}")
    return out


def _resume(args):
    out = R.OutputDir(args.resume)
    plan = out.read_json("run.json")
    safe_print(f"resume: {out.path}")
    return out, plan, make_stub(out.pinned_config(plan))
```

- [ ] **Step 4: 通ることを確かめる**

Run: `python -m pytest tests/test_selfplay_*.py -q -p no:cacheprovider`
Expected: すべて PASS（既存の `test_resume_refuses_a_changed_engine_unless_allow_mixed` は run.json を書き換えるテストなので写しの影響を受けない）。

- [ ] **Step 5: コミット**

```bash
git add katrain_debug tests/test_selfplay_cli.py
git commit -m "feat(selfplay): 実行ごとに config の写しを置き、--resume はその写しを読む" -m "spec §16.2 手順9。run.json に config_snapshot と config_sha256。写しの中身が変わっていれば再開を止める。" -m "Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>"
```

---

## Task 6: 序盤の窓の指標 `window_stats.py`（TDD）

状態: 完了（`a3693bf8`）。

**Files:**
- Create: `docs/superpowers/specs/calibration-data/selfplay/window_stats.py`
- Test: `tests/test_selfplay_window.py`（Create）

**Interfaces:**
- Consumes: 実行ディレクトリの `run.json`（`size`）・`games.jsonl`（`arm`・`seed`・`result`・`opponent_stats.book_exit_depth / book_exit_reason`）・
  `moves.jsonl`（`arm`・`seed`・`depth`・`is_ai`・`match`・`points_lost`・`trailing_pass`・`lead_after_ai`・`wr_before_ai`・`wr_after_ai`・
  `decision_u`・`decision_kind`・`decision_open`）。`S.percentile`・`S.t_interval(values, conf)`・`S.wilcoxon_signed_rank(diffs)`。
- Consumes（`--recon`）: 実戦の復元と事後解析のディレクトリ（`recon_9/`・C10 の `recon_9_veil/`）の `report_game_*.json`（`summary.game`・`summary.ai`・
  `summary.board_size`〈Task 8 の後の offline_report が書く・無ければ 13路〉・`rows[].depth / player / match / ptloss`）。
- Produces: `window_stats(dirs, window=None, compare=None, conf=0.975) -> dict`（`{"sources", "size", "window", "arms": {arm: summary}, "compare": [...]}`）・
  `main(argv)`（表を ASCII で出し、`<out>/window_stats.json` を書く）・`WINDOW = {9: 12, 13: 30}`。
  spec §16.5 の「実戦」用: `recon_game_stats(rows, ai, window) -> dict`（`own_in` / `own_out` / `opp_in` / `opp_out` の `{"n", "match"}`・`opp_streak`＝相手の
  最初の手から最善手が続いた相手の手の数）・`recon_window_stats(recon_dir, window=None) -> dict`（`{"source", "size", "window", "games", "summary"}`）・
  `format_recon_text(result)`・`main([... "--recon", DIR, ...])`（DIR ごとに表・`--out` のときだけ `<out>/window_stats_recon.json`）。

- [ ] **Step 1: 失敗するテストを書く**（`tests/test_selfplay_window.py` を Write で作る）

```python
# tests/test_selfplay_window.py
"""序盤の窓の指標（docs/superpowers/specs/calibration-data/selfplay/window_stats.py）。合成した moves.jsonl で確かめる。

設計: docs/superpowers/specs/2026-09-23-veil-strategy-design.md §16.2 手順7
"""

import importlib.util
import json
import os

import pytest

HERE = os.path.dirname(os.path.abspath(__file__))
SCRIPT = os.path.join(HERE, "..", "docs", "superpowers", "specs", "calibration-data", "selfplay", "window_stats.py")
_spec = importlib.util.spec_from_file_location("window_stats", SCRIPT)
W = importlib.util.module_from_spec(_spec)
_spec.loader.exec_module(W)


def _move(arm, seed, depth, is_ai, match=True, loss=0.0, **kw):
    row = {
        "arm": arm,
        "seed": seed,
        "depth": depth,
        "is_ai": is_ai,
        "match": match,
        "points_lost": loss,
        "trailing_pass": False,
        "lead_after_ai": float(depth),
        "wr_before_ai": 0.8,
        "wr_after_ai": 0.8,
    }
    row.update(kw)
    return row


def _game(arm, seed, n_moves=8, ai_first=True, result="win", overrides=None):
    """1局の moves（AI が黒なら奇数手）。overrides: {手数: {列: 値}}。"""
    rows = []
    for d in range(1, n_moves + 1):
        is_ai = (d % 2 == 1) == ai_first
        rows.append(_move(arm, seed, d, is_ai, **(overrides or {}).get(d, {})))
    rec = {"arm": arm, "seed": seed, "result": result, "opponent_stats": {}}
    return rec, rows


def _write_run(path, games, size=9):
    os.makedirs(path, exist_ok=True)
    with open(os.path.join(path, "run.json"), "w", encoding="utf-8") as f:
        json.dump({"size": size}, f)
    with open(os.path.join(path, "games.jsonl"), "w", encoding="utf-8") as f:
        f.writelines(json.dumps(rec) + "\n" for rec, _ in games)
    with open(os.path.join(path, "moves.jsonl"), "w", encoding="utf-8") as f:
        f.writelines(json.dumps(row) + "\n" for _, rows in games for row in rows)
    return str(path)


class TestWindowSplit:
    def test_rates_losses_and_lead_on_both_sides_of_the_window(self, tmp_path):
        game = _game(
            "open",
            1000,
            overrides={
                1: {"match": False, "loss": 2.5},
                2: {"match": False, "loss": 6.5},
                5: {"match": False, "loss": 0.4},
            },
        )
        run = _write_run(tmp_path / "r", [game])
        s = W.window_stats([run], window=4)["arms"]["open"]
        assert s["games"] == 1
        assert s["own_in"]["n"] == 2 and s["own_in"]["rate_mean"] == 0.5 and s["own_in"]["ge2_per_game"] == 1
        assert s["opp_in"]["rate_pooled"] == 0.5 and s["opp_in"]["ge6_per_game"] == 1
        assert s["own_out"]["n"] == 2 and s["own_out"]["rate_mean"] == 0.5
        assert s["own_out"]["loss_per_move"] == pytest.approx(0.2)
        assert s["lead_at_w"]["median"] == 4.0  # 手数 <= W の最後の行（手数 4）の lead_after_ai

    def test_default_window_follows_the_board_size(self, tmp_path):
        run9 = _write_run(tmp_path / "r9", [_game("a", 1000)], size=9)
        run13 = _write_run(tmp_path / "r13", [_game("a", 1000)], size=13)
        assert W.window_stats([run9])["window"] == 12 and W.window_stats([run13])["window"] == 30
        with pytest.raises(SystemExit, match="no default window"):
            W.window_stats([_write_run(tmp_path / "r19", [_game("a", 1000)], size=19)])

    def test_aborted_games_are_excluded(self, tmp_path):
        run = _write_run(tmp_path / "r", [_game("a", 1000), _game("a", 1001, result="aborted")])
        assert W.window_stats([run], window=4)["arms"]["a"]["games"] == 1

    def test_report_definition_drops_moves_without_points_lost_and_trailing_passes(self, tmp_path):
        game = _game("a", 1000, overrides={1: {"points_lost": None}, 7: {"trailing_pass": True, "match": False}})
        s = W.window_stats([_write_run(tmp_path / "r", [game])], window=4)["arms"]["a"]
        assert s["own_in"]["n"] == 1 and s["own_out"]["n"] == 1 and s["own_out"]["rate_mean"] == 1.0

    def test_flips_u_and_paid_after_the_window(self, tmp_path):
        game = _game(
            "a",
            1000,
            overrides={
                3: {"wr_before_ai": 0.6, "wr_after_ai": 0.4},
                5: {"decision_u": 1.0, "decision_kind": "paid"},
                7: {"decision_u": 0.5, "decision_kind": "free", "wr_before_ai": 0.55, "wr_after_ai": 0.45},
            },
        )
        s = W.window_stats([_write_run(tmp_path / "r", [game])], window=4)["arms"]["a"]
        assert s["flip_in_per_game"] == 1 and s["flip_out_per_game"] == 1
        assert s["u_after_mean"] == 0.75 and s["paid_after_per_game"] == 1

    def test_open_window_is_checked_against_decision_open(self, tmp_path):
        game = _game(
            "open",
            1000,
            overrides={1: {"decision_open": "played"}, 3: {"decision_open": "gate"}, 5: {"decision_open": "played"}},
        )
        s = W.window_stats([_write_run(tmp_path / "r", [game])], window=2)["arms"]["open"]
        assert s["open"] == {"played": 2, "gate": 1} and s["open_outside"] == 2 and s["open_missing_inside"] == 0
        plain = W.window_stats([_write_run(tmp_path / "p", [_game("layers", 1000)])], window=2)["arms"]["layers"]
        assert plain["open"] == {} and plain["open_missing_inside"] == 0  # 韜晦の窓の無いアームは数えない

    def test_book_exit_comes_from_the_opponent_stats(self, tmp_path):
        rec, rows = _game("a", 1000)
        rec["opponent_stats"] = {"book_exit_depth": 7, "book_exit_reason": "ai_loss"}
        rec2, rows2 = _game("a", 1001)
        rec2["opponent_stats"] = {"book_exit_depth": 24, "book_exit_reason": "limit"}
        s = W.window_stats([_write_run(tmp_path / "r", [(rec, rows), (rec2, rows2)])], window=4)["arms"]["a"]
        assert s["book_exit"] == {"games": 2, "median": 15.5, "min": 7, "reasons": {"ai_loss": 1, "limit": 1}}


class TestPairsAndRuns:
    def test_paired_diff_uses_the_same_seeds(self, tmp_path):
        games = [
            _game("open", 1000, overrides={1: {"match": False}}),
            _game("layers", 1000),
            _game("open", 1001, overrides={3: {"match": False}}),
            _game("layers", 1001),
            _game("layers", 1002),  # 対の無い seed は数えない
        ]
        result = W.window_stats([_write_run(tmp_path / "r", games)], window=4, compare=("open", "layers"))
        d = result["compare"][0]["diffs"]
        assert d["own_in"]["n"] == 2 and d["own_in"]["mean"] == -0.5
        assert d["own_out"]["mean"] == 0.0 and d["lead_at_w"]["mean"] == 0.0
        assert "## paired diff open - layers" in W.format_text(result)

    def test_two_runs_are_read_together_and_duplicates_stop(self, tmp_path):
        a = _write_run(tmp_path / "a", [_game("open", 1000)])
        b = _write_run(tmp_path / "b", [_game("open", 1024)])
        assert W.window_stats([a, b], window=4)["arms"]["open"]["games"] == 2
        with pytest.raises(SystemExit, match="appears twice"):
            W.window_stats([a, a], window=4)
        with pytest.raises(SystemExit, match="different board sizes"):
            W.window_stats([a, _write_run(tmp_path / "c", [_game("open", 2000)], size=13)], window=4)

    def test_main_writes_the_json_and_an_ascii_table(self, tmp_path, capsys):
        run = _write_run(tmp_path / "r", [_game("open", 1000), _game("layers", 1000)])
        W.main([run, "--window", "4", "--compare", "open", "layers"])
        saved = json.load(open(os.path.join(run, "window_stats.json"), encoding="utf-8"))
        assert saved["window"] == 4 and set(saved["arms"]) == {"layers", "open"}
        out = capsys.readouterr().out
        assert out.startswith("# window stats (W = 4") and out.isascii()


def _report(path, name, ai, matches, size=9):
    """合成の recon の report_<name>.json（rows は手数 1 から・黒が先・matches[i] は手数 i + 1 の match）。"""
    rows = [
        {"depth": d, "player": "B" if d % 2 else "W", "match": m, "ptloss": 0.0 if m else 1.0}
        for d, m in enumerate(matches, 1)
    ]
    summary = {"game": name, "ai": ai, "n_moves": len(rows), "strategy": "Veil9Strategy", "board_size": size}
    os.makedirs(path, exist_ok=True)
    with open(os.path.join(path, f"report_{name}.json"), "w", encoding="utf-8") as f:
        json.dump({"summary": summary, "rows": rows}, f)
    return str(path)


class TestReconMode:
    """実戦の事後解析（spec 2026-09-23-veil-strategy-design.md §16.5 の「実戦」: recon_9_veil と recon_9 を比べる）。"""

    def test_opponent_rates_and_best_move_streak(self, tmp_path):
        # AI は黒（奇数手）。相手（偶数手）は 2・4 手目が最善手、6 手目で外れ、8 手目は最善手
        run = _report(tmp_path / "rv", "game_1", "B", [True, True, False, True, True, False, True, True])
        result = W.recon_window_stats(run, window=4)
        (g,) = result["games"]
        assert g["opp_streak"] == 2 and g["opp_in"] == {"n": 2, "match": 2} and g["opp_out"] == {"n": 2, "match": 1}
        assert g["own_in"] == {"n": 2, "match": 1} and g["own_out"] == {"n": 2, "match": 2}
        assert result["window"] == 4 and result["summary"]["opp_out"]["rate_pooled"] == 0.5

    def test_default_window_and_the_table(self, tmp_path, capsys):
        veil = _report(tmp_path / "recon_9_veil", "game_1", "W", [True] * 30)
        enigma = _report(tmp_path / "recon_9", "game_2", "W", [False] * 30)
        assert W.recon_window_stats(veil)["window"] == 12  # 9路の既定の窓
        W.main(["--recon", veil, enigma])
        out = capsys.readouterr().out
        assert out.isascii() and out.count("# real games ") == 2
        assert not os.path.exists(os.path.join(veil, "window_stats_recon.json"))  # --out が無ければ書かない
        lines = out.splitlines()
        assert any(line.startswith("game_1 ") and line.split()[-1] == "15" for line in lines)  # 相手の 15 手すべて
        assert any(line.startswith("game_2 ") and line.split()[-1] == "0" for line in lines)
        W.main(["--recon", veil, "--out", str(tmp_path)])
        (saved,) = json.load(open(os.path.join(tmp_path, "window_stats_recon.json"), encoding="utf-8"))
        assert saved["games"][0]["opp_streak"] == 15
```

- [ ] **Step 2: 失敗を確かめる**

Run: `python -m pytest tests/test_selfplay_window.py -q -p no:cacheprovider`
Expected: 収集の ERROR（`FileNotFoundError: ... window_stats.py`）。

- [ ] **Step 3: 実装**（`docs/superpowers/specs/calibration-data/selfplay/window_stats.py` を Write で作る）

```python
"""序盤の窓の指標（spec 2026-09-23-veil-strategy-design.md §16.2 手順7・§16.5）: 自己対局ハーネスの実行ディレクトリの
moves.jsonl / games.jsonl から、アームごとに窓の中（手数 <= W）と窓の後の一致率・損失・大きな損、W 手目のリード、flip、
窓の後の緊急度 u と支払った外し（paid）、定跡を知る相手が定跡を外れた手数を出す。

    python window_stats.py DIR [DIR ...] [--window W] [--compare A B] [--conf 0.975] [--out DIR]
    python window_stats.py --recon RECON [RECON ...] [--window W] [--out DIR]   # 実戦の事後解析（spec §16.5 の「実戦」）

- W の既定は run.json の盤サイズ（9路 12・13路 30）。複数の DIR（例 9-1 と 9-2）は合わせて読む（同じ (arm, seed) は止まる）。
- 中断した局（games.jsonl の result aborted）は除く。一致率はレポートと同じ定義（points_lost の無い手と、最後の石より後の
  パスを除く）。手数は moves.jsonl の depth（打った手のノードの手数）。
- 韜晦のアームは decision_open でも窓を確かめる（窓の外に decision_open がある手・窓の中の AI の手で decision_open が無い手を数える）。
- --compare A B: 同じ seed の対の差 A − B（窓の中・後の自分と相手の一致率・W 手目のリード）。t 区間（--conf）と Wilcoxon の p。
- --recon: DIR は実戦の復元と事後解析のディレクトリ（recon_9/・recon_9_veil/ の report_game_*.json）。局ごとと全体の、自分と相手の
  窓の中・後の一致率（2500v の最善手との一致）と、相手の最初の手から最善手が続いた手数（opp_streak）。DIR ごとに別の表（合わせない）。
  json は --out を渡したときだけ <out>/window_stats_recon.json に書く（コミット済みの recon に書かない）。
出力は表（ASCII）と <out>/window_stats.json（既定の out は最初の DIR）。KataGo は使わない。
"""

import argparse
import glob
import json
import os
import statistics
import sys
from collections import Counter

REPO = os.path.abspath(os.path.join(os.path.dirname(os.path.abspath(__file__)), "..", "..", "..", "..", ".."))
sys.path.insert(0, REPO)

from katrain_debug import selfplay_stats as S  # noqa: E402

WINDOW = {9: 12, 13: 30}  # spec §15.2 の想定の窓（9路 12・13路 30）
SIDES = (("own", True), ("opp", False))


def read_jsonl(path):
    if not os.path.exists(path):
        return []
    with open(path, encoding="utf-8") as f:
        return [json.loads(line) for line in f if line.strip()]


def load_runs(dirs):
    """-> (size, records, moves)。中断した局は records・moves の両方から除く。"""
    sizes, records, moves, seen = set(), [], [], set()
    for d in dirs:
        with open(os.path.join(d, "run.json"), encoding="utf-8") as f:
            sizes.add(json.load(f)["size"])
        for r in read_jsonl(os.path.join(d, "games.jsonl")):
            key = (r["arm"], r["seed"])
            if key in seen:
                raise SystemExit(f"arm {r['arm']} seed {r['seed']} appears twice ({d})")
            seen.add(key)
            records.append(r)
        moves.extend(read_jsonl(os.path.join(d, "moves.jsonl")))
    if len(sizes) != 1:
        raise SystemExit(f"runs of different board sizes: {sorted(sizes)}")
    ok = {(r["arm"], r["seed"]) for r in records if r.get("result") != "aborted"}
    return (
        sizes.pop(),
        [r for r in records if (r["arm"], r["seed"]) in ok],
        [m for m in moves if (m["arm"], m["seed"]) in ok],
    )


def _mean(values):
    values = [v for v in values if v is not None]
    return statistics.fmean(values) if values else None


def game_window_stats(rows, window):
    """1局分の moves の行 → 窓の中（in）と後（out）の自分・相手の数・W 手目のリード・flip・窓の後の u と paid。"""
    out = {}
    counted = [r for r in rows if r.get("points_lost") is not None and not r.get("trailing_pass")]
    for side, is_ai in SIDES:
        for part in ("in", "out"):
            sel = [r for r in counted if r["is_ai"] == is_ai and (r["depth"] <= window) == (part == "in")]
            losses = [max(0.0, r["points_lost"]) for r in sel]
            out[f"{side}_{part}"] = {
                "n": len(sel),
                "match": sum(1 for r in sel if r["match"]),
                "loss": sum(losses),
                "ge2": sum(1 for x in losses if x >= 2.0),
                "ge6": sum(1 for x in losses if x >= 6.0),
            }
    inside = [r for r in rows if r["depth"] <= window]
    out["lead_at_w"] = inside[-1].get("lead_after_ai") if inside else None
    ai_rows = [r for r in rows if r["is_ai"]]
    for part in ("in", "out"):
        out[f"flip_{part}"] = sum(
            1
            for r in ai_rows
            if (r["depth"] <= window) == (part == "in")
            and r.get("wr_before_ai") is not None
            and r.get("wr_after_ai") is not None
            and r["wr_before_ai"] >= 0.5
            and r["wr_after_ai"] < 0.5
        )
    after = [r for r in ai_rows if r["depth"] > window]
    out["u_after"] = [r["decision_u"] for r in after if isinstance(r.get("decision_u"), (int, float))]
    out["paid_after"] = sum(1 for r in after if r.get("decision_kind") == "paid")
    opened = [r for r in ai_rows if r.get("decision_open") is not None]
    out["open"] = dict(Counter(r["decision_open"] for r in opened))
    out["open_outside"] = sum(1 for r in opened if r["depth"] > window)
    out["open_missing_inside"] = (
        sum(1 for r in ai_rows if r["depth"] <= window and r.get("decision_open") is None) if opened else 0
    )
    return out


def rate(block):
    return block["match"] / block["n"] if block["n"] else None


def per_game(records, moves, window):
    """(arm, seed) → 1局の指標（game_window_stats ＋ 定跡を知る相手の book_exit_depth / book_exit_reason）。"""
    rows_of = {}
    for m in moves:
        rows_of.setdefault((m["arm"], m["seed"]), []).append(m)
    out = {}
    for r in records:
        key = (r["arm"], r["seed"])
        st = game_window_stats(sorted(rows_of.get(key, []), key=lambda m: m["depth"]), window)
        opp = r.get("opponent_stats") or {}
        st["book_exit_depth"] = opp.get("book_exit_depth")
        st["book_exit_reason"] = opp.get("book_exit_reason")
        out[key] = st
    return out


def arm_summary(games):
    """1アームの局の指標のリスト → 要約（一致率は手をまとめた値と局平均・損失/手・大きな損/局・リードの分布ほか）。"""
    s = {"games": len(games)}
    for side, _ in SIDES:
        for part in ("in", "out"):
            blocks = [g[f"{side}_{part}"] for g in games]
            n = sum(b["n"] for b in blocks)
            s[f"{side}_{part}"] = {
                "n": n,
                "rate_pooled": sum(b["match"] for b in blocks) / n if n else None,
                "rate_mean": _mean([rate(b) for b in blocks]),
                "loss_per_move": sum(b["loss"] for b in blocks) / n if n else None,
                "ge2_per_game": _mean([b["ge2"] for b in blocks]),
                "ge6_per_game": _mean([b["ge6"] for b in blocks]),
            }
    leads = [g["lead_at_w"] for g in games if g["lead_at_w"] is not None]
    s["lead_at_w"] = {
        "n": len(leads),
        "median": S.percentile(leads, 0.5),
        "p25": S.percentile(leads, 0.25),
        "p75": S.percentile(leads, 0.75),
        "min": min(leads) if leads else None,
        "negative": sum(1 for x in leads if x < 0),
    }
    s["flip_in_per_game"] = _mean([g["flip_in"] for g in games])
    s["flip_out_per_game"] = _mean([g["flip_out"] for g in games])
    s["u_after_mean"] = _mean([u for g in games for u in g["u_after"]])
    s["paid_after_per_game"] = _mean([g["paid_after"] for g in games])
    opened = Counter()
    for g in games:
        opened.update(g["open"])
    s["open"] = dict(opened)
    s["open_outside"] = sum(g["open_outside"] for g in games)
    s["open_missing_inside"] = sum(g["open_missing_inside"] for g in games)
    exits = [g["book_exit_depth"] for g in games if g["book_exit_depth"] is not None]
    s["book_exit"] = {
        "games": len(exits),
        "median": S.percentile(exits, 0.5),
        "min": min(exits) if exits else None,
        "reasons": dict(Counter(g["book_exit_reason"] for g in games if g["book_exit_reason"])),
    }
    return s


PAIRED = (
    ("own_in", lambda g: rate(g["own_in"])),
    ("own_out", lambda g: rate(g["own_out"])),
    ("opp_in", lambda g: rate(g["opp_in"])),
    ("opp_out", lambda g: rate(g["opp_out"])),
    ("lead_at_w", lambda g: g["lead_at_w"]),
)


def paired_diffs(stats, a, b, conf=0.975):
    """同じ seed の対の差 a − b（窓の中・後の一致率と W 手目のリード）。"""
    seeds = sorted({s for arm, s in stats if arm == a} & {s for arm, s in stats if arm == b})
    out = {}
    for name, fn in PAIRED:
        diffs = []
        for seed in seeds:
            va, vb = fn(stats[a, seed]), fn(stats[b, seed])
            if va is not None and vb is not None:
                diffs.append(va - vb)
        lo, hi = S.t_interval(diffs, conf)
        out[name] = {
            "n": len(diffs),
            "mean": statistics.fmean(diffs) if diffs else None,
            "t_ci": [lo, hi],
            "wilcoxon_p": S.wilcoxon_signed_rank(diffs),
        }
    return out


def fmt(v, spec=".3f"):
    return "-" if v is None else format(v, spec)


def pct(v):
    return "-" if v is None else f"{100 * v:.1f}%"


def format_text(result):
    w = result["window"]
    lines = [
        f"# window stats (W = {w}: in = depth <= {w}, out = depth > {w}; aborted games excluded)",
        "",
        f"{'arm':<12} {'n':>3} {'own in':>7} {'own out':>7} {'opp in':>7} {'opp out':>7} {'loss o in/out':>13} "
        f"{'>=2 in':>6} {'>=6 in':>6} {'lead@W med':>10} {'<0':>3} {'flip i/o':>9} {'u out':>5} {'paid out':>8} "
        f"{'book exit':>9}",
    ]
    for arm, s in result["arms"].items():
        lines.append(
            f"{arm[:12]:<12} {s['games']:>3} {pct(s['own_in']['rate_mean']):>7} {pct(s['own_out']['rate_mean']):>7} "
            f"{pct(s['opp_in']['rate_mean']):>7} {pct(s['opp_out']['rate_mean']):>7} "
            f"{fmt(s['own_in']['loss_per_move'], '.2f'):>6}/{fmt(s['own_out']['loss_per_move'], '.2f'):<6} "
            f"{fmt(s['own_in']['ge2_per_game'], '.2f'):>6} {fmt(s['own_in']['ge6_per_game'], '.2f'):>6} "
            f"{fmt(s['lead_at_w']['median'], '+.1f'):>10} {s['lead_at_w']['negative']:>3} "
            f"{fmt(s['flip_in_per_game'], '.2f'):>4}/{fmt(s['flip_out_per_game'], '.2f'):<4} "
            f"{fmt(s['u_after_mean'], '.2f'):>5} {fmt(s['paid_after_per_game'], '.2f'):>8} "
            f"{fmt(s['book_exit']['median'], '.0f'):>9}"
        )
    for arm, s in result["arms"].items():
        if s["open"]:
            lines.append(
                f"open {arm}: {s['open']} outside_window={s['open_outside']} missing_inside={s['open_missing_inside']}"
            )
        if s["book_exit"]["reasons"]:
            lines.append(f"book {arm}: exit reasons {s['book_exit']['reasons']} (games {s['book_exit']['games']})")
    for c in result.get("compare") or []:
        lines += ["", f"## paired diff {c['a']} - {c['b']} (same seed; conf {c['conf']})"]
        for name, d in c["diffs"].items():
            lines.append(
                f"{name:<10} n={d['n']:>3} mean={fmt(d['mean'], '+.4f')} t={fmt(d['t_ci'][0], '+.4f')}.."
                f"{fmt(d['t_ci'][1], '+.4f')} wilcoxon_p={fmt(d['wilcoxon_p'], '.4f')}"
            )
    return "\n".join(lines) + "\n"


def window_stats(dirs, window=None, compare=None, conf=0.975):
    size, records, moves = load_runs(dirs)
    window = window or WINDOW.get(size)
    if window is None:
        raise SystemExit(f"no default window for {size}x{size}: pass --window W")
    stats = per_game(records, moves, window)
    arms = sorted({arm for arm, _ in stats})
    result = {
        "sources": list(dirs),
        "size": size,
        "window": window,
        "arms": {arm: arm_summary([g for (a, _), g in sorted(stats.items()) if a == arm]) for arm in arms},
    }
    if compare:
        a, b = compare
        result["compare"] = [{"a": a, "b": b, "conf": conf, "diffs": paired_diffs(stats, a, b, conf)}]
    return result


def recon_game_stats(rows, ai, window):
    """実戦の事後解析の1局（report の rows: depth・player・match・ptloss）→ 窓の中（手数 <= W）と後の自分・相手の手の数と
    最善手との一致の数（ptloss の無い手は除く＝offline_report と同じ）と、相手の最初の手から最善手が続いた相手の手の数（opp_streak）。"""
    out = {}
    counted = [r for r in rows if r.get("ptloss") is not None]
    for side, mine in SIDES:
        for part in ("in", "out"):
            sel = [r for r in counted if (r["player"] == ai) == mine and (r["depth"] <= window) == (part == "in")]
            out[f"{side}_{part}"] = {"n": len(sel), "match": sum(1 for r in sel if r["match"])}
    streak = 0
    for r in rows:
        if r["player"] == ai:
            continue
        if not r["match"]:
            break
        streak += 1
    out["opp_streak"] = streak
    return out


def recon_window_stats(recon_dir, window=None):
    """recon の report_game_*.json → {"source", "size", "window", "games": [...], "summary": {...}}（spec §16.5 の「実戦」）。"""
    reports = []
    for path in sorted(glob.glob(os.path.join(recon_dir, "report_game_*.json"))):
        with open(path, encoding="utf-8") as f:
            reports.append(json.load(f))
    if not reports:
        raise SystemExit(f"{recon_dir}: no report_game_*.json")
    sizes = {r["summary"].get("board_size", 13) for r in reports}
    if len(sizes) != 1:
        raise SystemExit(f"{recon_dir}: games of different board sizes: {sorted(sizes)}")
    size = sizes.pop()
    window = window or WINDOW.get(size)
    if window is None:
        raise SystemExit(f"no default window for {size}x{size}: pass --window W")
    games = [{"game": r["summary"]["game"], **recon_game_stats(r["rows"], r["summary"]["ai"], window)} for r in reports]
    summary = {}
    for side, _ in SIDES:
        for part in ("in", "out"):
            key = f"{side}_{part}"
            n = sum(g[key]["n"] for g in games)
            summary[key] = {
                "n": n,
                "rate_pooled": sum(g[key]["match"] for g in games) / n if n else None,
                "rate_mean": _mean([rate(g[key]) for g in games]),
            }
    streaks = [g["opp_streak"] for g in games]
    summary["opp_streak"] = {"median": S.percentile(streaks, 0.5), "min": min(streaks), "max": max(streaks)}
    return {"source": recon_dir, "size": size, "window": window, "games": games, "summary": summary}


def format_recon_text(result):
    w = result["window"]
    lines = [
        f"# real games {result['source']} (W = {w}: in = depth <= {w}; streak = opponent best moves from its first move)",
        f"{'game':<24} {'own in':>7} {'own out':>7} {'opp in':>7} {'opp out':>7} {'streak':>6}",
    ]
    for g in result["games"]:
        lines.append(
            f"{g['game'][:24]:<24} {pct(rate(g['own_in'])):>7} {pct(rate(g['own_out'])):>7} "
            f"{pct(rate(g['opp_in'])):>7} {pct(rate(g['opp_out'])):>7} {g['opp_streak']:>6}"
        )
    s = result["summary"]
    lines.append(
        f"{'mean of %d games' % len(result['games']):<24} {pct(s['own_in']['rate_mean']):>7} "
        f"{pct(s['own_out']['rate_mean']):>7} {pct(s['opp_in']['rate_mean']):>7} {pct(s['opp_out']['rate_mean']):>7} "
        f"{fmt(s['opp_streak']['median'], '.1f'):>6}"
    )
    return "\n".join(lines) + "\n\n"


def main(argv=None):
    ap = argparse.ArgumentParser(description="序盤の窓の指標（spec §16.2 手順7）")
    ap.add_argument("dirs", nargs="+", metavar="DIR", help="自己対局の実行ディレクトリ（合わせて読む）")
    ap.add_argument("--window", type=int, default=None, help="窓の手数 W（既定: 9路 12・13路 30）")
    ap.add_argument("--compare", nargs=2, metavar=("A", "B"), help="同じ seed の対の差 A - B")
    ap.add_argument("--conf", type=float, default=0.975, help="対の差の t 区間の信頼度")
    ap.add_argument("--out", default=None, metavar="DIR", help="window_stats.json の書き先（既定: 最初の DIR）")
    ap.add_argument(
        "--recon",
        action="store_true",
        help="DIR は実戦の復元と事後解析（report_game_*.json）＝実戦の相手の一致率（spec §16.5）",
    )
    args = ap.parse_args(argv)
    if args.recon:
        results = [recon_window_stats(d, args.window) for d in args.dirs]
        if args.out:
            with open(os.path.join(args.out, "window_stats_recon.json"), "w", encoding="utf-8") as f:
                json.dump(results, f, ensure_ascii=False, indent=1)
        text = "".join(format_recon_text(r) for r in results)
        print(text.encode("ascii", "replace").decode("ascii"), end="")
        return results
    result = window_stats(args.dirs, args.window, args.compare, args.conf)
    with open(os.path.join(args.out or args.dirs[0], "window_stats.json"), "w", encoding="utf-8") as f:
        json.dump(result, f, ensure_ascii=False, indent=1)
    text = format_text(result)
    print(text.encode("ascii", "replace").decode("ascii"), end="")
    return result


if __name__ == "__main__":
    main()
```

- [ ] **Step 4: 通ることを確かめる**

Run: `python -m pytest tests/test_selfplay_window.py -q -p no:cacheprovider` と `python -m black -l 120 --check docs/superpowers/specs/calibration-data/selfplay/window_stats.py tests/test_selfplay_window.py`
Expected: 12 passed・black は `2 files would be left unchanged`。

- [ ] **Step 5: コミット**

```bash
git add docs/superpowers/specs/calibration-data/selfplay/window_stats.py tests/test_selfplay_window.py
git commit -m "feat(selfplay): 序盤の窓の指標 window_stats.py を足す" -m "spec §16.2 手順7・§16.5。窓の中と後の一致率・損失・大きな損・W 手目のリード・flip・窓の後の u と paid・定跡の出口・decision_open の確かめ・同じ seed の対の差。--recon で実戦の事後解析（相手の窓の中と後の一致率・最善手が続いた手数）。" -m "Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>"
```

---

## Task 7: 実戦ログの復元 `recon_logs.py` を 9路に対応させる（TDD）

状態: 完了（`f8a632bd`）。

**Files:**
- Rewrite: `docs/superpowers/specs/calibration-data/selfplay/recon_logs.py`（Write で全体を置き換える。今のファイルは black 済み・255 行）
- Test: `tests/test_selfplay_recon.py`（Create。Task 8 でクラスを足す）

**Interfaces:**
- Produces（C2 が使う）:
  - CLI `python recon_logs.py [GAME ...] [--log-dir DIR] [--out DIR] [--size 9|13|19] [--strategy NAME]`（既定は今と同じ＝13路 `GAMES_13` → `recon/`）
  - `EXCLUDE_9 = {"game_20260919_224212": "持碁9路（初手だけ難解＋）"}`（9路の既定の局は log-dir の全部 − EXCLUDE_9）
  - `DEFAULT_STRATEGY = {9: "Enigma9PlusStrategy"}`: 9路はログの最初の `Generating move using` の戦略がこれの局だけを復元する（spec §16.2 手順1:
    一致率ひかえめ9路の実戦は同じ `realgames-9x9-logs/` に退避されるが `recon_9/` に混ぜない）。`--strategy Veil9Strategy` で一致率ひかえめ9路だけ
    （出力先 `recon_9_veil/`・C10）。13路は絞らない（`GAMES_13` の明示のまま）。絞った局は `<game> skipped: strategy <X> is not <Y>` と出す。
  - `insert_opponent_passes(events) -> (events, inserted)`・`setup_as_first_move(init_stones, first_player) -> (stones, first, move|None)`・`gtp_to_sgf(gtp, size)`
  - `summary.json` の各局に `board_size`・`komi`・`setup_as_first_move`・`inserted_opponent_passes`・`settings`（その局の戦略の最初の
    `Initializing` の設定）・`settings_last`・`settings_changed`（13路の元の出力のキーに足すだけ）
  - 9路の直し（spec §16.2 手順1）: root の置き石が相手の初手1子（色が initialPlayer の反対）なら初手に戻す／AI の手番の理由文に
    `opponent passed` があり直前が AI の手なら相手のパスを挟む

- [ ] **Step 1: 失敗するテストを書く**（`tests/test_selfplay_recon.py` を Write で作る）

```python
# tests/test_selfplay_recon.py
"""実戦の復元と事後解析のスクリプト（docs/superpowers/specs/calibration-data/selfplay/ の recon_logs.py・offline_report.py・
calib_targets.py）。KataGo は起動しない（offline_report は summarize だけ・calib_targets は合成の report で走らせる）。

設計: docs/superpowers/specs/2026-09-23-veil-strategy-design.md §16.2 手順1〜4
"""

import hashlib
import importlib.util
import json
import os
import subprocess
import sys

HERE = os.path.dirname(os.path.abspath(__file__))
DATA = os.path.join(HERE, "..", "docs", "superpowers", "specs", "calibration-data", "selfplay")


def _load(name):
    spec = importlib.util.spec_from_file_location(name, os.path.join(DATA, name + ".py"))
    module = importlib.util.module_from_spec(spec)
    spec.loader.exec_module(module)
    return module


class TestReconLogs:
    def test_missing_opponent_pass_is_inserted_before_an_opponent_passed_turn(self):
        rl = _load("recon_logs")
        reason = {"reason": "Enigma9Plus: opponent passed but the board is not settled, playing best move."}
        events = [
            ("ai", "E5", {"reason": "x"}),
            ("opp", "C3", None),
            ("ai", "F2", {"reason": "y"}),
            ("ai", "G8", reason),
        ]
        fixed, inserted = rl.insert_opponent_passes(events)
        assert [(w, m) for w, m, _ in fixed] == [
            ("ai", "E5"),
            ("opp", "C3"),
            ("ai", "F2"),
            ("opp", "pass"),
            ("ai", "G8"),
        ]
        assert inserted == [4]  # 1 始まりの手数

    def test_a_logged_opponent_pass_is_not_doubled(self):
        rl = _load("recon_logs")
        reason = {"reason": "Enigma9Plus: opponent passed but the board is not settled, playing best move."}
        events = [("ai", "E5", {"reason": "x"}), ("opp", "pass", None), ("ai", "G8", reason)]
        assert rl.insert_opponent_passes(events) == (events, [])

    def test_a_single_setup_stone_of_the_other_colour_is_the_first_move(self):
        rl = _load("recon_logs")
        assert rl.setup_as_first_move([("B", "G7")], "W") == ([], "B", ("B", "G7"))
        assert rl.setup_as_first_move([], "B") == ([], "B", None)
        assert rl.setup_as_first_move([("B", "C3"), ("B", "G7")], "W") == ([("B", "C3"), ("B", "G7")], "W", None)

    def test_board_size_goes_into_the_sgf_coordinates(self):
        rl = _load("recon_logs")
        assert rl.gtp_to_sgf("G7", 9) == "gc" and rl.gtp_to_sgf("G7", 13) == "gg" and rl.gtp_to_sgf("pass", 9) == ""
        assert rl.EXCLUDE_9 == {"game_20260919_224212": "持碁9路（初手だけ難解＋）"}

    def test_9x9_log_is_reconstructed_with_the_fixes(self, tmp_path):
        """合成のログ（相手の初手が root の置き石・監視の外の相手のパス）から SGF と summary を作る。"""
        stones = '"initialStones": [["B", "G7"]], "rules": "chinese", "initialPlayer": "W"'
        settings = {"enigma9plus_max_loss": 1.6, "enigma9plus_target_score": 0.2}
        lines = [
            "board_watch: アプリの局面（9路）を取り込みました（手番=W・2子以内なので石数から確定）",
            'Sending query QUERY:1: {"komi": 7.0, ' + stones + "}",
            f"Initializing Enigma9PlusStrategy with settings: {settings!r}",
            "Generating move using Enigma9PlusStrategy (mode ai:enigma9plus)",
            "Move generation complete: F6 -- Enigma9Plus: best move is also the most confusing in budget, playing it.",
            "board_watch: 相手の着手 G6 を反映しました",
            "Generating move using Enigma9PlusStrategy (mode ai:enigma9plus)",
            "Move generation complete: F5 -- Enigma9Plus: no admissible deviation (cap 6.00), playing best move.",
            "Generating move using Enigma9PlusStrategy (mode ai:enigma9plus)",
            "Move generation complete: G8 -- Enigma9Plus: opponent passed but the board is not settled, playing best move.",
        ]
        (tmp_path / "logs").mkdir()
        (tmp_path / "logs" / "game_20260919_213546.log").write_text("\n".join(lines) + "\n", encoding="utf-8")
        rl = _load("recon_logs")
        rl.main(["--size", "9", "--log-dir", str(tmp_path / "logs"), "--out", str(tmp_path / "recon_9")])
        sgf = (tmp_path / "recon_9" / "game_20260919_213546.sgf").read_text(encoding="utf-8")
        assert sgf.startswith("(;GM[1]FF[4]SZ[9]KM[7]RU[chinese]PB[app]PW[AI:Enigma9PlusStrategy]")
        assert sgf.endswith("PL[B];B[gc];W[fd];B[gd];W[fe];B[];W[gb])")
        (s,) = json.loads((tmp_path / "recon_9" / "summary.json").read_text(encoding="utf-8"))
        assert s["board_size"] == 9 and s["ai"] == "W" and s["n_moves"] == 6 and s["problems"] == []
        assert s["setup_as_first_move"] == ["B", "G7"] and s["inserted_opponent_passes"] == [5]
        assert s["settings"] == settings and s["settings_changed"] == []

    def test_9x9_default_keeps_only_the_enigma9plus_games(self, tmp_path):
        """spec §16.2 手順1: 同じ置き場に退避した一致率ひかえめ9路の局は recon_9 に混ぜない（--strategy で recon_9_veil に出す）。"""
        (tmp_path / "logs").mkdir()
        for name, cls, mode in (
            ("game_20260930_200000", "Enigma9PlusStrategy", "ai:enigma9plus"),
            ("game_20260930_210000", "Veil9Strategy", "ai:veil9"),
        ):
            lines = [
                'Sending query QUERY:1: {"komi": 7.0, "initialStones": [], "rules": "chinese", "initialPlayer": "B"}',
                f"Generating move using {cls} (mode {mode})",
                "Move generation complete: E5 -- best move is also the most confusing in budget, playing it.",
                "board_watch: 相手の着手 C3 を反映しました",
            ]
            (tmp_path / "logs" / f"{name}.log").write_text("\n".join(lines) + "\n", encoding="utf-8")
        rl = _load("recon_logs")
        logs = ["--size", "9", "--log-dir", str(tmp_path / "logs")]
        rl.main([*logs, "--out", str(tmp_path / "recon_9")])
        (s,) = json.loads((tmp_path / "recon_9" / "summary.json").read_text(encoding="utf-8"))
        assert (s["game"], s["strategy"]) == ("game_20260930_200000", "Enigma9PlusStrategy")
        assert not (tmp_path / "recon_9" / "game_20260930_210000.sgf").exists()
        rl.main([*logs, "--strategy", "Veil9Strategy", "--out", str(tmp_path / "recon_9_veil")])
        (v,) = json.loads((tmp_path / "recon_9_veil" / "summary.json").read_text(encoding="utf-8"))
        assert (v["game"], v["strategy"], v["n_moves"]) == ("game_20260930_210000", "Veil9Strategy", 2)
```

（`hashlib`・`subprocess`・`sys` の import は Task 8 のクラスが使う。Task 7 の時点では未使用だが、flake8 は掛けていないのでそのままでよい。）

- [ ] **Step 2: 失敗を確かめる**

Run: `python -m pytest tests/test_selfplay_recon.py -q -p no:cacheprovider`
Expected: 6 failed（`AttributeError: module 'recon_logs' has no attribute 'insert_opponent_passes'` が2件・`... 'setup_as_first_move'`・
`... 'EXCLUDE_9'`〈今の `gtp_to_sgf(gtp, size=13)` は盤を受けるので座標の assert は通る〉・`TypeError: main() takes 0 positional arguments but 1 was given`
が2件〈`test_9x9_log_is_reconstructed_with_the_fixes`・`test_9x9_default_keeps_only_the_enigma9plus_games`〉）。
今の recon_logs.py は import のときに `recon/` を `os.makedirs(..., exist_ok=True)` するだけ（コミット済みのファイルには触れない）。

- [ ] **Step 3: 実装**（`recon_logs.py` を Write で次の全文に置き換える。書いた後に `file` で見て、CRLF にしたければ
`python -c "p='docs/superpowers/specs/calibration-data/selfplay/recon_logs.py';b=open(p,'rb').read().replace(b'\r\n',b'\n').replace(b'\n',b'\r\n');open(p,'wb').write(b)"`）

```python
"""KaTrain の対局ログ（監視対局）から実戦を SGF に戻し、AI の手番を分類する。

    python recon_logs.py                                 # 13路 18局 → recon/（2026-09-18 の元の実行）
    python recon_logs.py --size 9 --log-dir <DIR> --out <DIR>/recon_9   # 9路（ログは experiments/selfplay/realgames-9x9-logs/）
    python recon_logs.py --size 9 --strategy Veil9Strategy --log-dir <DIR> --out <DIR>/recon_9_veil   # 一致率ひかえめ9路

出力: <out>/<game>.sgf・<out>/summary.json（局ごとの AI の色・手数・置き石・局の設定・直したこと・AI の手番の分類）と表。
9路では EXCLUDE_9 の局を除き、ログに出ない相手のパスを挟み、root の置き石になった相手の初手を初手に戻す（spec
2026-09-23-veil-strategy-design.md §16.2 手順1）。9路の既定は難解＋の局だけ（DEFAULT_STRATEGY）で、同じ置き場に退避した
一致率ひかえめ9路の局は --strategy Veil9Strategy で別の出力先（recon_9_veil/）に出す（recon_9 の投了の手数と校正の目標に混ぜない）。**コミット済みの recon/ を上書きしない**よう、13路を走らせ直すなら --out を別にする。
"""

import argparse
import ast
import glob
import json
import os
import re

HERE = os.path.dirname(os.path.abspath(__file__))
LOGDIR = os.path.expanduser("~/.katrain/logs")

GAMES_13 = [
    "game_20260918_004112",
    "game_20260918_002440",
    "game_20260917_233314",
    "game_20260917_232627",
    "game_20260917_225754",
    "game_20260916_225351",
    "game_20260916_221750",
    "game_20260915_010117",
    "game_20260915_004532",
    "game_20260915_003547",
    "game_20260915_003117",
    "game_20260915_001944",
    "game_20260914_221230",
    "game_20260914_213601",
    "game_20260914_212801",
    "game_20260914_211800",
    "game_20260917_214325",
    "game_20260917_215328",
]
# 9路で除く局（ID で書く・spec §16.2 手順1）
EXCLUDE_9 = {"game_20260919_224212": "持碁9路（初手だけ難解＋）"}
# 盤サイズ → 既定で復元する局の戦略クラス（ログの最初の Generating move using の戦略。無い盤は絞らない）
DEFAULT_STRATEGY = {9: "Enigma9PlusStrategy"}
COLS = "ABCDEFGHJKLMNOPQRST"


def gtp_to_sgf(gtp, size=13):
    if gtp.lower() == "pass":
        return ""
    col = COLS.index(gtp[0].upper())
    row = int(gtp[1:])
    return chr(ord("a") + col) + chr(ord("a") + (size - row))


RE_OPP = re.compile(r"^board_watch: 相手の着手 ([A-T][0-9]+|pass) を反映しました")
RE_GEN = re.compile(r"^Generating move using (\w+) \(mode ([^)]+)\)")
RE_DONE = re.compile(r"^Move generation complete: ([A-T][0-9]+|pass) -- (.*)$")
RE_SETTINGS = re.compile(r"^Initializing (\w+) with settings: (\{.*\})\s*$")
RE_SCORE = re.compile(
    r"\] Score ([A-T][0-9]+|pass): vloss=(-?[0-9.]+) \(raw (-?[0-9.]+)\) wr=([0-9.]+)% E=(-?[0-9.]+) .*?own_hp=([0-9.]+)"
)
RE_DEV_E = re.compile(r"\] (Deviate|Gamble): played ([A-T][0-9]+|pass) \((.*?)\) instead of ([A-T][0-9]+|pass)")
RE_MIMIC_SCORE = re.compile(r"\] Score ([A-T][0-9]+|pass): (.*)$")
RE_LEAD = re.compile(r"(?:Spend|Endgame budget|Budget): lead=(-?[0-9.]+)")
RE_KV = re.compile(r"([A-Za-z_]+)=(-?[0-9.]+)")
RE_INIT = re.compile(r'"initialStones": (\[\[.*?\]\]|\[\]), .*?"initialPlayer": "([BW])"')
RE_STONE = re.compile(r'\["([BW])", "([A-T][0-9]+)"\]')
RE_KOMI = re.compile(r'"komi": (-?[0-9.]+)')
OPP_PASSED = "opponent passed"  # 相手のパスの後の AI の手番の理由文（Enigma9Plus: opponent passed but the board is not settled ...）


def insert_opponent_passes(events):
    """ログの「相手の着手」の行に出ない相手のパスを挟む（監視を止めて再開した間のパス）。

    AI の手番の理由文に「opponent passed」があり、その直前が AI の手（またはまだ何も無い）なら、そこに相手のパスを足す。
    -> (直した events, 足した位置〈1 始まりの手数〉のリスト)
    """
    out, inserted = [], []
    for ev in events:
        who, _mv, info = ev
        if who == "ai" and OPP_PASSED in (info or {}).get("reason", "") and (not out or out[-1][0] == "ai"):
            out.append(("opp", "pass", None))
            inserted.append(len(out))
        out.append(ev)
    return out, inserted


def setup_as_first_move(init_stones, first_player):
    """root の置き石が「相手の初手」1子だけ（色が initialPlayer の反対）なら、その手を初手に戻す。

    監視対局で相手の初手の後に局面を取り込むと、その石は root の置き石になる（board_watch の「2子以内なので石数から確定」）。
    -> (置き石, 最初の手番, 初手に戻した手 (色, 座標) か None)
    """
    if len(init_stones) == 1 and init_stones[0][0] != first_player:
        return [], init_stones[0][0], init_stones[0]
    return init_stones, first_player, None


def parse(game, log_dir, size):
    path = os.path.join(log_dir, game + ".log")
    lines = open(path, encoding="utf-8", errors="replace").read().splitlines()
    events = []  # (who, move, info)
    turns = []
    cur = None
    strategy = None
    init_stones = []
    first_player = "B"
    komi = None
    init_seen = False
    settings = {}  # 戦略クラス → 最初の Initializing の設定
    last_settings = {}  # 戦略クラス → 最後の Initializing の設定
    settings_changed = []  # 局の途中で設定が変わった戦略クラス
    for ln in lines:
        if not init_seen and ln.startswith("Sending query"):
            m = RE_INIT.search(ln)
            if m:
                init_stones = RE_STONE.findall(m.group(1))
                first_player = m.group(2)
                mk = RE_KOMI.search(ln)
                komi = float(mk.group(1)) if mk else None
                init_seen = True
        m = RE_SETTINGS.match(ln)
        if m:
            cfg = ast.literal_eval(m.group(2))
            if m.group(1) not in settings:
                settings[m.group(1)] = cfg
            elif cfg != settings[m.group(1)] and m.group(1) not in settings_changed:
                settings_changed.append(m.group(1))
            last_settings[m.group(1)] = cfg
            continue
        m = RE_OPP.match(ln)
        if m:
            events.append(("opp", m.group(1), None))
            continue
        m = RE_GEN.match(ln)
        if m:
            strategy = strategy or m.group(1)
            cur = {"strategy": m.group(1), "lines": []}
            continue
        if cur is not None:
            if "QUERY" in ln or ln.startswith("Sending query"):
                continue
            cur["lines"].append(ln)
            m = RE_DONE.match(ln)
            if m:
                cur["move"] = m.group(1)
                cur["reason"] = m.group(2)
                turns.append(cur)
                events.append(("ai", m.group(1), cur))
                cur = None
    for t in turns:
        txt = "\n".join(t["lines"])
        r = t["reason"]
        t["kind"] = "?"
        t["best"] = None
        t["vloss"] = None
        t["E"] = None
        t["best_hp"] = None
        t["own_hp"] = None
        t["lead"] = None
        m = RE_LEAD.search(txt)
        if m:
            t["lead"] = float(m.group(1))
        scores = {}
        first_score = None
        for line in t["lines"]:
            ms = RE_SCORE.search(line)
            if ms:
                scores[ms.group(1)] = dict(
                    vloss=float(ms.group(2)), wr=float(ms.group(4)), E=float(ms.group(5)), own_hp=float(ms.group(6))
                )
                if first_score is None:
                    first_score = ms.group(1)
            else:
                mm = RE_MIMIC_SCORE.search(line)
                if mm and "price" in mm.group(2):
                    kv = dict((k, float(v)) for k, v in RE_KV.findall(mm.group(2)))
                    scores[mm.group(1)] = dict(
                        vloss=kv.get("vloss"),
                        E=kv.get("E"),
                        own_hp=kv.get("hp"),
                        price=kv.get("price"),
                        dE=kv.get("dE"),
                    )
                    if first_score is None:
                        first_score = mm.group(1)
        md = RE_DEV_E.search(txt)
        if "9d" in r and "[" in r:
            t["kind"] = "handoff"
        elif md:
            t["kind"] = "deviate" if md.group(1) == "Deviate" else "gamble"
            t["best"] = md.group(4)
            kv = dict((k, float(v)) for k, v in RE_KV.findall(md.group(3)))
            t["vloss"] = kv.get("vloss")
            t["E"] = kv.get("E", kv.get("dE"))
            t["dE"] = kv.get("dE")
            t["price"] = kv.get("price")
            t["own_hp"] = kv.get("hp", scores.get(md.group(2), {}).get("own_hp"))
            t["best_hp"] = scores.get(md.group(4), {}).get("own_hp")
            mk = re.search(r"kind=(\w+)", md.group(3))
            t["dev_kind"] = mk.group(1) if mk else None
        elif "obvious human move" in r or "Dominant:" in txt:
            t["kind"] = "dominant"
            mh = re.search(r"Dominant: best ([A-T][0-9]+|pass) hp=([0-9.]+)", txt)
            if mh:
                t["best_hp"] = float(mh.group(2))
        elif "playing best move" in r or "playing it" in r or "no natural or trap" in r:
            t["kind"] = "best"
            t["best_hp"] = scores.get(first_score, {}).get("own_hp") if first_score else None
            if "no admissible" in r or "no natural" in r:
                t["kind"] = "best_noadm"
        else:
            t["kind"] = "?:" + r[:60]
        t["best_move_logged"] = first_score
    if not events:
        return None
    other = {"B": "W", "W": "B"}
    inserted = []
    setup_move = None
    if size == 9:  # spec §16.2 手順1: 相手の初手が root の置き石の局・監視の外の相手のパス
        init_stones, first_player, setup_move = setup_as_first_move(init_stones, first_player)
        if setup_move is not None:
            events.insert(0, ("opp", setup_move[1], None))
        events, inserted = insert_opponent_passes(events)
    ai = first_player if events[0][0] == "ai" else other[first_player]
    moves = []
    prev = None
    problems = []
    for who, mv, info in events:
        color = ai if who == "ai" else other[ai]
        if prev == color:
            problems.append(f"same color twice at move {len(moves)+1} ({who} {mv})")
        prev = color
        moves.append((color, mv))
    if moves and moves[0][0] != first_player:
        problems.append(f"first mover {moves[0][0]} != initialPlayer {first_player}")
    sgf = "(;GM[1]FF[4]SZ[%d]KM[%s]RU[chinese]PB[%s]PW[%s]C[recon from %s]" % (
        size,
        "7" if komi is None or komi == 7.0 else "%g" % komi,
        ("AI:" + strategy) if ai == "B" else "app",
        ("AI:" + strategy) if ai == "W" else "app",
        game,
    )
    for col in "BW":
        pts = [gtp_to_sgf(mv, size) for c, mv in init_stones if c == col]
        if pts:
            sgf += "A%s%s" % (col, "".join("[%s]" % p for p in pts))
    sgf += "PL[%s]" % first_player
    for color, mv in moves:
        sgf += ";%s[%s]" % (color, gtp_to_sgf(mv, size))
    sgf += ")"
    return sgf, {
        "game": game,
        "strategy": strategy,
        "ai": ai,
        "board_size": size,
        "komi": komi,
        "n_moves": len(moves),
        "n_ai_turns": len(turns),
        "init_stones": init_stones,
        "first_player": first_player,
        "setup_as_first_move": list(setup_move) if setup_move else None,
        "inserted_opponent_passes": inserted,
        "settings": settings.get(strategy),
        "settings_last": last_settings.get(strategy),
        "settings_changed": settings_changed,
        "problems": problems,
        "turns": [{k: v for k, v in t.items() if k != "lines"} for t in turns],
    }


def default_games(args):
    if args.games:
        return args.games
    if args.size == 13:
        return GAMES_13
    names = sorted(os.path.basename(p)[:-4] for p in glob.glob(os.path.join(args.log_dir, "game_*.log")))
    return [g for g in names if g not in EXCLUDE_9]


def main(argv=None):
    ap = argparse.ArgumentParser(description="KaTrain の監視対局のログ → SGF と AI の手番の分類")
    ap.add_argument(
        "games",
        nargs="*",
        help="ログの名前（game_YYYYMMDD_HHMMSS。既定: 13路は GAMES_13・9路は log-dir の全部 − EXCLUDE_9）",
    )
    ap.add_argument("--log-dir", default=LOGDIR, help="ログのディレクトリ（既定 ~/.katrain/logs）")
    ap.add_argument("--out", default=os.path.join(HERE, "recon"), help="出力先（既定: recon/＝コミット済みの 13路）")
    ap.add_argument("--size", type=int, default=13, choices=(9, 13, 19), help="盤サイズ")
    ap.add_argument(
        "--strategy",
        default=None,
        help="この戦略クラスの局だけ（既定: 9路は Enigma9PlusStrategy・13路は絞らない。一致率ひかえめ9路は Veil9Strategy）",
    )
    args = ap.parse_args(argv)
    only = args.strategy or DEFAULT_STRATEGY.get(args.size)
    os.makedirs(args.out, exist_ok=True)
    all_summ = []
    print(
        "game                 strat        ai init moves turns dev gamble dominant best noadm handoff ? | sum_vloss(dev) dev_hp<.05 dev_bestHP>=.8 | last lead"
    )
    for g in default_games(args):
        parsed = parse(g, args.log_dir, args.size)
        if not parsed:
            print(g, "no events")
            continue
        sgf, s = parsed
        if g in EXCLUDE_9 and args.size == 9:
            print(g, "excluded:", EXCLUDE_9[g])
            continue
        if only and s["strategy"] != only:
            print(g, "skipped: strategy", s["strategy"], "is not", only)
            continue
        with open(os.path.join(args.out, g + ".sgf"), "w", encoding="utf-8") as f:
            f.write(sgf)
        all_summ.append(s)
        T = s["turns"]
        kinds = [t["kind"] for t in T]
        c = lambda k: sum(1 for x in kinds if x == k)  # noqa: E731
        devs = [t for t in T if t["kind"] in ("deviate", "gamble")]
        sum_vloss = sum(max(0.0, t["vloss"] or 0.0) for t in devs)
        unnat = sum(1 for t in devs if t["own_hp"] is not None and t["own_hp"] < 0.05)
        domdev = sum(1 for t in devs if t["best_hp"] is not None and t["best_hp"] >= 0.8)
        unk = sum(1 for x in kinds if x.startswith("?"))
        leads = [t["lead"] for t in T if t["lead"] is not None]
        print(
            "%-20s %-12s %s %4s %5d %5d %3d %6d %8d %4d %5d %7d %d | %6.1f %6d %8d | %s"
            % (
                g,
                s["strategy"][:12],
                s["ai"],
                "".join(c + m for c, m in s["init_stones"]) or "-",
                s["n_moves"],
                len(T),
                c("deviate"),
                c("gamble"),
                c("dominant"),
                c("best"),
                c("best_noadm"),
                c("handoff"),
                unk,
                sum_vloss,
                unnat,
                domdev,
                ("%.1f" % leads[-1]) if leads else "-",
            )
        )
        if s["setup_as_first_move"]:
            print("   setup stone -> first move:", s["setup_as_first_move"])
        if s["inserted_opponent_passes"]:
            print("   inserted opponent pass at move(s):", s["inserted_opponent_passes"])
        if s["settings_changed"]:
            print("   settings changed during the game:", s["settings_changed"])
        if s["problems"]:
            print("   !!", s["problems"][:3])
        for x in sorted(set(k for k in kinds if k.startswith("?"))):
            print("   ?", x)
    with open(os.path.join(args.out, "summary.json"), "w", encoding="utf-8") as f:
        json.dump(all_summ, f, ensure_ascii=False, indent=1)


if __name__ == "__main__":
    main()
```

- [ ] **Step 4: 通ることと、実物の 9路のログで直しが効くことを確かめる**（出力はリポジトリの外＝scratchpad）

Run: `python -m pytest tests/test_selfplay_recon.py -q -p no:cacheprovider` と
`python -m black -l 120 --check docs/superpowers/specs/calibration-data/selfplay/recon_logs.py tests/test_selfplay_recon.py`
Expected: 6 passed・black は変更なし。

```bash
python docs/superpowers/specs/calibration-data/selfplay/recon_logs.py --size 9 --log-dir "C:/Users/iwaki/Documents/katrain-1.17.1.1/katrain-1.17.1.1/experiments/selfplay/realgames-9x9-logs" --out "<scratchpad>/recon9_check" | grep -E "^game_|setup|inserted|changed|!!"
```
Expected（2026-09-26 に scratchpad の写しで確かめた出力）: `game_` で始まる行が 16（`game_20260919_224212` は無い）・
`setup stone -> first move:` が 4 行（213546 `['B', 'G7']`・215145 `['B', 'E6']`・223517 `['B', 'E6']`・20260922_201809 `['B', 'E5']`）・
`inserted opponent pass at move(s): [28]` が 1 行（223015）・`settings changed during the game:` が 3 行（100100・214631・222543）・`!!` の行は 0・
`skipped: strategy` の行は 0（17 本とも最初の `Generating move using` は `Enigma9PlusStrategy`。持碁9路の局も最初の1手番は難解＋＝`EXCLUDE_9` で除く）。
`python -c "import json,statistics;S=json.load(open(r'<scratchpad>/recon9_check/summary.json',encoding='utf-8'));print(len(S),statistics.median(s['n_moves'] for s in S))"` が `16 48.0`。
（この出力は確かめのためだけ。リポジトリの `recon_9/` は C2 で同じコマンドを `--out docs/superpowers/specs/calibration-data/selfplay/recon_9` で走らせて作る。）

- [ ] **Step 5: コミット**

```bash
git add docs/superpowers/specs/calibration-data/selfplay/recon_logs.py tests/test_selfplay_recon.py
git commit -m "feat(selfplay): 実戦ログの復元を 9路に対応させる（--log-dir / --out / --size / --strategy・除く局・相手のパス・置き石の初手・局の設定）" -m "spec §16.2 手順1。9路の既定は難解＋の局だけ（一致率ひかえめ9路は --strategy Veil9Strategy で recon_9_veil へ）。13路の既定の出力先と局は今のまま。" -m "Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>"
```

---

## Task 8: 事後解析と校正目標のスクリプトを盤サイズ別にする（TDD・パッチ）

状態: 完了（`71c4839c`）。

**Files:**
- Modify: `docs/superpowers/specs/calibration-data/selfplay/offline_report.py`（black 未整形 CRLF＝パッチ）
- Modify: `docs/superpowers/specs/calibration-data/selfplay/calib_targets.py`（black 未整形 CRLF＝パッチ）
- Test: `tests/test_selfplay_recon.py`（Edit で2クラス足す）

**Interfaces:**
- Consumes: Task 1 の `S.calib_bin_moves`。Task 7 の `recon_9/summary.json` の `settings`。
- Produces（C2・C3 が使う）:
  - `offline_report.py [--dir DIR] [--config CONFIG] [GAME ...]`（既定 `--dir recon`）・`summarize(game, ai, rows, size=13)` のキーは
    `mine_pre<N>` / `mine_post<N>` / `opp_pre<N>` / `opp_post<N>`（N = `calib_bin_moves(size)[1]`＝13路 85・9路 41）＋`board_size`、
    main が `settings`（recon の summary.json から）を足す。`analyze_game` は `(rows, secs, size)` を返す。
  - `calib_targets.py [DIR] [--size N] [--where k=v,...] [--ai-where k=v,...]`: 区間の境目は盤別、既存の出力の後に
    `games <n> / ai_mean over <m> (<ai-where>)`・`CALIB_TARGET_GAMES <m>/<n>`・`CALIB_TARGETS {json}`（`CALIB_TARGETS_<size>` と同じ形）の3行。
    13路の既存の 32 行は1バイトも変えない。

- [ ] **Step 1: 失敗するテストを書く**（`tests/test_selfplay_recon.py` の末尾に Edit で足す）

```python


class TestOfflineReport:
    def test_bins_follow_the_board_size(self):
        off = _load("offline_report")
        rows = [
            {"depth": d, "player": "B" if d % 2 else "W", "ptloss": 0.5, "match": d % 3 == 0, "score": 1.0}
            for d in range(1, 60)
        ]
        s13 = off.summarize("g", "B", rows, 13)
        assert [k for k in s13 if "85" in k] == ["mine_pre85", "mine_post85", "opp_pre85", "opp_post85"]
        assert s13["board_size"] == 13 and s13["mine_post85"] is None
        s9 = off.summarize("g", "B", rows, 9)
        assert [k for k in s9 if "41" in k] == ["mine_pre41", "mine_post41", "opp_pre41", "opp_post41"]
        assert s9["board_size"] == 9 and s9["mine_post41"]["n"] == 10


def _calib_targets(*args):
    r = subprocess.run(
        [sys.executable, os.path.join(DATA, "calib_targets.py"), *args],
        capture_output=True,
        text=True,
        encoding="utf-8",
        check=True,
    )
    return r.stdout.splitlines()


class TestCalibTargets:
    def test_13x13_output_is_unchanged(self):
        """recon/（13路 18局）の既存の出力 32 行は変えない（2026-09-26 の出力の sha256）。"""
        lines = _calib_targets()
        digest = hashlib.sha256("\n".join(lines[:32]).encode("utf-8")).hexdigest()
        assert digest == "1d8e92e93c8d56cad8b3e01d728dcaac2074fdd3254a02bc6795daec3b089cc0"
        assert lines[32:34] == ["games 18 / ai_mean over 18 (all)", "CALIB_TARGET_GAMES 18/18"]

    def test_9x9_bins_filters_and_the_targets_line(self, tmp_path):
        for i, (loss_ok, cfg) in enumerate(((True, 1.6), (False, 1.8))):
            rows = [
                {
                    "depth": d,
                    "player": "B" if d % 2 else "W",
                    "ptloss": 3.0 if d == 42 else 0.2,
                    "match": d < 12,
                    "score": 1.0,
                    "top_prior": 0.5,
                }
                for d in range(1, 49)
            ]
            summary = {
                "game": f"g{i}",
                "ai": "B",
                "n_moves": 48,
                "final_score": 5.0,
                "secs": 1.0,
                "strategy": "Enigma9PlusStrategy",
                "board_size": 9,
                "mine": {"n": 24, "match": 0.5 if loss_ok else 0.25, "mean_loss": 0.2},
                "opp": {"n": 24, "match": 0.2 + 0.1 * i, "mean_loss": 0.4},
                "settings": {"enigma9plus_max_loss": cfg},
            }
            (tmp_path / f"report_game_{i}.json").write_text(
                json.dumps({"summary": summary, "rows": rows}), encoding="utf-8"
            )
        lines = _calib_targets(str(tmp_path), "--size", "9", "--ai-where", "enigma9plus_max_loss=1.6")
        assert any(line.startswith("opp opening<12 ") for line in lines)
        assert any(line.startswith("opp middle12-40 ") for line in lines)
        assert any(line.startswith("opp endgame>=41 ") for line in lines)
        assert "CALIB_TARGET_GAMES 1/2" in lines
        targets = json.loads(next(line for line in lines if line.startswith("CALIB_TARGETS "))[len("CALIB_TARGETS ") :])
        assert targets["ai_mean"] == 0.5 and targets["opp_mean"] == 0.25 and targets["moves_median"] == 48
        assert targets["bins"]["cal_opening"]["opp_top1"] == 1.0 and targets["bins"]["cal_endgame"]["opp_top1"] == 0.0
        assert targets["opp_ge2"] == round(2 / 48, 2)
```

（sha256 は `76105598` の `calib_targets.py` を `recon/` に走らせた出力 32 行を `"\n"` でつないだもの＝パッチの前後で変わらないことを固定する。）

- [ ] **Step 2: 失敗を確かめる**

Run: `python -m pytest tests/test_selfplay_recon.py -q -p no:cacheprovider`
Expected: `TestOfflineReport` が `TypeError: summarize() takes 3 positional arguments but 4 were given`、`test_13x13_output_is_unchanged` が
AssertionError（`lines[32:34] == []`）、`test_9x9_bins_filters_and_the_targets_line` が AssertionError（`opp opening<12 ` で始まる行が無い。
今の `calib_targets.py` は argparse を持たず `sys.argv[1]` だけを読む＝`--size 9 ...` は無視して終了コード 0 で走り、13路の区間の `opp opening<24` を出す）。

- [ ] **Step 3: 実装**（パッチスクリプト `<scratchpad>/patch_9x9_t8.py` を書いて worktree のルートで実行する）

```python
"""Plan B Task 8: offline_report.py と calib_targets.py を盤サイズ別にする（CRLF・black 未整形のまま）。"""

import sys

sys.path.insert(0, r"<scratchpad の実パス>")
from crlf_patch import append, patch  # noqa: E402

OFF = "docs/superpowers/specs/calibration-data/selfplay/offline_report.py"
CT = "docs/superpowers/specs/calibration-data/selfplay/calib_targets.py"

patch(OFF, [
    (
        'Throwaway spike script (2026-09-18). Writes recon/report_<game>.json and prints a table.\n"""\n',
        'Throwaway spike script (2026-09-18). Writes <dir>/report_<game>.json and prints a table.\n'
        '\n'
        '    python offline_report.py [--dir DIR] [--config CONFIG] [GAME ...]   # 既定 --dir recon（13路）。KataGo を起動する\n'
        '\n'
        '区間の境目は盤サイズ別（katrain_debug.selfplay_stats.calib_bin_moves: 13路 85・9路 41 手）で、summary のキーは\n'
        'mine_pre<N> / mine_post<N> / opp_pre<N> / opp_post<N>（13路は pre85 / post85 のまま）。summary には board_size と\n'
        'recon の summary.json の局の設定（settings）も入る。**--dir の report_*.json を上書きする**（コミット済みの recon/ に注意）。\n'
        '"""\n',
        1,
    ),
    ("import glob\nimport json\n", "import argparse\nimport glob\nimport json\n", 1),
    (
        "from katrain_debug.runner import DebugGame  # noqa: E402\n",
        "from katrain_debug.runner import DebugGame  # noqa: E402\n"
        "from katrain_debug.selfplay_stats import calib_bin_moves  # noqa: E402\n",
        1,
    ),
    ("    return rows, time.time() - t0\n", "    return rows, time.time() - t0, root.board_size[0]\n", 1),
    ("def summarize(game, ai, rows):\n", "def summarize(game, ai, rows, size=13):\n", 1),
    (
        '    opp = "W" if ai == "B" else "B"\n'
        "    return dict(\n"
        "        game=game,\n"
        "        ai=ai,\n"
        "        n_moves=len(rows),\n"
        '        final_score=rows[-1]["score"] if rows else None,\n'
        '        mine=stats(lambda r: r["player"] == ai),\n'
        '        opp=stats(lambda r: r["player"] == opp),\n'
        '        mine_pre85=stats(lambda r: r["player"] == ai and r["depth"] < 85),\n'
        '        mine_post85=stats(lambda r: r["player"] == ai and r["depth"] >= 85),\n'
        '        opp_pre85=stats(lambda r: r["player"] == opp and r["depth"] < 85),\n'
        '        opp_post85=stats(lambda r: r["player"] == opp and r["depth"] >= 85),\n'
        "    )\n",
        '    opp = "W" if ai == "B" else "B"\n'
        "    end = calib_bin_moves(size)[1]  # 終盤の区間の始まり（13路 85・9路 41）\n"
        "    return {\n"
        '        "game": game,\n'
        '        "ai": ai,\n'
        '        "n_moves": len(rows),\n'
        '        "final_score": rows[-1]["score"] if rows else None,\n'
        '        "mine": stats(lambda r: r["player"] == ai),\n'
        '        "opp": stats(lambda r: r["player"] == opp),\n'
        '        f"mine_pre{end}": stats(lambda r: r["player"] == ai and r["depth"] < end),\n'
        '        f"mine_post{end}": stats(lambda r: r["player"] == ai and r["depth"] >= end),\n'
        '        f"opp_pre{end}": stats(lambda r: r["player"] == opp and r["depth"] < end),\n'
        '        f"opp_post{end}": stats(lambda r: r["player"] == opp and r["depth"] >= end),\n'
        '        "board_size": size,\n'
        "    }\n",
        1,
    ),
    (
        "def main():\n"
        '    summ = json.load(open(os.path.join(RECON, "summary.json"), encoding="utf-8"))\n'
        '    ai_of = {s["game"]: s["ai"] for s in summ}\n'
        '    strat_of = {s["game"]: s["strategy"] for s in summ}\n'
        '    games = sys.argv[1:] or [s["game"] for s in summ]\n'
        '    stub = KaTrainStub(os.path.expanduser("~/.katrain/config.json"), debug_level=0, quiet=True)\n',
        "def main():\n"
        '    ap = argparse.ArgumentParser(description="recon の SGF を KataGo で事後解析して report_<game>.json を書く")\n'
        '    ap.add_argument("games", nargs="*", help="局の名前（既定: <dir>/summary.json の全局）")\n'
        '    ap.add_argument("--dir", default=RECON, help="recon のディレクトリ（既定 recon＝13路。9路は recon_9）")\n'
        "    ap.add_argument(\n"
        '        "--config", default=os.path.expanduser("~/.katrain/config.json"), help="エンジン設定（max_visits 2500）を読む config"\n'
        "    )\n"
        "    args = ap.parse_args()\n"
        '    summ = json.load(open(os.path.join(args.dir, "summary.json"), encoding="utf-8"))\n'
        '    ai_of = {s["game"]: s["ai"] for s in summ}\n'
        '    strat_of = {s["game"]: s["strategy"] for s in summ}\n'
        '    settings_of = {s["game"]: s.get("settings") for s in summ}\n'
        '    games = args.games or [s["game"] for s in summ]\n'
        "    stub = KaTrainStub(args.config, debug_level=0, quiet=True)\n",
        1,
    ),
    (
        '            sgf = os.path.join(RECON, g + ".sgf")\n'
        "            rows, secs = analyze_game(stub, engine, sgf)\n"
        "            s = summarize(g, ai_of[g], rows)\n"
        '            s["strategy"] = strat_of[g]\n'
        '            s["secs"] = secs\n',
        '            sgf = os.path.join(args.dir, g + ".sgf")\n'
        "            rows, secs, size = analyze_game(stub, engine, sgf)\n"
        "            s = summarize(g, ai_of[g], rows, size)\n"
        '            s["strategy"] = strat_of[g]\n'
        '            s["settings"] = settings_of.get(g)\n'
        '            s["secs"] = secs\n'
        "            end = calib_bin_moves(size)[1]\n",
        1,
    ),
    (
        '                open(os.path.join(RECON, "report_" + g + ".json"), "w", encoding="utf-8"),\n',
        '                open(os.path.join(args.dir, "report_" + g + ".json"), "w", encoding="utf-8"),\n',
        1,
    ),
    (
        "pre85 mine %s opp %s | post85 mine %s opp %s | %.0fs\"\n",
        "pre%d mine %s opp %s | post%d mine %s opp %s | %.0fs\"\n",
        1,
    ),
    (
        '                    o["mean_loss"],\n'
        '                    ("%.0f%%" % (100 * s["mine_pre85"]["match"])) if s["mine_pre85"] else "-",\n'
        '                    ("%.0f%%" % (100 * s["opp_pre85"]["match"])) if s["opp_pre85"] else "-",\n'
        "                    (\n"
        '                        ("%.0f%%/n%d" % (100 * s["mine_post85"]["match"], s["mine_post85"]["n"]))\n'
        '                        if s["mine_post85"]\n'
        '                        else "-"\n'
        "                    ),\n"
        '                    ("%.0f%%" % (100 * s["opp_post85"]["match"])) if s["opp_post85"] else "-",\n',
        '                    o["mean_loss"],\n'
        "                    end,\n"
        '                    ("%.0f%%" % (100 * s[f"mine_pre{end}"]["match"])) if s[f"mine_pre{end}"] else "-",\n'
        '                    ("%.0f%%" % (100 * s[f"opp_pre{end}"]["match"])) if s[f"opp_pre{end}"] else "-",\n'
        "                    end,\n"
        "                    (\n"
        '                        ("%.0f%%/n%d" % (100 * s[f"mine_post{end}"]["match"], s[f"mine_post{end}"]["n"]))\n'
        '                        if s[f"mine_post{end}"]\n'
        '                        else "-"\n'
        "                    ),\n"
        '                    ("%.0f%%" % (100 * s[f"opp_post{end}"]["match"])) if s[f"opp_post{end}"] else "-",\n',
        1,
    ),
    (
        '    json.dump(out, open(os.path.join(RECON, "report_all.json"), "w", encoding="utf-8"), indent=1)\n',
        '    json.dump(out, open(os.path.join(args.dir, "report_all.json"), "w", encoding="utf-8"), indent=1)\n',
        1,
    ),
])

patch(CT, [
    (
        'import glob, json, os, statistics as st, sys\n'
        'D = sys.argv[1] if len(sys.argv) > 1 else os.path.join(os.path.dirname(os.path.abspath(__file__)), "recon")\n'
        'rows_all = []\n'
        'games = []\n',
        'import argparse, glob, json, math, os, statistics as st, sys\n'
        'AP = argparse.ArgumentParser(description="校正目標（相手の一致率・損失・区間の形・AI 側の一致率）を recon の事後解析から出す")\n'
        'AP.add_argument("dir", nargs="?", default=os.path.join(os.path.dirname(os.path.abspath(__file__)), "recon"))\n'
        'AP.add_argument("--size", type=int, default=13, help="盤サイズ（区間の境目 = ceil(24/169・85/169 × 盤面積)＝13路 24/85・9路 12/41）")\n'
        'AP.add_argument("--where", default="", help="局を summary（無ければ summary.settings）の値で絞る（k=v,...）")\n'
        'AP.add_argument("--ai-where", default="", help="AI 側の局平均 ai_mean だけさらに絞る（k=v,...）")\n'
        'ARGS = AP.parse_args()\n'
        'D = ARGS.dir\n'
        'LO, HI = (math.ceil(f * ARGS.size * ARGS.size) for f in (24 / 169, 85 / 169))  # selfplay_stats.calib_bin_moves と同じ\n'
        '\n'
        '\n'
        'def pick(s, where):\n'
        '    """summary s が where（k=v,...）をすべて満たすか。キーは summary、無ければ summary.settings。数は数で比べる。"""\n'
        '    for kv in filter(None, where.split(",")):\n'
        '        k, v = kv.split("=", 1)\n'
        '        have = s[k] if k in s else (s.get("settings") or {}).get(k)\n'
        '        try:\n'
        '            if float(have) != float(v):\n'
        '                return False\n'
        '        except (TypeError, ValueError):\n'
        '            if str(have) != v:\n'
        '                return False\n'
        '    return True\n'
        '\n'
        '\n'
        'rows_all = []\n'
        'games = []\n'
        'summaries = []\n',
        1,
    ),
    (
        '    s = d["summary"]; rows = d["rows"]; ai = s["ai"]\n',
        '    s = d["summary"]; rows = d["rows"]; ai = s["ai"]\n'
        '    if not pick(s, ARGS.where):\n'
        '        continue\n'
        '    summaries.append(s)\n',
        1,
    ),
    (
        '        sum((r["top_prior"] or 0) >= 0.8 for r in rs) / len(rs)))\n',
        '        sum((r["top_prior"] or 0) >= 0.8 for r in rs) / len(rs)))\n'
        '    return dict(n=len(rs), match=sum(r["match"] for r in rs) / len(rs), loss=sum(L) / len(L),\n'
        '                ge2=sum(x >= 2 for x in L) / len(L), ge5=sum(x >= 5 for x in L) / len(L))\n'
        'T = {}\n',
        1,
    ),
    (
        '    agg(f, who + " all")\n'
        '    agg(lambda r, f=f: f(r) and r["depth"] < 24, who + " opening<24")\n'
        '    agg(lambda r, f=f: f(r) and 24 <= r["depth"] < 85, who + " middle24-84")\n'
        '    agg(lambda r, f=f: f(r) and r["depth"] >= 85, who + " endgame>=85")\n',
        '    T[who, "all"] = agg(f, who + " all")\n'
        '    T[who, "cal_opening"] = agg(lambda r, f=f: f(r) and r["depth"] < LO, who + " opening<%d" % LO)\n'
        '    T[who, "cal_middle"] = agg(lambda r, f=f: f(r) and LO <= r["depth"] < HI, who + " middle%d-%d" % (LO, HI - 1))\n'
        '    T[who, "cal_endgame"] = agg(lambda r, f=f: f(r) and r["depth"] >= HI, who + " endgame>=%d" % HI)\n',
        1,
    ),
])
append(
    CT,
    '# 校正目標の形（selfplay_stats.CALIB_TARGETS_<size> にそのまま写す。spec 2026-09-23-veil-strategy-design.md §16.2 手順4）\n'
    'ai_games = [s for s in summaries if pick(s, ARGS.ai_where)]\n'
    'opp_rates = [s["opp"]["match"] for s in summaries]\n'
    'targets = {\n'
    '    "opp_mean": round(st.fmean(opp_rates), 3),\n'
    '    "opp_sd": round(st.pstdev(opp_rates), 3),\n'
    '    "opp_loss": round(st.fmean(s["opp"]["mean_loss"] for s in summaries), 2),\n'
    '    "opp_ge2": round(T["opp", "all"]["ge2"], 2),\n'
    '    "opp_ge5": round(T["opp", "all"]["ge5"], 2),\n'
    '    "ai_mean": round(st.fmean(s["mine"]["match"] for s in ai_games), 3),\n'
    '    "moves_median": st.median(s["n_moves"] for s in summaries),\n'
    '    "bins": {n: {"opp_top1": round(T["opp", n]["match"], 3), "opp_loss": round(T["opp", n]["loss"], 2)}\n'
    '             for n in ("cal_opening", "cal_middle", "cal_endgame")},\n'
    '}\n'
    'print("games %d / ai_mean over %d (%s)" % (len(summaries), len(ai_games), ARGS.ai_where or "all"))\n'
    'print("CALIB_TARGET_GAMES %d/%d" % (len(ai_games), len(summaries)))\n'
    'print("CALIB_TARGETS " + json.dumps(targets))\n',
)
print("patched", OFF, CT)
```

（`sys.path.insert` の `<scratchpad の実パス>` は自分のセッションの scratchpad の絶対パスに置き換える＝例 `C:/Users/.../scratchpad`。）

- [ ] **Step 4: 通ることを確かめる**

Run: `python <scratchpad>/patch_9x9_t8.py` → `python -m pytest tests/test_selfplay_recon.py -q -p no:cacheprovider` → `file docs/superpowers/specs/calibration-data/selfplay/offline_report.py docs/superpowers/specs/calibration-data/selfplay/calib_targets.py`
Expected: `patched ...`・9 passed（Task 7 の 6 件＋この Task の 3 件）・どちらも `with CRLF line terminators`。
13路の確認: `python docs/superpowers/specs/calibration-data/selfplay/calib_targets.py | tail -3` の最後の行が
`CALIB_TARGETS {"opp_mean": 0.222, "opp_sd": 0.068, "opp_loss": 1.77, "opp_ge2": 0.28, "opp_ge5": 0.1, "ai_mean": 0.524, "moves_median": 78.5, "bins": {"cal_opening": {"opp_top1": 0.267, "opp_loss": 0.56}, "cal_middle": {"opp_top1": 0.182, "opp_loss": 2.43}, "cal_endgame": {"opp_top1": 0.291, "opp_loss": 0.96}}}`
（18局。`--where strategy=Enigma13PlusStrategy` なら opp_mean 0.231・opp_sd 0.065・ai_mean 0.533＝`CALIB_TARGETS_13` の局平均の主目標と同じ。
区間の形・損失・手数は 18局の値が定数と同じ）。

- [ ] **Step 5: コミット**

```bash
git add docs/superpowers/specs/calibration-data/selfplay/offline_report.py docs/superpowers/specs/calibration-data/selfplay/calib_targets.py tests/test_selfplay_recon.py
git commit -m "feat(selfplay): 事後解析と校正目標のスクリプトを盤サイズ別の区間・出力先・局の絞り込みに対応させる" -m "spec §16.2 手順2・3・4。offline_report --dir / --config・summary に board_size と settings。calib_targets --size / --where / --ai-where と CALIB_TARGETS の1行。13路の既存の出力は不変。" -m "Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>"
```

---

## Task 9: 調査のスクリプトの置き場（spec §16.2 手順8）と README

状態: 完了（`1dee1727`）。

**Files:**
- Create: `docs/superpowers/specs/calibration-data/selfplay/enigma9_log_stats.py`（元: scratchpad の `parse9.py`・`table9.py`・`e9parse2.py`・`e9an.py` を1本にまとめた）
- Create: `docs/superpowers/specs/calibration-data/selfplay/opp_bot_match9.py`（相手 BOT の一致。元のスクリプトは無い＝§16.1 の値を出した集計を書き直した）
- Copy: `docs/superpowers/specs/calibration-data/selfplay/book9/book9.py`・`book9/book9deep.py`（元: scratchpad の同名。中身は変えない）
- Modify: `docs/superpowers/specs/calibration-data/selfplay/README.md`（パッチ）

**Interfaces:**
- Consumes: 退避した実戦ログ `C:/Users/iwaki/Documents/katrain-1.17.1.1/katrain-1.17.1.1/experiments/selfplay/realgames-9x9-logs/`（17 本・gitignore）・
  `docs/superpowers/specs/calibration-data/enigma9/ponder_pick_probe.json`（コミット済み）。
- Produces: §16.1 の数字を再現する2本のスクリプト（KataGo 不要）と、本の読み取りのコード（web の本のデータはコミットしない）。

- [ ] **Step 1: `enigma9_log_stats.py` を Write で作る**

```python
"""難解＋9路の実戦ログの集計（spec 2026-09-23-veil-strategy-design.md §16.1。ログを読むだけ・KataGo 不要）。

    python enigma9_log_stats.py [--log-dir DIR]   # 既定: <リポジトリ>/experiments/selfplay/realgames-9x9-logs

1. 自分の一致率（ログの `Move generation complete` の理由文が最善手の手を一致とした数。2500v の事後解析ではない）:
   局ごと・全体・AI の 1〜5 手番目・16 手番目以降。持碁9路の局（EXCLUDE）は数えない。
2. 勝率勾配: `Score` 行（子局面 500 visits の検証値）の最善手（vloss 0）の勝率が 30〜70% の局面で、
   (最善手の勝率 − 候補の勝率) / vloss の中央値（vloss 0.2〜2.0 と 0.5〜2.0）。
3. 安い代わりの手: 検証した候補（最善手以外）の最小 vloss が閾値以下の手番の割合（打つ手の手数の帯ごと）。
2・3 は `Score` 行のある難解＋の手番すべて（持碁9路の局の初手の1手番も含む＝§16.1 の「17 局」）。
最初の `Generating move using` が難解＋9路でないログ（同じ置き場に退避した一致率ひかえめ9路の局など）は 1〜3 のどれにも数えない。
2026-09-26 の値: 一致率 65.7%（261/397）・1〜5 手番目 43.8%・16 手番目以降 75.9%、勾配 26.0%/目（n 236）・24.0%/目
（0.5〜2.0・n 125）、0.3目以内の代わりの手 1〜2手 94%・3〜6手 65%・7〜12手 27%・13〜20手 24%。
"""

import argparse
import glob
import json
import os
import re
import statistics
from collections import Counter

REPO = os.path.abspath(os.path.join(os.path.dirname(os.path.abspath(__file__)), "..", "..", "..", "..", ".."))
LOG_DIR = os.path.join(REPO, "experiments", "selfplay", "realgames-9x9-logs")
EXCLUDE = {"game_20260919_224212"}  # 持碁9路（初手だけ難解＋）＝recon_logs.EXCLUDE_9 と同じ

RE_DONE = re.compile(r"^Move generation complete: (\S+) -- (.*)$")
RE_GEN = re.compile(r"^Generating move using Enigma9PlusStrategy")
RE_FIRST = re.compile(r"^Generating move using (\w+)")
RE_SCORE = re.compile(
    r"\[Enigma9PlusStrategy\] Score (\S+): vloss=([-\d.]+) \(raw ([-\d.]+)\) wr=([\d.]+)% E=([-\d.]+).*?own_hp=([\d.]+)"
)
RE_PROBE = re.compile(r'"analyzeTurns": \[(\d+)\], "maxVisits": 500')
TURN_BANDS = ((1, 2), (3, 6), (7, 12), (13, 20), (21, 999))
CHEAP = (0.1, 0.2, 0.3, 0.5, 1.0)


def turn_kind(reason):
    """AI の手番の理由文 → best（最善手を打った）/ dev / jigo / other（罠・終局の手など）。"""
    if "playing best move" in reason or "most confusing in budget" in reason or "best move is pass" in reason:
        return "best"
    if "deviated to" in reason:
        return "dev"
    if reason.startswith("Jigo"):
        return "jigo"
    return "other"


def first_strategy(path):
    """ログの最初の `Generating move using` の戦略クラス（無ければ None）。"""
    with open(path, encoding="utf-8", errors="replace") as f:
        for line in f:
            m = RE_FIRST.match(line)
            if m:
                return m.group(1)
    return None


def own_turns(path):
    """1局の AI の手番の種類（時系列）。"""
    kinds = []
    with open(path, encoding="utf-8", errors="replace") as f:
        for line in f:
            m = RE_DONE.match(line)
            if m:
                kinds.append(turn_kind(m.group(2)))
    return kinds


def probed_decisions(path):
    """1局の難解＋の手番のうち Score 行のあるもの: {"turn": 打つ手の手数, "scores": [{mv, vloss, wr}]}（最初が最善手）。"""
    out, cur = [], None
    with open(path, encoding="utf-8", errors="replace") as f:
        for line in f:
            if RE_GEN.match(line):
                if cur and cur["scores"]:
                    out.append(cur)
                cur = {"turn": None, "scores": []}
                continue
            if cur is None:
                continue
            if cur["turn"] is None and RE_PROBE.search(line):
                query = json.loads(line.split(": ", 1)[1])
                cur["turn"] = len(query.get("initialStones", [])) + len(query.get("moves", []))
            m = RE_SCORE.search(line)
            if m:
                cur["scores"].append({"mv": m.group(1), "vloss": float(m.group(2)), "wr": float(m.group(4))})
    if cur and cur["scores"]:
        out.append(cur)
    return out


def match_rates(logs):
    rows, first5, late = [], [0, 0], [0, 0]
    for path in logs:
        name = os.path.basename(path)[:-4]
        if name in EXCLUDE:
            continue
        kinds = [k for k in own_turns(path) if k != "jigo"]
        best = sum(1 for k in kinds if k == "best")
        rows.append((name, best, len(kinds)))
        for i, k in enumerate(kinds, 1):
            bucket = first5 if i <= 5 else late if i >= 16 else None
            if bucket is not None:
                bucket[0] += k == "best"
                bucket[1] += 1
    return rows, first5, late


def gradient(decisions, lo=0.2, hi=2.0):
    values = []
    for d in decisions:
        best = [s for s in d["scores"] if s["vloss"] == 0.0]
        if d["turn"] is None or not best or not 30 <= best[0]["wr"] <= 70:
            continue
        values += [(best[0]["wr"] - s["wr"]) / s["vloss"] for s in d["scores"] if lo <= s["vloss"] <= hi]
    return values


def cheap_alternatives(decisions):
    bands = {band: [] for band in TURN_BANDS}
    for d in decisions:
        if d["turn"] is None:
            continue
        band = next(b for b in TURN_BANDS if b[0] <= d["turn"] <= b[1])
        bands[band].append(min((s["vloss"] for s in d["scores"][1:]), default=None))
    return bands


def main(argv=None):
    ap = argparse.ArgumentParser(description="難解＋9路の実戦ログの集計（spec §16.1）")
    ap.add_argument(
        "--log-dir", default=LOG_DIR, help="ログのディレクトリ（既定: experiments/selfplay/realgames-9x9-logs）"
    )
    args = ap.parse_args(argv)
    logs = sorted(glob.glob(os.path.join(args.log_dir, "game_*.log")))
    logs = [p for p in logs if first_strategy(p) == "Enigma9PlusStrategy"]  # 難解＋9路の局だけ
    rows, first5, late = match_rates(logs)
    print("## own match rate (log reasons; best / AI turns without Jigo)")
    for name, best, n in rows:
        print(f"{name} {best}/{n} {100 * best / n:.1f}%")
    total = sum(b for _, b, _ in rows), sum(n for _, _, n in rows)
    print(f"all {total[0]}/{total[1]} {100 * total[0] / total[1]:.1f}% ({len(rows)} games)")
    print(f"AI turns 1-5 {first5[0]}/{first5[1]} {100 * first5[0] / first5[1]:.1f}%")
    print(f"AI turns 16+ {late[0]}/{late[1]} {100 * late[0] / late[1]:.1f}%")
    decisions = [d for path in logs for d in probed_decisions(path)]
    games = Counter(os.path.basename(p) for p in logs for _ in probed_decisions(p)[:1])
    print(f"\n## probed decisions: {len(decisions)} in {len(games)} games")
    for label, lo in (("vloss 0.2-2.0", 0.2), ("vloss 0.5-2.0", 0.5 + 1e-9)):
        g = gradient(decisions, lo)
        print(f"winrate gradient {label}: n={len(g)} median={statistics.median(g):.1f}%/pt")
    print("\n## cheapest verified alternative (share of turns with an alternative within the loss)")
    for (lo, hi), mins in cheap_alternatives(decisions).items():
        n = len(mins)
        cells = " ".join(f"<={t}:{100 * sum(1 for x in mins if x is not None and x <= t) / n:.0f}%" for t in CHEAP)
        print(f"moves {lo}-{hi if hi < 999 else '':<3} n={n:>3} {cells}")


if __name__ == "__main__":
    main()
```

- [ ] **Step 2: `opp_bot_match9.py` を Write で作る**

```python
"""9路の相手 BOT の一致（spec 2026-09-23-veil-strategy-design.md §16.1。JSON を読むだけ・KataGo 不要）。

    python opp_bot_match9.py [JSON]   # 既定: calibration-data/enigma9/ponder_pick_probe.json（8月の監視対局の 130 局面）

各行はこちらの手の直後（相手の手番）の局面。実際の相手の手（reply）が clean 約 520 visits の最善手（k[0]）と、humanSL 9段の
1位（h_all[0]）と同じだった割合。idx はこちらの手の手数 − 1（idx <= 10 ＝ こちらの手が 11 手目まで）。
2026-09-26 の値: 最善手 23.8%（31/130。11 手目まで 6/25）・humanSL 9段の1位 35.4%（46/130）。段位は不明・こちらが外した後の局面。
"""

import json
import os
import sys

HERE = os.path.dirname(os.path.abspath(__file__))
PROBE = os.path.join(HERE, "..", "enigma9", "ponder_pick_probe.json")


def share(rows, key):
    hits = sum(1 for r in rows if r[key] and r[key][0] == r["reply"])
    return hits, len(rows)


def main(argv=None):
    argv = sys.argv[1:] if argv is None else argv
    with open(argv[0] if argv else PROBE, encoding="utf-8") as f:
        rows = json.load(f)
    early = [r for r in rows if r["idx"] <= 10]
    for label, key in (("clean best (k[0])", "k"), ("humanSL 9d top (h_all[0])", "h_all")):
        hits, n = share(rows, key)
        e_hits, e_n = share(early, key)
        print(f"{label}: {hits}/{n} {100 * hits / n:.1f}%  (our move <= 11: {e_hits}/{e_n})")


if __name__ == "__main__":
    main()
```

- [ ] **Step 3: 本の読み取りのコードをコピーする**（中身は変えない。web から本のページを取るコードだけで、取ったデータはコミットしない）

```bash
mkdir -p docs/superpowers/specs/calibration-data/selfplay/book9
cp "C:/Users/iwaki/AppData/Local/Temp/claude/C--Users-iwaki--katrain/fa4754c0-234e-47cd-8f07-eff2747ac3ec/scratchpad/book9.py" docs/superpowers/specs/calibration-data/selfplay/book9/book9.py
cp "C:/Users/iwaki/AppData/Local/Temp/claude/C--Users-iwaki--katrain/fa4754c0-234e-47cd-8f07-eff2747ac3ec/scratchpad/book9deep.py" docs/superpowers/specs/calibration-data/selfplay/book9/book9deep.py
```
（コピー元が無ければこの Step は飛ばし、README の book9 の行も書かない＝最後の報告で伝える。コピーしたら **走らせない**
〈katagobooks.org へ curl で取りにいく〉。）

- [ ] **Step 4: 2本の集計が §16.1 の値を出すことを確かめる**

```bash
python docs/superpowers/specs/calibration-data/selfplay/enigma9_log_stats.py --log-dir "C:/Users/iwaki/Documents/katrain-1.17.1.1/katrain-1.17.1.1/experiments/selfplay/realgames-9x9-logs" | grep -E "^all|^AI turns|gradient|^moves"
python docs/superpowers/specs/calibration-data/selfplay/opp_bot_match9.py
```
Expected（2026-09-26 に確かめた出力）:
```
all 261/397 65.7% (16 games)
AI turns 1-5 35/80 43.8%
AI turns 16+ 120/158 75.9%
winrate gradient vloss 0.2-2.0: n=236 median=26.0%/pt
winrate gradient vloss 0.5-2.0: n=125 median=24.0%/pt
moves 1-2   n= 17 <=0.1:76% <=0.2:82% <=0.3:94% <=0.5:100% <=1.0:100%
moves 3-6   n= 26 <=0.1:35% <=0.2:54% <=0.3:65% <=0.5:85% <=1.0:96%
moves 7-12  n= 37 <=0.1:22% <=0.2:24% <=0.3:27% <=0.5:38% <=1.0:59%
moves 13-20  n= 49 <=0.1:14% <=0.2:20% <=0.3:24% <=0.5:33% <=1.0:49%
moves 21-    n=108 <=0.1:42% <=0.2:48% <=0.3:52% <=0.5:56% <=1.0:69%
clean best (k[0]): 31/130 23.8%  (our move <= 11: 6/25)
humanSL 9d top (h_all[0]): 46/130 35.4%  (our move <= 11: 8/25)
```
`python -m black -l 120 --check docs/superpowers/specs/calibration-data/selfplay/enigma9_log_stats.py docs/superpowers/specs/calibration-data/selfplay/opp_bot_match9.py` が変更なし。

- [ ] **Step 5: README を直す**（パッチスクリプト `<scratchpad>/patch_9x9_t9.py`）

```python
import sys

sys.path.insert(0, r"<scratchpad の実パス>")
from crlf_patch import patch  # noqa: E402

P = "docs/superpowers/specs/calibration-data/selfplay/README.md"
patch(P, [
    (
        "| `recon_logs.py` | `~/.katrain/logs/game_*.log` → `recon/*.sgf` と `summary.json` |\n",
        "| `recon_logs.py` | `~/.katrain/logs/game_*.log` → `recon/*.sgf` と `summary.json`。`--log-dir`・`--out`・`--size`・`--strategy`。9路は難解＋の局だけ（一致率ひかえめ9路は `--strategy Veil9Strategy` で `recon_9_veil/`）・除く局（`EXCLUDE_9`）・監視の外の相手のパス・root の置き石になった相手の初手を直す（spec 韜晦 §16.2 手順1） |\n",
        1,
    ),
    (
        "| `offline_report.py` | `recon/*.sgf` を KataGo で事後解析して `recon/report_*.json` を書く（KataGo を起動する） |\n",
        "| `offline_report.py` | `recon/*.sgf` を KataGo で事後解析して `recon/report_*.json` を書く（KataGo を起動する）。`--dir`（9路は `recon_9`）・`--config`。区間は盤別（13路 85・9路 41 手）で summary に `board_size` と局の設定 |\n",
        1,
    ),
    (
        "| `calib_targets.py` | 校正目標（相手の一致率・損失・区間 <24 / 24〜84 / >=85 の形）を `recon/` から出す＝spec §3 の表 |\n",
        "| `calib_targets.py` | 校正目標（相手の一致率・損失・区間 <24 / 24〜84 / >=85 の形）を `recon/` から出す＝spec §3 の表。`--size 9`（区間 12 / 41）・`--where` / `--ai-where k=v,...`（局の設定で絞る）。最後の行 `CALIB_TARGETS {...}` は `selfplay_stats.CALIB_TARGETS_<size>` の形 |\n",
        1,
    ),
    (
        "`veil13-p1-default-vs-<アーム>-summary.{json,txt}`）の写し |\n",
        "`veil13-p1-default-vs-<アーム>-summary.{json,txt}`）の写し |\n"
        "| `window_stats.py` | 序盤の窓の指標（spec 韜晦 §16.2 手順7）: 実行ディレクトリの moves.jsonl から窓の中（手数 <= W・9路 12・13路 30）と後の一致率・損失・大きな損・W 手目のリード・flip・窓の後の u と paid・定跡を知る相手の出口・`decision_open` の確かめ・同じ seed の対の差（`--compare A B`）。`--recon DIR ...` は実戦の事後解析（`recon_9/`・`recon_9_veil/`）の自分と相手の窓の中・後の一致率と相手が最善手を続けた手数（spec 韜晦 §16.5 の「実戦」） |\n"
        "| `enigma9_log_stats.py` | 難解＋9路の実戦ログ（`experiments/selfplay/realgames-9x9-logs/`・gitignore）の集計: ログの理由文の自分の一致率・勝率勾配・安い代わりの手（spec 韜晦 §16.1）。最初の手番が難解＋9路でないログは数えない。KataGo 不要 |\n"
        "| `opp_bot_match9.py` | 9路の相手 BOT の手と clean 最善手・humanSL 9段の1位の一致（`../enigma9/ponder_pick_probe.json`・spec 韜晦 §16.1） |\n"
        "| `book9/` | KataGo の 9路の本（katagobooks.org `book9x9tt`）を読んだコード（`book9.py`・`book9deep.py`。curl で web から取る＝テストでは走らせない・取ったデータはコミットしない。`book9deep.py` は `book9/` を cwd にして走らせる） |\n",
        1,
    ),
])
print("patched", P)
```

（`recon_9/`・`opponent_pool_9.json`・`veil9-campaign.md` の行は C2・C4・C9 で足す。）

- [ ] **Step 6: コミット**

```bash
git add docs/superpowers/specs/calibration-data/selfplay/enigma9_log_stats.py docs/superpowers/specs/calibration-data/selfplay/opp_bot_match9.py docs/superpowers/specs/calibration-data/selfplay/book9 docs/superpowers/specs/calibration-data/selfplay/README.md
git commit -m "docs(selfplay): 9路の調査の集計スクリプトを calibration-data/selfplay/ に置く" -m "spec §16.2 手順8。実戦ログの一致率・勝率勾配・安い代わりの手（enigma9_log_stats.py）・相手 BOT の一致（opp_bot_match9.py）・本の読み取りのコード（book9/）。" -m "Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>"
```

---

## Task 10: 文書（CLAUDE.md・ハーネスの spec・INDEX）と全体のテスト

**Files:**
- Modify（パッチ）: `CLAUDE.md`・`docs/superpowers/specs/2026-09-23-selfplay-harness-design.md`・`docs/superpowers/specs/INDEX.md`
- Modify: この計画（Task 1〜9 の見出しの下に ``状態: 完了（`<短いハッシュ>`）。``。Task 10 の行は C1 Step 1 でコントローラが足す）

- [ ] **Step 1: パッチスクリプト `<scratchpad>/patch_9x9_t10.py`**

```python
import sys

sys.path.insert(0, r"<scratchpad の実パス>")
from crlf_patch import patch  # noqa: E402

patch("CLAUDE.md", [
    (
        "python -m katrain_debug.selfplay calibrate --size 13 --strategy enigma13plus --games 8 --write-pool docs/superpowers/specs/calibration-data/selfplay/opponent_pool_13.json\n",
        "python -m katrain_debug.selfplay calibrate --size 13 --strategy enigma13plus --games 8 --write-pool docs/superpowers/specs/calibration-data/selfplay/opponent_pool_13.json\n"
        "# 9路（spec 韜晦 §16）: 投了の手数は recon_9/（難解＋9路の実戦・縮めない）。calibrate --strategy NAME:key=v,... で AI の設定を上書きする\n"
        "python -m katrain_debug.selfplay calibrate --size 9 --strategy enigma9plus:enigma9plus_max_loss=1.6,enigma9plus_target_score=0.2,enigma9plus_gamble_until_move=20 --ranks rank_1d,rank_3d,rank_5d,rank_7d,rank_9d --games 8 --write-pool docs/superpowers/specs/calibration-data/selfplay/opponent_pool_9.json\n"
        "# 定跡を知る相手（手数 < B の間、AI の手がすべて X 目以下なら KataGo の最善手・既定 X 0.3）と序盤の窓の指標\n"
        "python -m katrain_debug.selfplay run --arm A=veil9:veil9_open_moves=0 --arm B=veil9:veil9_open_moves=12 --size 9 --pairs 24 --book-moves 24 --label book\n"
        "python docs/superpowers/specs/calibration-data/selfplay/window_stats.py experiments/selfplay/<dir> --compare B A\n",
        1,
    ),
    (
        "KataGo の探索は非決定的＝seed は開始条件の対で再現ではない（最低 20 ペアで比べる）。\n",
        "KataGo の探索は非決定的＝seed は開始条件の対で再現ではない（最低 20 ペアで比べる）。"
        "run / calibrate は使った config の写し `config-snapshot.json` を出力ディレクトリに置き、`--resume` はその写しを読む"
        "（中身が run.json の sha256 と違えば止まる）。`<prefix>_open_moves` > 0 の韜晦のアームは、窓を任せる難解＋の節"
        "（戦略キー・設定・指紋）を run.json の `delegate` に持ち、再開と `summarize` の合算で違えば止まる（アームの上書きは任せる先に効かない"
        "＝難解＋の節はユーザー config のまま。計画時に `note: ... opening window delegated to ...` を出す）。\n",
        1,
    ),
])
patch("docs/superpowers/specs/2026-09-23-selfplay-harness-design.md", [
    (
        "**9路**: 相手の一致率の実測が無いので、先に保持中の難解＋9路ログ17局を SGF に戻して事後解析し、目標を作ってから同じ手順で校正する。\n",
        "**9路**: 相手の一致率の実測が無いので、先に保持中の難解＋9路ログ17局を SGF に戻して事後解析し、目標を作ってから同じ手順で校正する。"
        "手順と結果は韜晦の spec（`2026-09-23-veil-strategy-design.md`）§16（2026-09-26）: 復元は `recon_logs.py --size 9`（持碁9路の1局を除く難解＋の局＝"
        "`recon_9/`・局数は韜晦の spec §16.2 手順1。一致率ひかえめ9路の実戦は `--strategy Veil9Strategy` で `recon_9_veil/` に分ける）・"
        "区間は 12 / 41 手・投了の手数は `recon_9/` を縮めずに使う（lead モデルは 9路では `--resign-lead` が要る）・校正の AI は実戦の多数派の設定"
        "（`calibrate --strategy enigma9plus:<上書き>`）・目標は `CALIB_TARGETS_9`。\n",
        1,
    ),
])
patch("docs/superpowers/specs/INDEX.md", [
    (
        "試作と計測・韜晦の反実仮想（`README.md` に一覧）\n",
        "試作と計測・韜晦の反実仮想・9路（`recon_9/`・`window_stats.py`・調査の集計 `enigma9_log_stats.py` / `opp_bot_match9.py`・韜晦 spec §16）（`README.md` に一覧）\n",
        1,
    ),
    (  # Plan A の Task 6 が足した文（計画の一覧）にこの計画を足す
        "`2026-09-26-veil-opening.md`（同 §15 序盤の研究外し）も同じ spec の追加の計画。",
        "`2026-09-26-veil-opening.md`（同 §15 序盤の研究外し）と `2026-09-26-selfplay-9x9.md`（同 §16 9路の校正と研究外しの計測・"
        "ハーネスの 9路対応）も同じ spec の追加の計画。",
        1,
    ),
])
print("ok")
```

- [ ] **Step 2: 全体のテスト**

Run: `python -m pytest -q -p no:cacheprovider tests --ignore=tests/test_ai.py`
Expected: 2171 件すべて PASS（Plan A の終わりの 2118 件〈Plan A の計画の Task 7 Step 1 の期待値〉＋この計画の 53 件＝Task 1 2・Task 2 3・Task 3 3・
Task 4 10・Task 5a 12・Task 5b 2・Task 6 12・Task 7 6・Task 8 3）。

- [ ] **Step 3: 計画の状態**（パッチスクリプト `<scratchpad>/patch_9x9_t10_status.py`。Task 1〜9 の見出しの下に、Plan A と同じ形の
``状態: 完了（`<短いハッシュ>`）。`` の行を足す。ハッシュは件名で引いたそのタスクのコミット〈レビューの修正のコミットは含めない〉。Task 10 のコミットは
まだ無いので、Task 10 の行は C1 Step 1 でコントローラが足す）

```python
import subprocess
import sys

sys.path.insert(0, r"<scratchpad の実パス>")
from crlf_patch import patch  # noqa: E402

PLAN = "docs/superpowers/plans/2026-09-26-selfplay-9x9.md"
TASKS = [  # (見出しの行, そのタスクのコミットの件名)
    ("## Task 1: 校正用の区間と校正目標を盤サイズ別にする（TDD）", "feat(selfplay): 校正用の区間の境目と校正目標を盤サイズ別に引けるようにする"),
    ("## Task 2: 投了の手数を盤サイズ別の実戦から読む（TDD）", "feat(selfplay): 9路の投了の手数を recon_9 の実戦から縮めずに読む"),
    (
        "## Task 3: 校正（calibrate）を盤サイズ別にし、AI の設定の上書きを受ける（TDD）",
        "feat(selfplay): 校正の目標とプールの選択を盤サイズ別にし、calibrate で AI の設定を上書きできるようにする",
    ),
    ("## Task 4: 定跡を知る相手（`--book-moves B`・`--book-loss X`）（TDD）", "feat(selfplay): 定跡を知る相手（--book-moves / --book-loss）を足す"),
    (
        "## Task 5a: 研究外しの任せる先の指紋（再開と合算の突き合わせ）（TDD）",
        "feat(selfplay): 研究外しの任せる先の指紋を run.json に持たせ、再開と合算で突き合わせる",
    ),
    (
        "## Task 5b: 設定の固定（実行ごとの config の写し・`--resume` はその写しを読む）（TDD）",
        "feat(selfplay): 実行ごとに config の写しを置き、--resume はその写しを読む",
    ),
    ("## Task 6: 序盤の窓の指標 `window_stats.py`（TDD）", "feat(selfplay): 序盤の窓の指標 window_stats.py を足す"),
    (
        "## Task 7: 実戦ログの復元 `recon_logs.py` を 9路に対応させる（TDD）",
        "feat(selfplay): 実戦ログの復元を 9路に対応させる（--log-dir / --out / --size / --strategy・除く局・相手のパス・置き石の初手・局の設定）",
    ),
    (
        "## Task 8: 事後解析と校正目標のスクリプトを盤サイズ別にする（TDD・パッチ）",
        "feat(selfplay): 事後解析と校正目標のスクリプトを盤サイズ別の区間・出力先・局の絞り込みに対応させる",
    ),
    ("## Task 9: 調査のスクリプトの置き場（spec §16.2 手順8）と README", "docs(selfplay): 9路の調査の集計スクリプトを calibration-data/selfplay/ に置く"),
]
log = subprocess.run(["git", "log", "--format=%h%x09%s", "76105598..HEAD"], capture_output=True, check=True).stdout
latest = {}
for line in log.decode("utf-8").splitlines():  # 新しい順＝同じ件名が2つあれば新しい方
    h, _, subject = line.partition("\t")
    latest.setdefault(subject, h)
edits = []
for heading, subject in TASKS:
    if subject not in latest:
        sys.exit(f"commit not found: {subject}")
    edits.append((heading + "\n", f"{heading}\n\n状態: 完了（`{latest[subject]}`）。\n", 1))
patch(PLAN, edits)
print({s: latest[s] for _, s in TASKS})
```

Run: `python <scratchpad>/patch_9x9_t10_status.py`（worktree のルートで）
Expected: 10 件の件名とハッシュが出る（`commit not found` で止まったら、そのタスクのコミットの件名を `git log --oneline 76105598..HEAD` で確かめる）。
old は「見出し＋改行」なので、このスクリプトと C1 Step 1 のスクリプトの中の見出しの文字列〈後ろが `"`〉には当たらない。

- [ ] **Step 4: コミット**

```bash
git add CLAUDE.md docs/superpowers/specs/2026-09-23-selfplay-harness-design.md docs/superpowers/specs/INDEX.md docs/superpowers/plans/2026-09-26-selfplay-9x9.md
git commit -m "docs(selfplay): 9路対応・定跡を知る相手・窓の指標・設定の固定を CLAUDE.md とハーネスの spec に反映" -m "Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>"
```

---

## 計測のキャンペーン（C1〜C10・コントローラ＝メインセッション）

共通:
- 走らせるのは worktree のルート（`C:/Users/iwaki/Documents/katrain-1.17.1.1/katrain-1.17.1.1/.claude/worktrees/veil-opening`）。出力は既定の
  `experiments/selfplay/<YYYYMMDD_HHMM>_<label>/`（worktree の中・gitignore）。C9 で本体の `experiments/selfplay/` へ移す（worktree を消すと消える）。
- すべての run / calibrate / offline_report に **設定の写し** `--config experiments/selfplay/veil-open-config.json`（C1 で作る）を渡す。
- すべての run に `--hp-audit rank_9d`（外した手の 9段 hp）。コンソールは `experiments/selfplay/<label>.console.txt` に落とす（`run_in_background: true`）。
- 走らせている間は KaTrain を起動しないようユーザーに伝える。各実行の後に `WARN integrity` の行が無いこと・aborted が 0 か少ないこと
  （`grep -c '"result": "aborted"' <run>/games.jsonl`。`<run>` はその実行の出力ディレクトリ＝`$(ls -d experiments/selfplay/*_<label>)`）を見る。
  aborted があれば `--resume <run> --retry-aborted` で打ち直す。
- **9-1 と 9-2 の間はコミットしない**（`summarize` の合算は git HEAD の一致を見る＝コミットを挟むと `--allow-mixed` が要る）。記録のコミットは C6 の後にまとめる。
- アームの文字列（すべてのキーを明示。値は spec §6.1 の 9路の列・§13.2・§14.2・§16.3、13路は §6.1 の表の後の段落の loose と §16.4）は
  C1 Step 5 のファイル `experiments/selfplay/veil-open-arms.sh` に書き、各コマンドの前で `source` する（Bash の変数は呼び出しをまたがないため）。
  **例外は `enigma` のアーム**（`enigma=enigma9plus` / `enigma=enigma13plus`・上書きなし）: 研究外しの任せる先と同じ難解＋の節を設定の写しから読む
  （spec §16.2 手順9。節の中身は C9 で campaign md に写す）。`human` のアームも HumanStyle 9段の2キーだけ（`human=human:human_kyu_rank=-8,modern_style=true`）。

### C1: 前提の確認・ユーザーへの確認・設定の写し

- [ ] **Step 1: Plan A と Task 1〜10 が入っていること**

```bash
git log --oneline 76105598..HEAD | head -30
python -c "from katrain.core.ai import VEIL_OPEN_DELEGATES, STRATEGY_REGISTRY as R; print(VEIL_OPEN_DELEGATES, R['ai:veil9'].SETTING_DEFAULTS['open_moves'])"
python -m pytest -q -p no:cacheprovider tests --ignore=tests/test_ai.py
```
Expected: Plan A と Plan B の Task 1〜10 のコミット・`{9: 'ai:enigma9plus', 13: 'ai:enigma13plus', 19: 'ai:enigma19plus'} 0`・2171 件すべて PASS
（Task 10 Step 2 と同じ）。

Task 10 の状態の行（パッチスクリプト `<scratchpad>/patch_9x9_c1_status.py`。Task 10 の見出しの下に Task 10 のコミットのハッシュを足してコミットする）:

```python
import subprocess
import sys

sys.path.insert(0, r"<scratchpad の実パス>")
from crlf_patch import patch  # noqa: E402

PLAN = "docs/superpowers/plans/2026-09-26-selfplay-9x9.md"
HEADING = "## Task 10: 文書（CLAUDE.md・ハーネスの spec・INDEX）と全体のテスト"
SUBJECT = "docs(selfplay): 9路対応・定跡を知る相手・窓の指標・設定の固定を CLAUDE.md とハーネスの spec に反映"
log = subprocess.run(["git", "log", "--format=%h%x09%s", "76105598..HEAD"], capture_output=True, check=True).stdout
hashes = [h for h, _, s in (line.partition("\t") for line in log.decode("utf-8").splitlines()) if s == SUBJECT]
if not hashes:
    sys.exit(f"commit not found: {SUBJECT}")
patch(PLAN, [(HEADING + "\n", f"{HEADING}\n\n状態: 完了（`{hashes[0]}`）。\n", 1)])
print(hashes[0])
```

```bash
python <scratchpad>/patch_9x9_c1_status.py
git add docs/superpowers/plans/2026-09-26-selfplay-9x9.md
git commit -m "docs(selfplay): 9路対応の計画の Task 10 の状態を記録する" -m "Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>"
```
（計測の実行〈C4 以降〉の run.json は git HEAD を記録する。このコミットは計測の前なので、9-1 と 9-2 の合算には影響しない。）

- [ ] **Step 2: ユーザーに確かめる**（チャットで。承認が無ければ走らせない）
  「KataGo を使う計測（spec §16.3〜16.4）を、KaTrain を止めて走らせてよいか。見積もり（spec §16.5・13路の実測の1局の時間から換算）:
  準備（9路の実戦 16 局の事後解析と相手の校正 40 局）約 1 時間、9-1 約 2 時間、9-2 約 0.7 時間、9-1 の投了なし 10 対 約 0.4 時間、
  9-3 約 1 時間、9-4 約 1.3 時間、9-5 約 1.4 時間（9路 合わせて約 7.8 時間）、13路 13-1〜13-4 合わせて約 6 時間。9路と 13路は分けて
  確かめる（9路の結果を見てから 13路を走らせるか決めてもよい）。走らせている間は KaTrain を起動しないでほしい。」

- [ ] **Step 3: KaTrain と KataGo が止まっていること**（PowerShell）

`Get-CimInstance Win32_Process | Where-Object { $_.CommandLine -match '-m katrain' -or $_.Name -eq 'katago.exe' } | Select-Object ProcessId, Name, CommandLine`
Expected: 何も出ない（出たらユーザーに KaTrain を閉じてもらう）。ハーネス自身も起動時に `ensure_no_katago` で確かめる。

- [ ] **Step 4: ユーザー設定の写し**（読むだけ。以後の計測はすべてこの写しで走らせる＝途中で GUI の設定が変わっても計測は変わらない）

```bash
mkdir -p experiments/selfplay
python -c "import shutil,json;shutil.copyfile(r'C:/Users/iwaki/.katrain/config.json','experiments/selfplay/veil-open-config.json');c=json.load(open('experiments/selfplay/veil-open-config.json',encoding='utf-8'));print(c['engine']['max_visits'], c['game']['handicap']);print(json.dumps(c['ai']['ai:enigma9plus'],ensure_ascii=False));print(json.dumps(c['ai']['ai:enigma13plus'],ensure_ascii=False))"
```
Expected: `2500 0` と2つの節（C9 で campaign md の「実行条件」に写す＝任せる先と `enigma` アームの設定）。

- [ ] **Step 5: アームのファイル** `experiments/selfplay/veil-open-arms.sh`（Write で作る。gitignore の中）

```bash
# spec §16.3 / §16.4 のアーム（すべてのキーを明示）。source してから --arm "$SPEC9" のように使う
SPEC9='spec=veil9:veil9_target_rate=0.4,veil9_reserve=3.0,veil9_min_winrate=0.85,veil9_free_loss=0.2,veil9_free_wr_drop=0.03,veil9_close_drift_cap=0.0,veil9_spend_rate=0.5,veil9_max_loss=3.0,veil9_yose_max_loss=1.0,veil9_dominant_hp=0.8,veil9_dominant_max_loss=1.5,veil9_min_human_policy=0.05,veil9_natural_ratio=0.2,veil9_cost_slack=0.3,veil9_trap_mode=false,veil9_trap_min_delta_e=0.5,veil9_blunder_mode=0,veil9_blunder_max_loss=6.0,veil9_blunder_per_game=1,veil9_blunder_hp_ratio=0.7,veil9_forced_mode=0,veil9_forced_min_lead=1.0,veil9_forced_min_winrate=0.7,veil9_forced_max_loss=5.0,veil9_open_moves=0'
LOOSE9='loose9=veil9:veil9_target_rate=0.4,veil9_reserve=3.0,veil9_min_winrate=0.85,veil9_free_loss=0.3,veil9_free_wr_drop=0.03,veil9_close_drift_cap=0.0,veil9_spend_rate=1.0,veil9_max_loss=4.0,veil9_yose_max_loss=1.5,veil9_dominant_hp=1.01,veil9_dominant_max_loss=2.0,veil9_min_human_policy=0.05,veil9_natural_ratio=0.1,veil9_cost_slack=0.3,veil9_trap_mode=false,veil9_trap_min_delta_e=0.5,veil9_blunder_mode=0,veil9_blunder_max_loss=6.0,veil9_blunder_per_game=1,veil9_blunder_hp_ratio=0.7,veil9_forced_mode=0,veil9_forced_min_lead=1.0,veil9_forced_min_winrate=0.7,veil9_forced_max_loss=5.0,veil9_open_moves=0'
LAYERS9='layers=veil9:veil9_target_rate=0.4,veil9_reserve=3.0,veil9_min_winrate=0.85,veil9_free_loss=0.3,veil9_free_wr_drop=0.03,veil9_close_drift_cap=0.0,veil9_spend_rate=1.0,veil9_max_loss=4.0,veil9_yose_max_loss=1.5,veil9_dominant_hp=1.01,veil9_dominant_max_loss=2.0,veil9_min_human_policy=0.05,veil9_natural_ratio=0.1,veil9_cost_slack=0.3,veil9_trap_mode=false,veil9_trap_min_delta_e=0.5,veil9_blunder_mode=2,veil9_blunder_max_loss=6.0,veil9_blunder_per_game=1,veil9_blunder_hp_ratio=0.7,veil9_forced_mode=2,veil9_forced_min_lead=1.0,veil9_forced_min_winrate=0.7,veil9_forced_max_loss=5.0,veil9_open_moves=0'
OPEN9='open=veil9:veil9_target_rate=0.4,veil9_reserve=3.0,veil9_min_winrate=0.85,veil9_free_loss=0.3,veil9_free_wr_drop=0.03,veil9_close_drift_cap=0.0,veil9_spend_rate=1.0,veil9_max_loss=4.0,veil9_yose_max_loss=1.5,veil9_dominant_hp=1.01,veil9_dominant_max_loss=2.0,veil9_min_human_policy=0.05,veil9_natural_ratio=0.1,veil9_cost_slack=0.3,veil9_trap_mode=false,veil9_trap_min_delta_e=0.5,veil9_blunder_mode=2,veil9_blunder_max_loss=6.0,veil9_blunder_per_game=1,veil9_blunder_hp_ratio=0.7,veil9_forced_mode=2,veil9_forced_min_lead=1.0,veil9_forced_min_winrate=0.7,veil9_forced_max_loss=5.0,veil9_open_moves=12'
ENIGMA9='enigma=enigma9plus'
HUMAN9='human=human:human_kyu_rank=-8,modern_style=true'
LOCAL13='local=veil13:veil13_target_rate=0.3,veil13_reserve=5.0,veil13_min_winrate=0.85,veil13_free_loss=0.4,veil13_free_wr_drop=0.03,veil13_close_drift_cap=0.0,veil13_spend_rate=1.0,veil13_max_loss=6.0,veil13_yose_max_loss=2.0,veil13_dominant_hp=1.01,veil13_dominant_max_loss=3.0,veil13_min_human_policy=0.05,veil13_natural_ratio=0.1,veil13_cost_slack=0.3,veil13_trap_mode=false,veil13_trap_min_delta_e=0.5,veil13_blunder_mode=2,veil13_blunder_max_loss=10.0,veil13_blunder_per_game=1,veil13_blunder_hp_ratio=0.7,veil13_forced_mode=2,veil13_forced_min_lead=2.0,veil13_forced_min_winrate=0.7,veil13_forced_max_loss=10.0,veil13_open_moves=0'
LOCALOPEN13='local_open=veil13:veil13_target_rate=0.3,veil13_reserve=5.0,veil13_min_winrate=0.85,veil13_free_loss=0.4,veil13_free_wr_drop=0.03,veil13_close_drift_cap=0.0,veil13_spend_rate=1.0,veil13_max_loss=6.0,veil13_yose_max_loss=2.0,veil13_dominant_hp=1.01,veil13_dominant_max_loss=3.0,veil13_min_human_policy=0.05,veil13_natural_ratio=0.1,veil13_cost_slack=0.3,veil13_trap_mode=false,veil13_trap_min_delta_e=0.5,veil13_blunder_mode=2,veil13_blunder_max_loss=10.0,veil13_blunder_per_game=1,veil13_blunder_hp_ratio=0.7,veil13_forced_mode=2,veil13_forced_min_lead=2.0,veil13_forced_min_winrate=0.7,veil13_forced_max_loss=10.0,veil13_open_moves=30'
ENIGMA13='enigma=enigma13plus'
STRONG='strategy:human:human_kyu_rank=-8,modern_style=true'  # HumanStyle 9段（--opponent の値）
CFG='experiments/selfplay/veil-open-config.json'
```
確かめ: `source experiments/selfplay/veil-open-arms.sh && for a in "$SPEC9" "$LOOSE9" "$LAYERS9" "$OPEN9" "$LOCAL13" "$LOCALOPEN13"; do echo "$a" | tr ',' '\n' | wc -l; done`
Expected: どれも 25（25 キー）。

### C2: 9路の実戦の復元と事後解析（spec §16.2 手順1・2・約 15 分）

- [ ] **Step 1: 復元**

```bash
python docs/superpowers/specs/calibration-data/selfplay/recon_logs.py --size 9 --log-dir "C:/Users/iwaki/Documents/katrain-1.17.1.1/katrain-1.17.1.1/experiments/selfplay/realgames-9x9-logs" --out docs/superpowers/specs/calibration-data/selfplay/recon_9 | tee experiments/selfplay/recon9.console.txt | grep -E "^game_|setup|inserted|!!"
```
Expected: Task 7 Step 4 と同じ（16 局・置き石の初手 4・相手のパス 1・`!!` 0）。`!!` が出た局は除く局に足す（`EXCLUDE_9` に ID と理由を書き、
Task 7 のテストを直してコミット）＝spec §16.2 手順1 の「直せなければ除く」。

- [ ] **Step 2: 事後解析（KataGo・2500v）**（`run_in_background: true`）

```bash
python docs/superpowers/specs/calibration-data/selfplay/offline_report.py --dir docs/superpowers/specs/calibration-data/selfplay/recon_9 --config experiments/selfplay/veil-open-config.json > experiments/selfplay/offline9.console.txt 2>&1
```
Expected: `recon_9/report_game_*.json` が 16・`report_all.json`。コンソールに `TIMEOUT` の行が無い。各行の `pre41` / `post41`。

- [ ] **Step 3: 実データで投了の手数を凍結するテスト**（`tests/test_selfplay_run.py` の `test_recon_dir_by_board_size` の後に Edit で足す）

```python
    def test_recon_9_lengths_are_the_9x9_real_games(self):
        """計測の準備（C2）: 難解＋9路の実戦 16 局（recon_9/report_game_*.json・縮めない）。手数の中央値 48。"""
        lens = R.load_resign_lengths(9)
        assert len(lens) == 16 and statistics.median(lens) == 48
        summaries = R._recon_summaries(R.recon_dir_for(9))
        assert {s["board_size"] for s in summaries} == {9} and all(s.get("settings") for s in summaries)
```
Run: `python -m pytest tests/test_selfplay_run.py -q -p no:cacheprovider -k recon_9` → 1 passed。
（C2 Step 1 で局を除いたら 16 と 48 をその実数に直す。）

- [ ] **Step 4: README に行を足してコミット**（パッチスクリプト `<scratchpad>/patch_9x9_c2.py`）

```python
import sys

sys.path.insert(0, r"<scratchpad の実パス>")
from crlf_patch import patch  # noqa: E402

P = "docs/superpowers/specs/calibration-data/selfplay/README.md"
OLD = "| `recon/summary.json` | ログ側の手番の分類（`recon_logs.py` の出力） |\n"
ROW = (
    "| `recon_9/` | 難解＋9路の実戦 16 局（2026-09-19〜22 の監視対局ログ・`experiments/selfplay/realgames-9x9-logs/` から "
    "`recon_logs.py --size 9`。持碁9路 `game_20260919_224212` を除く・置き石の初手 4 局と監視の外の相手のパス 1 局を直した）と"
    "事後 2500v（`offline_report.py --dir recon_9`）。summary に局の難解＋の設定。9路の投了の手数（length モデル）と `CALIB_TARGETS_9` の元 |\n"
)
patch(P, [(OLD, OLD + ROW, 1)])
```
（C2 Step 1 で局を除いたら「16 局」と直したことの文をその実数に直す。）

```bash
git add docs/superpowers/specs/calibration-data/selfplay/recon_9 docs/superpowers/specs/calibration-data/selfplay/README.md tests/test_selfplay_run.py
git commit -m "docs(selfplay): 難解＋9路の実戦 16 局を復元して事後 2500v で解析する（recon_9）" -m "spec §16.2 手順1・2。" -m "Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>"
```

### C3: 9路の校正目標 `CALIB_TARGETS_9`（spec §16.2 手順4）

- [ ] **Step 1: 目標を出す**

```bash
python docs/superpowers/specs/calibration-data/selfplay/calib_targets.py docs/superpowers/specs/calibration-data/selfplay/recon_9 --size 9 --ai-where enigma9plus_max_loss=1.6,enigma9plus_target_score=0.2,enigma9plus_gamble_until_move=20 | tee experiments/selfplay/calib-targets-9.txt | tail -3
```
Expected: `games 16 / ai_mean over 7 (...)`・`CALIB_TARGET_GAMES 7/16`・`CALIB_TARGETS {...}`（7 局＝213928・214631・215145・222543・223015・223517・20260921_171019。
AI 側 ai_mean はこの 7 局＝calibrate の AI と同じ設定の局、相手の値は 16 局）。

- [ ] **Step 2: 定数とテスト**（Edit）: `selfplay_stats.py` の Task 1 のコメント行
`# 盤サイズ → 校正目標（spec 2026-09-23-veil-strategy-design.md §16.2 手順4。9路は recon_9 から足す）。無い盤はプールを選ばない`
の直前に（コメントと `CALIB_TARGETS = ...` の行を離さない）、空行1つを挟んで

```python
# 校正目標（9路の実戦・難解＋9路 16局・事後 2500v。ai_mean は実戦の多数派の設定〈max_loss 1.6・target_score 0.2・
# gamble_until_move 20〉の 7 局＝calibrate の AI と同じ設定の局。spec §16.2 手順4。calib_targets.py --size 9 の CALIB_TARGETS 行）
CALIB_TARGETS_9 = <Step 1 の CALIB_TARGETS の JSON をそのまま Python の dict として貼る（true/false/null は無い）>
```
を足し、`CALIB_TARGETS = {13: CALIB_TARGETS_13, 9: CALIB_TARGETS_9}`・`CALIB_TARGET_GAMES = {13: "16/18", 9: "7/16"}` にする。
（C2 で局を除いたら `"7/16"` は Step 1 の `CALIB_TARGET_GAMES` の値。）`tests/test_selfplay_stats.py` の `test_calibration_targets_by_board_size` の後に足す:

```python
    def test_calibration_targets_9_have_the_shape_of_13(self):
        """計測の準備（C3）: 9路の目標は recon_9 から（spec §16.2 手順4）。形は 13路と同じ。"""
        assert S.calib_targets_for(9) is S.CALIB_TARGETS_9 and S.CALIB_TARGET_GAMES[9] == "7/16"
        assert set(S.CALIB_TARGETS_9) == set(S.CALIB_TARGETS_13)
        assert all(set(S.CALIB_TARGETS_9["bins"][b]) == {"opp_top1", "opp_loss"} for b in S.CALIB_TARGETS_13["bins"])
        assert S.CALIB_TARGETS_9["moves_median"] == 48
```
Run: `python -m pytest tests/test_selfplay_*.py -q -p no:cacheprovider` → すべて PASS（Task 1 のテストの `calib_targets_for(19) is None` は変わらない）。

- [ ] **Step 3: コミット**

```bash
git add katrain_debug/selfplay_stats.py tests/test_selfplay_stats.py
git commit -m "feat(selfplay): 9路の校正目標 CALIB_TARGETS_9 を recon_9 から足す" -m "spec §16.2 手順4。AI 側は実戦の多数派の設定の 7 局。" -m "Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>"
```

### C4: 9路の相手プールの校正（spec §16.2 手順4・40 局・約 45 分）

- [ ] **Step 1: 上書きを出す**（実戦の多数派の 7 局の設定と、設定の写しの `ai:enigma9plus` の違い）

```bash
python - <<'EOF'
import ast, json
from collections import Counter
snap = json.load(open("experiments/selfplay/veil-open-config.json", encoding="utf-8"))["ai"]["ai:enigma9plus"]
games = json.load(open("docs/superpowers/specs/calibration-data/selfplay/recon_9/summary.json", encoding="utf-8"))
maj = [g["settings"] for g in games if (g["settings"]["enigma9plus_max_loss"], g["settings"]["enigma9plus_target_score"], g["settings"]["enigma9plus_gamble_until_move"]) == (1.6, 0.2, 20)]
assert len(maj) == 7, len(maj)
items = []
for k in sorted({k for s in maj for k in s}):
    value = ast.literal_eval(Counter(repr(s.get(k)) for s in maj).most_common(1)[0][0])
    if value != snap.get(k):
        items.append(f"{k}={str(value).lower() if isinstance(value, bool) else value}")
print("enigma9plus:" + ",".join(items))
EOF
```
Expected（2026-09-26 のユーザー設定での出力。C1 の写しが違えば出力も変わる＝その出力を使う）:
`enigma9plus:enigma9plus_gamble_min_winrate=0.25,enigma9plus_gamble_until_move=20.0,enigma9plus_large_lead_max_loss=6.0,enigma9plus_max_loss=1.6,enigma9plus_overdraft_deficit=2.0,enigma9plus_overdraft_probes=6,enigma9plus_target_score=0.2`
（7 局で値が分かれるキー〈`overdraft_deficit`: 2.0 が 6 局・1.0 が 1 局〉は多い方。）

- [ ] **Step 2: 校正**（`run_in_background: true`。`<STRAT>` は Step 1 の出力の文字列そのもの）

```bash
python -m katrain_debug.selfplay calibrate --size 9 --strategy '<STRAT>' --ranks rank_1d,rank_3d,rank_5d,rank_7d,rank_9d --games 8 --config experiments/selfplay/veil-open-config.json --label calib9 --write-pool docs/superpowers/specs/calibration-data/selfplay/opponent_pool_9.json > experiments/selfplay/calib9.console.txt 2>&1
```
Expected: 最後に `best pool: [...]`・`pool written: .../opponent_pool_9.json`（`WARN integrity` なら `.suspect` に書かれる＝logs/ を見て原因を
直してから `CAL9=$(ls -d experiments/selfplay/*_calib9) && python -m katrain_debug.selfplay calibrate --resume "$CAL9" --retry-aborted --write-pool
docs/superpowers/specs/calibration-data/selfplay/opponent_pool_9.json`）。`$CAL9/calibration.md` の段位ごとの表・3段位の候補・ずれ（`harness_drift_ai`＝
選んだプールの AI 一致率 − 実戦の多数派の 7 局の 2500v の局平均・参考値）。

- [ ] **Step 3: 手数の確かめと記録**: `calibration.md` の段位ごとの「手数中央値」と、全局の手数の中央値を実戦の 48 と比べる。

```bash
CAL9=$(ls -d experiments/selfplay/*_calib9) && echo "$CAL9"
python -c "import json,statistics,sys;R=[json.loads(l) for l in open(sys.argv[1]+'/games.jsonl',encoding='utf-8')];print(statistics.median(r['n_moves'] for r in R if r['result']!='aborted'))" "$CAL9"
```
大きく外れる（±25% 超）なら結果をユーザーに見せてから進む（投了モデルの見直しは spec の範囲外）。
`$CAL9/calibration.md` を写し、末尾に2行を足す（`<手数の中央値>` は上の出力・`<STRAT>` は Step 1 の文字列）:

```bash
CAL9=$(ls -d experiments/selfplay/*_calib9) && MD=docs/superpowers/specs/calibration-data/selfplay/selfplay-calibration-results-$(date +%Y%m%d)-9x9.md
cp "$CAL9/calibration.md" "$MD"
printf '\n- 手数の中央値 %s（実戦 48）\n- AI の設定の上書き: `%s`\n' '<手数の中央値>' '<STRAT>' >> "$MD"
```
README はパッチスクリプト `<scratchpad>/patch_9x9_c4.py` で1行足す:

```python
import sys

sys.path.insert(0, r"<scratchpad の実パス>")
from crlf_patch import patch  # noqa: E402

P = "docs/superpowers/specs/calibration-data/selfplay/README.md"
OLD = "| `selfplay-calibration-results-*.md` |"
ROW = (
    "| `opponent_pool_9.json` | 9路の相手ボットの 3段位プール（`calibrate --size 9 --strategy enigma9plus:<実戦の多数派の 7 局の設定> "
    "--ranks rank_1d,rank_3d,rank_5d,rank_7d,rank_9d --games 8`・結果 `selfplay-calibration-results-<YYYYMMDD>-9x9.md`） |\n"
)
patch(P, [(OLD, ROW + OLD, 1)])
```

```bash
git add docs/superpowers/specs/calibration-data/selfplay/opponent_pool_9.json docs/superpowers/specs/calibration-data/selfplay/selfplay-calibration-results-*-9x9.md docs/superpowers/specs/calibration-data/selfplay/README.md
git commit -m "docs(selfplay): 9路の相手ボットを実戦の多数派の設定の難解＋で校正する（opponent_pool_9.json）" -m "spec §16.2 手順4。" -m "Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>"
```

### C5: 9-0 スモークと 9-1 校正（spec §16.3）

- [ ] **Step 1: 9-0 スモーク**（2 対・約 5 分）

```bash
source experiments/selfplay/veil-open-arms.sh && python -m katrain_debug.selfplay run --size 9 --pairs 2 --config "$CFG" --hp-audit rank_9d --label veil9-smoke --arm "$LAYERS9" --arm "$OPEN9" > experiments/selfplay/veil9-smoke.console.txt 2>&1
```
確かめること（`RUN=$(ls -d experiments/selfplay/*_veil9-smoke)`）:
- コンソールに `note: arm open (veil9): opening window delegated to ai:enigma9plus (user config, fingerprint ...)`・`WARN integrity` が無い・aborted 0。
- `python -c "import json;p=json.load(open('$RUN/run.json',encoding='utf-8'));print([(a['name'],a.get('delegate') and a['delegate']['strategy_key']) for a in p['arms']], p['config_snapshot'], p['opponent']['ranks'])"`
  → `[('layers', None), ('open', 'ai:enigma9plus')] config-snapshot.json [<opponent_pool_9.json の3段位>]`。
- `grep -c '"decision_open": "played"' $RUN/moves.jsonl` が 1 以上・`grep -h "VeilOpenEnigma9PlusStrategy" $RUN/logs/open_*.log | head -3` に行がある・
  `python docs/superpowers/specs/calibration-data/selfplay/window_stats.py $RUN` の `open open:` の行が `outside_window=0 missing_inside=0`
  （gate があれば `missing_inside` は 0 のまま＝gate も `decision_open` を持つ）。
問題があれば止めてユーザーに報告（Plan A か Plan B のバグ＝直してからやり直す）。

- [ ] **Step 2: 9-1 校正**（6 アーム × 24 対＝144 局・約 2 時間）

```bash
source experiments/selfplay/veil-open-arms.sh && python -m katrain_debug.selfplay run --size 9 --pairs 24 --config "$CFG" --hp-audit rank_9d --label veil9-p1 --arm "$SPEC9" --arm "$LOOSE9" --arm "$LAYERS9" --arm "$OPEN9" --arm "$ENIGMA9" --arm "$HUMAN9" > experiments/selfplay/veil9-p1.console.txt 2>&1
```
終わったら（run の最後の要約は先頭のアーム `spec` と各アームの差。`summarize --out $P1` は `summary.{json,txt}` を open−layers の比較つきで
書き直すので、先に `summary-vs-spec.*` に写す）:
```bash
P1=$(ls -d experiments/selfplay/*_veil9-p1) && cp $P1/summary.json $P1/summary-vs-spec.json && cp $P1/summary.txt $P1/summary-vs-spec.txt
python -m katrain_debug.selfplay summarize $P1 --compare open layers --out $P1 > experiments/selfplay/veil9-p1-open-vs-layers.txt
python docs/superpowers/specs/calibration-data/selfplay/window_stats.py $P1 --compare open layers > experiments/selfplay/veil9-p1-window.txt
```

### C6: 9-2 研究外しの効果・9-1 の投了なし（spec §16.3）

- [ ] **Step 1: 9-2**（2 アーム × 24 対・seed 1024〜1047・約 0.7 時間。**9-1 との間にコミットしない**）

```bash
source experiments/selfplay/veil-open-arms.sh && python -m katrain_debug.selfplay run --size 9 --pairs 24 --seed-base 1024 --config "$CFG" --hp-audit rank_9d --label veil9-p2 --arm "$LAYERS9" --arm "$OPEN9" > experiments/selfplay/veil9-p2.console.txt 2>&1
```
合算（48 対。9-2 だけの run の要約は先に `summary-vs-layers.*` に写す）:
```bash
P1=$(ls -d experiments/selfplay/*_veil9-p1) && P2=$(ls -d experiments/selfplay/*_veil9-p2) && cp $P2/summary.json $P2/summary-vs-layers.json && cp $P2/summary.txt $P2/summary-vs-layers.txt
python -m katrain_debug.selfplay summarize $P1 $P2 --compare open layers --out $P2 > experiments/selfplay/veil9-p12-open-vs-layers.txt
python docs/superpowers/specs/calibration-data/selfplay/window_stats.py $P1 $P2 --compare open layers --out $P2 > experiments/selfplay/veil9-p12-window.txt
```
Expected: `summarize` が止まらない（アームの指紋・任せる先の指紋・対局条件・計測の基準が同じ）。止まったら中身の違いを報告する。

- [ ] **Step 2: 9-1 の投了なし**（`layers`・`open` × 10 対・約 0.4 時間。10 対は 3 段位 × 2 色の倍数でないので偏りの警告が出る＝spec §16.3 どおり）

```bash
source experiments/selfplay/veil-open-arms.sh && python -m katrain_debug.selfplay run --size 9 --pairs 10 --no-resign --config "$CFG" --hp-audit rank_9d --label veil9-p1-nores --arm "$LAYERS9" --arm "$OPEN9" > experiments/selfplay/veil9-p1-nores.console.txt 2>&1
```
投了ありの 9-1 と同じ seed（1000〜1009）の `layers`・`open` の自分の一致率・手数と比べる（投了が一致率を左右していないか）。対局条件（投了モデル）が
違うので `summarize P1 P1-nores` は止まる＝games.jsonl を直接読む:

```bash
P1=$(ls -d experiments/selfplay/*_veil9-p1) && NR=$(ls -d experiments/selfplay/*_veil9-p1-nores) && python - "$P1" "$NR" <<'EOF' | tee experiments/selfplay/veil9-p1-nores-vs-p1.txt
import json, statistics, sys

def load(d):
    with open(d + "/games.jsonl", encoding="utf-8") as f:
        return {(r["arm"], r["seed"]): r for r in map(json.loads, f) if r["result"] != "aborted"}

res, nores = load(sys.argv[1]), load(sys.argv[2])
for arm in ("layers", "open"):
    seeds = [s for s in range(1000, 1010) if (arm, s) in res and (arm, s) in nores]
    for label, runs in (("resign", res), ("no-resign", nores)):
        own = [runs[arm, s]["own_top1"] for s in seeds if runs[arm, s]["own_top1"] is not None]
        moves = [runs[arm, s]["n_moves"] for s in seeds]
        print(f"{arm:<7} {label:<9} seeds={len(seeds)} own_top1_mean={statistics.fmean(own):.3f} n_moves_median={statistics.median(moves)}")
    diffs = [nores[arm, s]["own_top1"] - res[arm, s]["own_top1"] for s in seeds
             if nores[arm, s]["own_top1"] is not None and res[arm, s]["own_top1"] is not None]
    print(f"{arm:<7} no-resign - resign own_top1 (same seed): mean={statistics.fmean(diffs):+.3f} n={len(diffs)}")
EOF
```

### C7: 9-3 定跡を知る相手・9-4 強い相手・9-5 接戦ストレス（spec §16.3）

- [ ] **Step 1: 9-3**（3 アーム × 24 対・約 1 時間）

```bash
source experiments/selfplay/veil-open-arms.sh && python -m katrain_debug.selfplay run --size 9 --pairs 24 --book-moves 24 --config "$CFG" --hp-audit rank_9d --label veil9-p3book --arm "$LAYERS9" --arm "$OPEN9" --arm "$ENIGMA9" > experiments/selfplay/veil9-p3book.console.txt 2>&1
```
集計（run の要約＝先頭のアーム `layers` と各アームの差は先に `summary-vs-layers.*` に写す。window_stats の `book open:` / `book layers:` /
`book enigma:` の行が定跡の出口の理由と手数）:

```bash
P3=$(ls -d experiments/selfplay/*_veil9-p3book) && cp $P3/summary.json $P3/summary-vs-layers.json && cp $P3/summary.txt $P3/summary-vs-layers.txt
python -m katrain_debug.selfplay summarize $P3 --compare open layers --out $P3 > experiments/selfplay/veil9-p3book-open-vs-layers.txt
python docs/superpowers/specs/calibration-data/selfplay/window_stats.py $P3 --compare open layers > experiments/selfplay/veil9-p3book-window.txt
```

- [ ] **Step 2: 9-4**（HumanStyle 9段・コミ互角・3 アーム × 20 対・約 1.3 時間）

```bash
source experiments/selfplay/veil-open-arms.sh && python -m katrain_debug.selfplay run --size 9 --pairs 20 --opponent "$STRONG" --config "$CFG" --hp-audit rank_9d --label veil9-p4strong --arm "$LAYERS9" --arm "$OPEN9" --arm "$ENIGMA9" > experiments/selfplay/veil9-p4strong.console.txt 2>&1
```
集計（window_stats の窓の中の ≥2目 / ≥6目・W 手目のリード）:

```bash
P4=$(ls -d experiments/selfplay/*_veil9-p4strong) && cp $P4/summary.json $P4/summary-vs-layers.json && cp $P4/summary.txt $P4/summary-vs-layers.txt
python -m katrain_debug.selfplay summarize $P4 --compare open layers --out $P4 > experiments/selfplay/veil9-p4strong-open-vs-layers.txt
python docs/superpowers/specs/calibration-data/selfplay/window_stats.py $P4 --compare open layers > experiments/selfplay/veil9-p4strong-window.txt
```

- [ ] **Step 3: 9-5**（HumanStyle 9段・`--komi-shift 2`・3 アーム × 20 対・約 1.4 時間）

```bash
source experiments/selfplay/veil-open-arms.sh && python -m katrain_debug.selfplay run --size 9 --pairs 20 --komi-shift 2 --opponent "$STRONG" --config "$CFG" --hp-audit rank_9d --label veil9-p5stress --arm "$LAYERS9" --arm "$OPEN9" --arm "$ENIGMA9" > experiments/selfplay/veil9-p5stress.console.txt 2>&1
```
（AI が不利なので窓の中の難解＋は勝率フロアでほぼ最善手を打つ＝窓の後の安全を見る。spec §16.3。）集計:

```bash
P5=$(ls -d experiments/selfplay/*_veil9-p5stress) && cp $P5/summary.json $P5/summary-vs-layers.json && cp $P5/summary.txt $P5/summary-vs-layers.txt
python -m katrain_debug.selfplay summarize $P5 --compare open layers --out $P5 > experiments/selfplay/veil9-p5stress-open-vs-layers.txt
python docs/superpowers/specs/calibration-data/selfplay/window_stats.py $P5 --compare open layers > experiments/selfplay/veil9-p5stress-window.txt
```

- [ ] **Step 4: 9路の中間報告**: C9 の表の形で 9路の結果をまとめてユーザーに見せ、13路（C8・約 6 時間）に進むか確かめる。

### C8: 13路の段階（spec §16.4・合わせて約 6 時間）

- [ ] **Step 1: 13-1**（`local`・`local_open` × 24 対）

```bash
source experiments/selfplay/veil-open-arms.sh && python -m katrain_debug.selfplay run --size 13 --pairs 24 --opp-pool docs/superpowers/specs/calibration-data/selfplay/opponent_pool_13.json --config "$CFG" --hp-audit rank_9d --label veil13-open-p1 --arm "$LOCAL13" --arm "$LOCALOPEN13" > experiments/selfplay/veil13-open-p1.console.txt 2>&1
```
- [ ] **Step 2: 13-2**（`--book-moves 40`・3 アーム × 24 対）

```bash
source experiments/selfplay/veil-open-arms.sh && python -m katrain_debug.selfplay run --size 13 --pairs 24 --book-moves 40 --opp-pool docs/superpowers/specs/calibration-data/selfplay/opponent_pool_13.json --config "$CFG" --hp-audit rank_9d --label veil13-open-p2book --arm "$LOCAL13" --arm "$LOCALOPEN13" --arm "$ENIGMA13" > experiments/selfplay/veil13-open-p2book.console.txt 2>&1
```
- [ ] **Step 3: 13-3**（HumanStyle 9段・コミ互角・3 アーム × 20 対）

```bash
source experiments/selfplay/veil-open-arms.sh && python -m katrain_debug.selfplay run --size 13 --pairs 20 --opponent "$STRONG" --config "$CFG" --hp-audit rank_9d --label veil13-open-p3strong --arm "$LOCAL13" --arm "$LOCALOPEN13" --arm "$ENIGMA13" > experiments/selfplay/veil13-open-p3strong.console.txt 2>&1
```
- [ ] **Step 4: 13-4**（段階3b と同じ＝`--komi-shift 4`・HumanStyle 9段・2 アーム × 20 対）

```bash
source experiments/selfplay/veil-open-arms.sh && python -m katrain_debug.selfplay run --size 13 --pairs 20 --komi-shift 4 --opponent "$STRONG" --config "$CFG" --hp-audit rank_9d --label veil13-open-p4stress --arm "$LOCAL13" --arm "$LOCALOPEN13" > experiments/selfplay/veil13-open-p4stress.console.txt 2>&1
```
- [ ] **Step 5: 集計**: 各段で run の要約（先頭のアーム `local` と各アームの差）を `summary-vs-local.*` に写してから `summarize --compare local_open local`
と window_stats（W は 13路の既定 30。13路の窓の最初の 11 手は HumanStyle 9段の手＝spec §15.2）。

```bash
for L in veil13-open-p1 veil13-open-p2book veil13-open-p3strong veil13-open-p4stress; do
  RUN=$(ls -d experiments/selfplay/*_$L) && cp $RUN/summary.json $RUN/summary-vs-local.json && cp $RUN/summary.txt $RUN/summary-vs-local.txt
  python -m katrain_debug.selfplay summarize $RUN --compare local_open local --out $RUN > experiments/selfplay/$L-open-vs-local.txt
  python docs/superpowers/specs/calibration-data/selfplay/window_stats.py $RUN --compare local_open local > experiments/selfplay/$L-window.txt
done
```

### C9: 記録（campaign md・spec・INDEX・実行ディレクトリの移動）

- [ ] **Step 1: `docs/superpowers/specs/calibration-data/selfplay/veil9-campaign.md` を作る**（Write。CRLF にする）。節と中身:
  - `# 一致率ひかえめ（韜晦）9路と序盤の研究外しの自己対局キャンペーン（<日付>）`・spec §16 へのリンク。
  - `## 実行条件`: git HEAD（run.json の `git.head`・dirty）・エンジン（run.json の `engine`）・設定の写し `veil-open-config.json` の
    `ai:enigma9plus` の全文（任せる先と `enigma` アーム）・相手プール（`opponent_pool_9.json` の段位・τ）・投了モデル（length・recon_9）・
    アーム（C1 Step 5 の 6 文字列を貼る）・段ごとの出力ディレクトリ・局数・所要（console の最初と最後の時刻）・aborted・`WARN integrity` の有無。
  - `## 準備`: recon_9（16 局・直したこと）・事後解析・`CALIB_TARGETS_9`（calib_targets の出力の最後の3行）・校正（段位ごとの表と選んだプール・
    ずれ・手数の中央値 vs 48）。
  - `## 9-1 校正`: 6 アームの表（summary.txt の `## arms` の行＝W-L-J・自分の一致率の局平均と区間・相手・損失・≥6目/局・flip/局・リードの中央値・
    手数・外した手の 9段 hp の中央値・p95 秒）と spec との差（`summary-vs-spec.txt` の paired diff）。
  - `## 研究外しの効果（9-1＋9-2・48 対）`: `open − layers` の paired diff（own_top1・opp_top1・flip_moves・win・ge6）と window_stats の表と対の差
    （own_in・own_out・opp_in・opp_out・lead_at_w）・窓の後の u と paid。
  - `## 投了なし（9-1 の 10 対）`・`## 9-3 定跡を知る相手`（定跡の出口の手数と理由・`open` / `layers` / `enigma` の一致率と勝ち以外）・
    `## 9-4 強い相手`（窓の中の ≥2 / ≥6目・W 手目のリードの分布・勝ち以外）・`## 9-5 接戦ストレス`（勝・負・持碁と flip）。
  - `## 見るものと決め方（spec §16.5）`: 目安（`open − layers` の自分の一致率の 97.5% 区間が 0 をまたがずに下がり、9-3・9-4 で `open` の勝ち以外の数が
    同じ相手の `enigma` 以下なら採る候補）に照らした結果を1段落。持碁も勝ち以外に数える。
  - `## ユーザーの決定（<日付>）`: C10 で書く（最初は「未定」と書かない＝C10 まで節を作らない）。
- [ ] **Step 2: 写し**: `docs/superpowers/specs/calibration-data/selfplay/veil9-campaign/` に各段の `run.json`・`summary.{json,txt}`（と
  `summary-vs-*.{json,txt}`）・`window_stats.json` を `veil9-<段>-<ファイル名>` の名前で写す（veil13-campaign/ と同じ形）。
- [ ] **Step 3: 13路**: `veil13-campaign.md` の末尾に `## 序盤の研究外し（spec §15・§16.4）の計測（<日付>）` の節（パッチの `append`）:
  実行条件（`LOCAL13` / `LOCALOPEN13` の文字列・出力ディレクトリ・所要）・13-1〜13-4 の表と `local_open − local` の対の差・window_stats（W 30）・
  勝ち以外の数。写しは `veil13-campaign/veil13-open-<段>-*`。
- [ ] **Step 4: spec と INDEX**（パッチ）: 先に `<scratchpad>/spec_16_6.md` を Write で作る＝空行1つの後に `### 16.6 計測の結果（<日付>）` の節
  （段ごとに1〜2行の要点と campaign md へのリンク `calibration-data/selfplay/veil9-campaign.md`・13路は `veil13-campaign.md` の節。数値は campaign md の表から写す）。
  §16.5 は spec の最後の節なので、§16.6 は末尾に足す。次にパッチスクリプト `<scratchpad>/patch_9x9_c9.py`（worktree のルートで実行）:

```python
import datetime
import json
import sys

sys.path.insert(0, r"<scratchpad の実パス>")
from crlf_patch import append, patch  # noqa: E402

SPEC = "docs/superpowers/specs/2026-09-23-veil-strategy-design.md"
INDEX = "docs/superpowers/specs/INDEX.md"
SECTION = r"<scratchpad の実パス>/spec_16_6.md"
DATE = datetime.date.today().isoformat()
with open("docs/superpowers/specs/calibration-data/selfplay/recon_9/summary.json", encoding="utf-8") as f:
    games = [s["game"] for s in json.load(f)]
n = len(games)  # C2 で確定した局数（2026-09-26 のログでは 16）
if "game_20260919_223015" in games:
    fixed = "`0919_223015` は監視の外の相手のパスを挟んで直せた"
else:
    fixed = "`0919_223015` は直せずに除いた"
patch(SPEC, [
    (
        "・9路の校正と研究外しの効果の計測は §16（📝 計画）",
        f"・9路の校正と研究外しの効果の計測は §16（計測済み〈{DATE}〉・結果 §16.6）",
        1,
    ),
    (
        "## 16. 9路の校正と、序盤の研究外しの効果の計測（2026-09-26 追記・📝 計画）\n",
        f"## 16. 9路の校正と、序盤の研究外しの効果の計測（2026-09-26 追記・✅ 計測済み〈{DATE}〉）\n",
        1,
    ),
    ("   - 9路の出力先は `recon_9/`（難解＋9路 15 局）。", f"   - 9路の出力先は `recon_9/`（難解＋9路 {n} 局・{fixed}）。", 1),
    ("比べる先は難解＋9路の 15 局）。", f"比べる先は難解＋9路の {n} 局）。", 1),
])
with open(SECTION, encoding="utf-8") as f:
    append(SPEC, f.read())
patch(INDEX, [
    (
        "§16 9路の校正と研究外しの計測（📝 計画）",
        f"§16 9路の校正と研究外しの計測（{DATE}・結果 calibration-data/selfplay/veil9-campaign.md）",
        1,
    ),
])
print(DATE, n, fixed)
```
Run: `python <scratchpad>/patch_9x9_c9.py` → `file docs/superpowers/specs/2026-09-23-veil-strategy-design.md docs/superpowers/specs/INDEX.md`
Expected: 日付・局数・直したことの1行、どちらも `with CRLF line terminators`（spec の「15 局」の2か所は §16.1 の数え方〈難解＋16局〉と合わせて実数になる）。
- [ ] **Step 5: 実行ディレクトリの移動**: worktree の `experiments/selfplay/` の C2〜C8 のディレクトリとコンソール・`veil-open-config.json`・
  `veil-open-arms.sh` を本体の `C:/Users/iwaki/Documents/katrain-1.17.1.1/katrain-1.17.1.1/experiments/selfplay/` に `mv` する（同じ名前が無いことを先に確かめる）。
  campaign md の出力ディレクトリの書き方は `experiments/selfplay/<dir>`（リポジトリ相対）なので移しても変わらない。
- [ ] **Step 6: コミット**

```bash
git add docs/superpowers/specs/calibration-data/selfplay/veil9-campaign.md docs/superpowers/specs/calibration-data/selfplay/veil9-campaign docs/superpowers/specs/calibration-data/selfplay/veil13-campaign.md docs/superpowers/specs/calibration-data/selfplay/veil13-campaign docs/superpowers/specs/2026-09-23-veil-strategy-design.md docs/superpowers/specs/INDEX.md
git commit -m "docs(veil): 9路の校正と序盤の研究外しの計測の結果を記録する（spec §16）" -m "Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>"
```

### C10: ユーザーの決定と仕上げ

- [ ] **Step 1: 結果を見せて決めてもらう**（spec §16.5。コードもユーザー設定もここでは変えない）:
  1. 9路の既定値（`loose9` を 9路の既定にするか・失着 / forced の層の既定）と、9路のローカル設定（層・研究外しを ON にするか・`open_moves` の値）。
     9路の既定を後で変えたら研究外しの差（`open` − `layers`）は測り直しになる（両アームは `loose9` の上）と添える。
  2. 13路のローカル設定に研究外し（`veil13_open_moves` 30）を足すか。
  3. 1 手目からの研究外しも見たいか（13路の `enigma13plus_opening_humanstyle_moves` を 0 にした設定の写しで別の実行を足す＝spec §16.4。ライブの設定は変えない）。
  4. 実戦の確認（spec §16.5 の「実戦」）: 研究外しを ON にした実戦は各 10 局以上・実戦の直後にログを `experiments/selfplay/realgames-9x9-logs/` に退避する。
     一致率ひかえめ9路の実戦（数局たまったら・KaTrain を止めて）は、難解＋の局と同じ置き場から `--strategy Veil9Strategy` で分けて復元し
     （`recon_9/` に混ぜない。C2 の既定〈難解＋の局だけ〉で走らせ直しても一致率ひかえめ9路の局は入らない）、事後 2500v で解析して、`window_stats.py --recon`
     で難解＋9路の局（`recon_9/`）と比べる（相手の最善手との一致率・序盤で相手が最善手を続けた手数〈`streak`〉・窓の後の相手の一致率〈`opp out`〉）:

```bash
EXP="C:/Users/iwaki/Documents/katrain-1.17.1.1/katrain-1.17.1.1/experiments/selfplay"  # 本体の experiments（C9 Step 5 で設定の写しも移した）
D=docs/superpowers/specs/calibration-data/selfplay
python $D/recon_logs.py --size 9 --strategy Veil9Strategy --log-dir "$EXP/realgames-9x9-logs" --out $D/recon_9_veil | grep -E "^game_|setup|inserted|!!" | grep -v "skipped: strategy"
python $D/offline_report.py --dir $D/recon_9_veil --config "$EXP/veil-open-config.json" > "$EXP/offline9veil.console.txt" 2>&1
python $D/window_stats.py --recon $D/recon_9_veil $D/recon_9 --out "$EXP" | tee "$EXP/recon9-veil-vs-enigma-window.txt"
```
     （リポジトリのルート〈マージの後なら本体・前なら worktree〉で走らせる。`offline_report.py` は数分以上かかるので `run_in_background: true`。W は 9路の既定 12。
     記録は `veil9-campaign.md` の `## 実戦` の節と spec §16.6 に1行。）
- [ ] **Step 2: 決定を記録する**: `veil9-campaign.md`（と 13路なら `veil13-campaign.md`）に `## ユーザーの決定（<日付>）` の節、spec §16.6 に1行。コミット。
- [ ] **Step 3: 既定値やローカル設定を変えると決まったら**: 既定値（`SETTING_DEFAULTS`・両 config.json・マニュアル・ルール文書）の変更は別の計画
  （Plan A と同じ手順・`SPEC_DEFAULTS` の凍結の扱いを含む）にする。ユーザー設定の変更は、**ユーザーの承認の後に、KaTrain を止めて、メインセッションで**
  次の手順で書く: (1) `C:/Users/iwaki/.katrain/config.json` の写しを `experiments/selfplay/sdd-records/opening/config.before-<変更名>.json` に置く、(2) python で JSON を読み、決まったキーだけを変え（`ai:veil9` / `ai:veil13` / `ai:veil19` に `<prefix>_open_moves` が無ければ 0 で足す）、書いた後に読み直して、変えたキー以外のすべての節とキーが写しと同じことを確かめる、(3) 結果をユーザーに報告する。
- [ ] **Step 4: ブランチの仕上げ**: ブランチ全体のレビュー（superpowers:requesting-code-review・Plan A と Plan B の範囲）→ 全体のテスト →
  superpowers:finishing-a-development-branch（master へのマージはユーザーに確かめてから）。

---

## spec のあいまいさと、この計画での決め方

1. **「CALIB_BINS を盤サイズ別にする」（§16.2 手順3）**: `game_report` の `depth_filter` は盤面積の割合を受け取り `ceil(割合 × 盤面積)` で手数に直すので、
   同じ割合（24/169・85/169）のままで 9路はすでに 12 / 41 手になる（既存のテスト `test_calibration_bins_match_game_report_boundaries` も 9路の 11 / 12 を
   確かめている）。→ `CALIB_BINS` は割合のまま変えず、盤別の境目の手数を `calib_bin_moves(size)` で出して、スクリプト（offline_report・calib_targets）と
   窓の指標が同じ境目を使う形にした。13路の値（24 / 85）と、全手数で `depth_bin` と一致することをテストで固定する。
2. **recon_9 の局数（spec は「15 局」）**: 17 本のログのうち持碁9路 1 本を除くと難解＋は 16 局。spec の「`0919_223015` は…直せなければ除く」は直せた
   （監視の外の相手のパスを 28 手目に挟むと全局で手番の食い違いが 0・本物の `Game` で打ち直せる）ので 16 局にした。直せない局が出たら C2 Step 1 で除き、
   件数の凍結（C2 Step 3・C3 のテスト）を実数に直す。spec の「15 局」の2か所（§16.2 手順1・§16.5）は C9 Step 4 のパッチが C2 で確定した局数
   （`recon_9/summary.json` の局の数）に直す。Task 10 のハーネスの spec の文は局数を書かずに韜晦の spec §16.2 手順1 を指す。
3. **校正の AI の「実戦の多数派の設定」（§16.2 手順4）**: spec は多数派の 7 局を `max_loss` 1.6・`target_score` 0.2・`gamble_until_move` 20 で特定しているが、
   その 7 局は今のユーザー設定と他のキー（`gamble_min_winrate` 0.25・`large_lead_max_loss` 6.0・`overdraft_probes` 6・`overdraft_deficit` 2.0〈7 局中 6 局〉）
   でも違う。ハーネスと実戦のずれ（`harness_drift_ai`）をその 7 局と比べるので、7 局の設定を再現するため、7 局で多数の値のうち設定の写しと違うキーを
   すべて上書きにした（C4 Step 1 のスクリプトが出す。3 キーだけにしたいときはそのスクリプトの出力の代わりに 3 キーを渡せばよい）。
   AI 側の目標 `ai_mean` は同じ 7 局の局平均（`calib_targets.py --ai-where`）、相手側の目標は 16 局。
4. **3 局の `aim_jigo` true（`0919_095719`・`095858`・`100100` の最初の設定）**: spec は除く局に挙げていない。相手の一致率・損失の目標（16 局）と投了の手数に
   入れたまま、summary の `settings` に残して campaign md に書く（AI 側の目標は多数派の 7 局なので影響しない）。
5. **設定の固定（§16.2 手順9）の「写し」**: キャンペーンの写し（`experiments/selfplay/veil-open-config.json`・C1）を全実行に `--config` で渡すことと、
   ハーネスが実行ごとに `config-snapshot.json` を置いて `--resume` がそれを読むことの両方にした（前者で段の間の設定が揃い 9-1 と 9-2 を合算でき、後者で
   再開の途中の設定の変化を防ぐ）。合算（`summarize`）は写しの sha256 を比べない（無関係の節の違いで止めない）。比べるのはアームの指紋と任せる先の指紋。
6. **任せる先の指紋の中身**: 難解＋の節（`ai/<戦略キー>`）の生の dict（韜晦が `dict(config(...) or {})` で渡すものと同じ）。節が無ければ `{}`（難解＋の
   コードの既定値）で、`settings_source` に書く。`open_moves` 0 と韜晦以外のアームは None（古い run.json と同じ扱い）。
7. **定跡を知る相手の「手数 < B」**: 相手の手番の局面の手数（それまでに打たれた手の数）＜ B の間だけ最善手＝相手が打つ手の番号は B 以下。B に届いたら
   `book_exit_reason` は `limit`・`book_exit_depth` は B。AI の手の `points_lost` が無い（解析が無い）手も「定跡の外」に数える（控えめ）。
   相手が strategy（HumanStyle 9段など）のときは使えない（spec は「通常の相手（プールの humanSL）を包む」）。
8. **窓の指標の W と境目**: `depth <= W` を窓の中（moves.jsonl の depth は打った手のノードの手数＝打つ手の番号）。Plan A の窓（index = 打つ前の手数 +
   置き石 < `open_moves`）と同じ手番になる（ハーネスは置き石なし）。9-3・13-2 の定跡の出口は games.jsonl の `opponent_stats` から読む（moves.jsonl には無い）。
9. **9路の lead モデル**: spec は「lead モデルは 9路では使わない」。実戦の最終リードの標本が 9路に無いので、9路で `--resign-model lead` を選ぶには
   `--resign-lead LO:HI` を要る形にした（既存のテスト `test_runs_with_different_resign_models_are_not_merged` は 9路で lead を使うので範囲を足した）。
10. **9-1 の「投了なし 10 対」の seed**: 既定の seed 1000〜1009（9-1 の同じ seed の投了ありの局と比べられる）。10 対は 3 段位 × 2 色の倍数でないので
    偏りの警告が出る（spec §16.3 が 10 対と書いている＝そのまま）。
11. **調査のスクリプト（§16.2 手順8）**: 4 本（`parse9.py`・`table9.py`・`e9parse2.py`・`e9an.py`）は `enigma9_log_stats.py` 1 本にまとめ、§16.1 の値を
    再現することを確かめた。相手 BOT の一致は元のスクリプトが残っていないので `opp_bot_match9.py` を書き直した（同じ値）。本の読み取り（`book9.py`・
    `book9deep.py`）は web から取るコードなので中身を変えずに `book9/` に写すだけにした（取ったデータはコミットしない・テストで走らせない）。
12. **一致率ひかえめ9路の実戦のログの置き場（§16.2 手順1）**: spec は一致率ひかえめ9路の実戦を `recon_9_veil/` に分けて「混ぜない」と書きつつ、ログは
    難解＋の局と同じ `experiments/selfplay/realgames-9x9-logs/` に退避する。→ 置き場は分けず、`recon_logs.py` が 9路の既定で難解＋9路の局
    （ログの最初の `Generating move using` が `Enigma9PlusStrategy`）だけを復元し、`--strategy Veil9Strategy` で一致率ひかえめ9路だけを `recon_9_veil/` に出す
    形にした（C2 を走らせ直しても `recon_9/`・投了の手数・`CALIB_TARGETS_9` は変わらない）。`enigma9_log_stats.py` も最初の手番が難解＋9路でないログを数えない。
    2026-09-26 の 17 本はすべて最初が `Enigma9PlusStrategy`（持碁9路の局も最初の1手番は難解＋・`EXCLUDE_9` で除く）なので、§16.1 の値と C2 の出力は変わらない。
13. **実戦の指標（§16.5 の「実戦」）の道具**: spec は `recon_9_veil/` の事後解析で、相手の最善手との一致率・序盤で相手が最善手を続けた手数・窓の後の相手の
    一致率を見て難解＋9路の局と比べるとするが、`window_stats.py`（§16.2 手順7）はハーネスの実行ディレクトリしか読まない。→ `window_stats.py --recon DIR ...`
    を足した（report_game_*.json の rows から、窓の中・後の自分と相手の一致率と、相手の最初の手から最善手が続いた相手の手の数 `opp_streak`。DIR ごとに別の表）。
    窓 W はハーネスと同じ既定（9路 12）で、実戦の手数（置き石の初手を初手に戻した後の depth）で数える。
