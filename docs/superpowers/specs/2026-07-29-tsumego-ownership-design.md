# 詰碁キャプチャ: ownership を目的関数にした着手選択（ai:tsumego）設計書

日付: 2026-07-29
ステータス: 承認済み

## 目的

詰碁キャプチャの黒番AIが「盤全体の目数が最良の手」ではなく「**対象石群の死活を最も自分に有利に動かす手**」を
選ぶようにする。詰碁の正解判定は対象石群の死活で決まるのに、KataGo の目的関数は盤全体の目数であり、
この不一致が誤答の主因である。

## 背景: 目数ベースでは解けないことの実測

先行する2ブランチ（枠の配線バグ修正 `ee0376c`、枠なしモード `2049937`）で盤面とリージョンの
問題は解消したが、誤答は残った。原因は目的関数の不一致で、**盤面や枠をどう調整しても解けない**
構造的ジレンマとして現れる。

| ケース | 状態 | root scoreLead / 勝率 | 1位 | 正解手 |
|---|---|---|---|---|
| case B | 枠なし | +37.72 / **1.000** | A5 | B4 +1.84 |
| case B | 枠なし + komi 44.5 | +0.98 / 0.712 | A5 | B4 +1.23 |
| case C | 枠なし | −53.29 / **0.000** | J1 | H1 +2.24 |
| case C | 枠なし + komi −46.5 | +0.34 / 0.464 | **G7（空き地）** | H1 +4.22 (2 visits) |

- **飽和させる**と詰碁の成否が勝敗に影響せず、KataGo は死活を読む動機を持たない
- **バランスを取る**と今度はリージョン内の空き地の手が価値を持つ

枠はこの両方を「盤を埋める＋バランスを取る」で同時に解こうとしていた設計であり、
枠なし＋リージョンだけではジレンマを解けない。**目的関数を変えるのが本筋**。

## ownership が信号になることの実測

候補手ごとの ownership（`includeMovesOwnership`）で、目数が取り逃す差が出る。

### case C（正解 H1 はコウ、AI は J1 を選び黒が死んだ）

```
move   ptLost  visits   B_own（黒石群のownership平均）
J1     +1.85     897   -0.515   ← AIの選択
H1     +2.03     882   -0.129   ← 正解
```

目数では J1 が上、**ownership では H1 が明確に上**。しかも H1 の値は 0 付近で、
**コウ＝どちらのものとも決まっていない状態が中間値として現れる**。

### case B（正解 B4、AI は A5 を選び白が生きた）

連ごとに見ると、攻撃対象の白 G7 は ROOT / A5 / B4 いずれも −0.76 で動かない。
しかし**全連の変化を合計すると B4 が勝つ**（サイズ加重・黒視点）:

```
連                ROOT    A5      B4      B4の寄与
G0(B,1) B8       +0.95   +0.95   +0.92    -0.03
G3(B,1) D4       +0.44   +0.44   +0.01    -0.43
G4(B,1) B3       -0.61   -0.61   -0.12    +0.49
G5(B,1) A2       -0.65   -0.65   -0.43    +0.22
G6(B,1) B1       -0.70   -0.70   -0.54    +0.16
G7(W,2) B5 C5    -0.76   -0.76   -0.76     0
G8(W,6)          -0.77   -0.78   -0.66    +0.66
G9(W,1) F1       -0.42   -0.42   -0.39    +0.03
                        合計:   A5 = -0.06   B4 = +1.10
```

なお同じ局面で D5 は gain +3.49 と最大だが pointsLost が +4.87 と大きい。
**目数のガードが必要**なことを示している。

## アプローチ

採用: **目数ガードで大損の手を弾き、残りを ownership gain で順位付ける**。

不採用:

- **対象石群を事前に同定して、その連だけを見る** — case B の実測では ROOT で最も
  中間値なのは無関係な単独の黒石 D4(+0.44) で、攻撃対象の白 G7(−0.76) ではなかった。
  同定を誤ると完全に外れるため、全連の変化を合計するほうが頑健
- **ownership gain だけで選ぶ（目数ガードなし）** — case B で 4.87目損の D5 が選ばれる
- **目数と ownership の加重和** — 重みの意味づけが恣意的で調整が難しい。
  ガード（ハード制約）＋ gain（目的関数）のほうが各パラメータの役割が明確

## 設計

### 1. 着手選択

リージョン限定解析が完了した候補手 `cn.candidate_moves` に対して:

```
1. 目数ガード
   best = min(pointsLost)
   候補 = { m | m.pointsLost <= best + max_points_behind }   （既定 max_points_behind = 2.0）

2. ownership gain（手番側から見て有利な向きを正とする）
   sign = +1 if 手番 == B else -1
   gain(m) = Σ_{盤上の全石 s} sign × ( own_after(m, s) − own_root(s) )

3. gain 最大の手を選ぶ。同点は pointsLost が小さい方
```

石ごとに合計するので、大きい連の死活ほど重く効く（連の平均ではなくサイズ加重）。
空き地の手はどの石の ownership も動かさないので gain ≈ 0 となり自動的に沈む。

実測での検算:

| ケース | 目数ガード通過 | gain 最大 | 結果 |
|---|---|---|---|
| case C | J1(+1.85), H1(+2.03), L1(+2.24), K1(+3.13) 等 | **H1 +1.61**（J1 は −1.59） | 正解 |
| case B | A5(−0.08), B4(+1.70)。D5(+4.87) は除外 | **B4 +1.10**（A5 は −0.06） | 正解 |

### 2. ownership データの取得

`engine.py:457-458` は `includeOwnership` / `includeMovesOwnership` を
`self.config["_enable_ownership"]` で制御している。ユーザーのローカル設定では
現在 `false` なので **`true` にする必要がある**（パッケージ側 `katrain/config.json` は既に `true`）。

候補手ごとの ownership は `cn.analysis["moves"][gtp]["ownership"]` に入る。
`analysis_dumps` が per-move ownership を捨てるのは SGF へ保存する時だけで、
メモリ上の解析結果には残る（`game_node.py:26-28`）。

ROOT の ownership は `cn.analysis["ownership"]`（= `cn.ownership`）。

配列→盤座標の変換は既存の `var_to_grid(array, (size_x, size_y))` を使う。
戻り値は `grid[y][x]` で y は下origin。`Move.coords` が `(x, y)` なので `grid[y][x]` で引ける。

### 3. 実装場所

`ai.py` に既存の `OwnershipBaseStrategy` 系（`AI_SIMPLE_OWNERSHIP` / `AI_SETTLE_STONES`）が
あり、per-move ownership を使う前例になっている。同じパターンで新戦略を追加する:

- `constants.py`: `AI_TSUMEGO = "ai:tsumego"` を追加。どのリスト（`AI_STRATEGIES_ENGINE` /
  `AI_STRATEGIES_RECOMMENDED_ORDER` / `AI_STRENGTH` 等）に登録するかは、既存の
  `AI_SIMPLE_OWNERSHIP` / `AI_SETTLE_STONES` の登録先に倣う（両者で登録先が異なるため、
  実装時に既存コードを確認して合わせる）。手順は `.claude/rules/ai-settings-gui.md` に従う
- `ai.py`: `@register_strategy(AI_TSUMEGO)` を付けた `TsumegoOwnershipStrategy` を追加
- `config.json`（パッケージ側とユーザーローカルの両方）: `ai/ai:tsumego` の設定に
  `max_points_behind` を置く
- `__main__.py`: キャプチャ適用時の黒番を `AI_DEFAULT` から `AI_TSUMEGO` に変更

### 4. フォールバック

ownership が取れない場合（`_enable_ownership` が false、古い KataGo 等）は
`candidate_moves[0]`（= 現行 `ai:default` と同じ挙動）にフォールバックし、
その旨を `OUTPUT_INFO` でログに出す。無言で劣化させない。

## 検証

### ユニットテスト

`ai.py` の戦略は Kivy 非依存で、`katrain_debug` のスタブ経由でテストできる。
ownership 配列を固定値で与えた偽の解析結果に対して:

- gain 計算が手番の符号を正しく扱う（黒番と白番で符号が反転する）
- 目数ガードが `best + max_points_behind` を超える手を除外する
  （case B の D5 相当: gain 最大だが pointsLost 超過 → 選ばれない）
- gain 同点時に pointsLost の小さい方が選ばれる
- ownership が無い場合に `candidate_moves[0]` へフォールバックする
- `var_to_grid` の座標変換が `Move.coords` と一致する（既知の石の ownership を引けること）

### 実機検証

case A / B / C を含む問題群で `ai:default` と比較する。判定は
「アプリが正解と判定したか」で行う（コウ・セキに持ち込む別解も正解）。

## 限界（既知・未解決）

- **`max_points_behind = 2.0` は case B / C の2ケースからの推定値**。問題群で調整が要る。
  小さすぎると正解手を弾き（case C の H1 は +2.03）、大きすぎると大損の手が入る（case B の D5 は +4.87）
- gain は全連の合計なので、詰碁と無関係な連が同時に動く局面では信号が希釈される
- **KataGo の読み自体が誤っている問題は直らない**。case B は gain では正解に転んだが、
  攻撃対象の白 G7 の ownership は動いておらず、KataGo はこの白を「どちらにせよ生きている」と
  読んでいる。正解に転んだのは黒の死に石が蘇る副次効果によるもので、保証ではない
- 飽和時（勝率0/1）は KataGo の探索自体が甘くなり ownership の推定精度も落ちる。
  komi によるバランス調整の併用は有効な可能性があるが、本設計には含めない（別途評価する）

## 追記（2026-07-29）: gain 飽和による誤答と ε 同着タイブレーク

実戦で誤答が出た（13路・左上・黒番、正解 C12 に対し AI は D12 を選択）。
キャプチャ経路と同じクエリ（region A–J×5–13 / 1800 visits / wideRootNoise 0 / ownership）で再現した結果:

- **root の時点で対象の白石が全て死に判定**（W:A11,B11 / C10,D10 / E12,E11 / D8 がいずれも +0.99〜+1.00、
  root は勝率 98.5%・+3.1目）。ownership に伸びしろが無く、正解手を打っても gain は動かない
- 目数ガードを通る上位3手（C12 / D12 / E13）の gain は **±0.03 のノイズ**しか出ない
  （本物の死活差は同局面の C13/C11 で −3.8〜−4.6 と桁が違う）
- 同一局面を繰り返すと gain の大小が run ごとに入れ替わり、**選択がコイン投げになる**
  （実測 4 run: C12 が 3回、D12 が 1回。実戦は D12 を引いた）
- 一方 pointsLost は毎 run 安定して C12 が 0.6目良い。旧実装は `(gain, -pointsLost)` の
  タプル比較なので、gain が浮動小数で完全一致しない限り pointsLost は評価されなかった

**設計前提の限界**: 「正解手は ownership を有利に動かす」は root が未決着のとき（case C のコウ≒0）
だけ成立する。KataGo が既に勝ちを読み切っている局面（＝解答の途中では普通に起きる）では
正解手も gain 0 で、信号が消える。

**対処**: `gain_epsilon`（既定 0.3）を追加し、best gain との差が ε 以内の手は同着として
pointsLost で決める。ノイズ実測 ≤0.03、case B / case C の実信号は 1.16 / 3.20 なので
両ケースの挙動は変わらない。設定は `config.json` の `ai/ai:tsumego`（ai:tsumego は GUI 非表示のため
`AI_OPTION_VALUES` への登録は不要）。

**検証**（fresh engine を毎回起動して trial）: 探索が C12 を見つけた 8 trial で
**修正後は 8/8 が C12**、旧実装なら 4/8 が E13/D12（ノイズ勝ち）だった。

**併発していた第2の要因: 探索の飢餓（対処済み）**。KataGo の探索自体が半数以上の trial で崩れ、
C12 に 12〜29 visits しか振らないまま「+14〜+21目損」と誤評価する。この状態はどの選択則でも
救えない。原因は `wideRootNoise = 0.0`（「深掘り・ノイズなし」という当初設計）で、root の探索が
1手に集中して正解手が visits を貰えないこと。各設定 8 trial（fresh engine 毎回起動）の実測:

| region クエリ設定 | 旧選択則での正解 | ε 修正後の正解 | 追加コスト |
|---|---|---|---|
| 1800 visits / wRN 0.0（旧既定） | 1/8 | 3/8 | — |
| **1800 visits / wRN 0.04（採用）** | 3/8 | **7/8** | ほぼ 0 |
| 6000 visits / wRN 0.0 | — | 6/8 | +5秒/手 |
| 6000 visits / wRN 0.04 | — | 7/8 | +5秒/手 |

visits 増は wRN 0.04 の上に乗らない（7/8 で頭打ち）ため、visits は 1800 のまま
**wideRootNoise を 0.04 に変更**した（`tsumego_capture/region_wide_root_noise`、既定 0.04。
0.0 に戻せば旧挙動）。ε 修正と wRN 変更は**両方必要**で、同一サンプルで
旧コード 1/8 → ε のみ 3/8 → ε + wRN 0.04 で **7/8**。
残る 1/8 は正解手が候補にすら上がらない完全な読み落としで、打つ手がない。

## 追記2（2026-07-30）: コウが正解の問題と枠のコウダテ

別の実戦誤答（13路下辺、正解は D1 でコウに持ち込む手。AI は E2 を選び白が生きた）を再現したところ、
**ownership も目数も信号が正しかった**（＝選択則の問題ではない）。枠の側が壊れていた。

- ROOT で白の対象石は **−1.00（無条件生き）**、黒は **−23.7目 / 勝率 0.7%**
- 正解の手順（D1 E2 C2 D2 B1）を打つと **−26.2 / 勝率 0.01%** とさらに悪化する

原因は `put_ko_threat`。`ko_p`（config `frame_ko`）は**攻め方と守り方のどちらにコウダテ形を
与えるか**の切り替えで、`ko_p=False` では守り側にコウダテが渡る。正解がコウ止まりの問題では
黒にコウダテが無く白がコウに勝つ＝**白の無条件生き**になり、正解手が価値を失う。
KataGo の「白 −1.00」は誤読ではなく正しい読みだった。

同じコアから枠を張り直した実測:

| | root スコア（黒） | AI の選択 |
|---|---|---|
| case2（コウが正解）ko_p=False | **−23.0** | B1 / E2 ✗ |
| case2 ko_p=**True** | **+0.68** | **D1 ✓**（1800中1762visitsが D1、他は全て −18目以上） |
| case1（コウでない）ko_p=False | **+2.92** | 3手目 C12 ✓ |
| case1 ko_p=**True** | **+14.22** | 初手が E13 ✗ に悪化 |

**`frame_ko: true` を既定にするのは誤り**で、どちらが正しいかは問題ごとに異なる（＝その問題の
答えがコウかどうか。lizgoban が手動トグルにしている理由）。ただし**枠の root スコアが判定材料になる**:
枠は「攻め方成功 = `offence_to_win`(5) 目勝ち」に調整する設計なので、`|lead|` が 5 から遠いほど枠が壊れている。

### 採用した対処: 枠の自動選択

キャプチャ時に `frame_ko` とその反転の両方で枠を張り、短い解析（既定 400 visits）で root スコアを取り、
`frame_balance_distance = abs(abs(lead) - 5)` が小さい方を採用する（`pick_balanced_frame`）。

- 400 visits で十分に判別できる（case1: 2.18 vs 9.80 / case2: 19.02 vs 3.08）。実測コストは 2本で約 1.2 秒
- 両方の枠が同一（コウダテ形が置けない盤）なら試算をスキップ
- 解析が取れなければ設定値の枠にフォールバック
- 設定: `tsumego_capture/frame_ko_auto`（既定 true）、`frame_ko_trial_visits`（既定 400）

検証: 自動選択を通した end-to-end で **case1 は ko_p=False を選んで C12（正解）**、
**case2 は ko_p=True を選んで D1（正解）**。

**落とし穴（要注意）**: `grid_to_sgf` は `RU` を出力しない。`BaseGame` は未指定なら設定の
ルールを入れる（chinese）が、素の `GameNode` を直接解析すると engine 既定の **japanese** になり、
面積計算前提の枠のスコアが **25目規模でずれる**（実測: 同一盤面で −23.0 → +2.3 と符号ごと反転し、
枠の破綻を見逃す）。枠バランスを測るコード・診断スクリプトでは必ず `RU` を明示すること。

## 追記3（2026-07-30）: 1visit の手が gain 争いに勝つ

3例目の誤答（13路右上、正解 M7。AI は **J7＝−16.5目**を選択）。枠の自動選択は正しく働いており
（実戦の SGF は ko_p=True の枠と完全一致＝コウ問題として正しい枠が選ばれていた）、
枠も ownership も目数も正常だった。原因は候補手の **visits** にある。

KataGo は root の子手を最低1回は評価するので、**1visit の手が moveInfos に載る**。その手の
ownership とスコアは探索結果ではなく **NN の生評価1回**で、gain が実手の10〜100倍のノイズになる:

```
探索された手   M7(v1324) gain +0.002 / L9(v65) +0.007 / N10(v50) +0.059
1visit の手    N6(v1)    gain +0.550 / M13(v1) +1.188   ← これが勝つ
```

実測4 run 中1 run で 1visit の M13 が選ばれた（残り3 run は 1visit の手が候補に出ず正解）。
GUI に出ていた「J7 −16.5目/303visits」は**子ノードの解析が親の手に書き戻された値**
（`set_analysis` が親の move dict を child の rootInfo で更新する）で、決定時の値ではない。

**対処**: `min_visits`（既定 10）未満の候補を除外する。目数ガードより**前**に落とすのは、
1visit の楽観的なスコアが `best_loss` を押し下げてガードを不当に狭め、本命手まで弾くのを防ぐため。
全候補が min_visits 未満（＝解析がほとんど進んでいない）ならフィルタせず従来どおり全候補で判断する。

**検証**（実機・3 run × 4局面、設定はユーザー config）:

| 局面 | 選ばれた枠 | 選択 | 結果 |
|---|---|---|---|
| case1 初手 | ko=False (+2.8 / +16.0) | A11 ×3 | 実戦は B12 が受理。A11 とは 0.03目差の同着（下記の限界を参照） |
| case1 3手目 | ko=False (+3.0 / +15.6) | C12 ×3 | 正解 |
| case2 初手 | ko=True (−23.4 / +2.5) | D1 ×3 | 正解 |
| case3 初手 | ko=True (−7.4 / +4.3) | M7 ×3 | 正解 |

**残る限界**: case1 の初手は A11(loss −0.05) と B12(loss −0.02) が 0.03目差で、ε 同着後の
目数タイブレークが A11 を選ぶ。実戦でアプリが受理したのは B12。**visits の多い方**を採る案は
case1 3手目で正解 C12(354visits) が D12(769visits) に負ける run があるため採用しない
（目数のほうが安定した信号）。

## 追記4（2026-07-30）: コウは「コウダテがある前提」で評価する

4例目の誤答（13路右上、正解 N10＝コウ。AI は N8＝セキを選択）。枠の自動選択は正しく働き
（実戦 SGF は ko_p=True の枠と完全一致）、枠自体も機能していた（白11子を取れれば +10.75目）。
**KataGo の評価も枠内では正しかった**:

- N10 の後、黒自身の連（L10-L11-M11-M12-N12）が呼吸点1（N13）になり、白 N13 で黒5子が取られる
- つまり N10 はコウにしかならず、黒はコウに勝たないと大損。20000 visits でも評価は変わらない
- 枠が攻め方に与えるコウダテは**1つだけ**で、白は B9 の1手（しかも +2目の得）で消せる

一方、**コウを黒が1回勝った局面は +8.09目 / 勝率 99.8%**（セキの N8 は −12.4目）。
つまり正解手 N10 は枠の中でも正しく、足りないのはコウダテだけだった。

詰碁は**コウダテがあるものと仮定して正解が決まる**（コウに持ち込めればそれが最大の成果）。
枠にコウダテを増やす案は試作したがバランスが崩れ（case2 が +2.3 → −16.1 で誤答に退行）、
配置先の地形次第で有効な脅しにならず不採用。代わりに**評価側で慣習を実装した**。

### 採用した対処: コウ手は「取り返した後」で評価する

候補手ごとに、打った石が**単独で呼吸点1** → 守り側がそこに打って取る → **攻め方の取り返しが
コウで弾かれる**、という形かを KaTrain 自身の着手判定（`IllegalMoveException("Ko")`）で判定する。
コウなら `ignore_ko=True` で取り返した局面を作り、そこを短く解析（既定 800 visits）した
scoreLead で評価する。通常評価の最善手を `TSUMEGO_KO_MARGIN` 超えて上回る場合のみ
その手を打つ（当初 0.5目。**追記7 で 5.0目 に変更**）。判定は本譜を汚さない使い捨てゲーム
（`tsumego_simulation_game`）で行う。

- `tsumego_ko_win_node()` / `tsumego_simulation_game()` — `ai.py`、ユニットテスト `tests/test_tsumego_ko.py`
- 設定: `ai/ai:tsumego` の `ko_win_assumption`（既定 true）、`ko_win_visits`（既定 800）
- コストはコウが見つかった候補だけ解析1本（実測 +1秒）。ハードコード定数: 判定する候補は
  visits 上位3手まで、マージン 0.5目

**検証**（実戦の戦略コードを実行、各2回、設定はユーザー config）:

| 局面 | 枠 | 選択 | 結果 |
|---|---|---|---|
| case1 初手 | ko=False | A11 ×2 | （B12 と 0.03目差の同着。下記の限界参照） |
| case1 3手目 | ko=False | C12 ×2 | 正解 |
| case2 初手 | ko=True | D1 ×2 | 正解 |
| case3 初手 | ko=True | M7 ×2 | 正解 |
| **case4** | ko=True | **N10 ×2** | **正解**（コウ判定: 通常 −21.7目 → コウ勝ち前提 +3.1目） |

コウと判定されたのは case4 の N10 のみで、他の局面の挙動は変わらない。

### 併せて修正: 枠選択が拮抗したときのタイブレーク

同じ問題を再キャプチャしたところ、今度は **ko_p=False の枠**が選ばれて再び誤答した。
両枠のバランス距離が **12.36 vs 11.61** とほぼ同点で、400 visits の試算では区別できずコイン投げに
なっていた。しかも ko_p=False では守り側にコウダテが渡るため、「攻め方がコウに勝った局面」を
作っても守り側が取り返せてしまい、**コウ判定が発動しない**（実測: ko_p=False で −19.9、
ko_p=True で +5.4）。

そこで `pick_balanced_frame` に **`FRAME_BALANCE_TIE_MARGIN`(2.0目)** を導入し、バランス距離が
これ以内で拮抗する場合は **攻め方にコウダテを渡す枠（ko_p=True）を優先**するようにした。
詰碁はコウダテがある前提で正解が決まるので、迷ったら攻め方に渡すほうが慣習に近い。
差が大きい場合は従来どおりバランス優先（case1 は距離 2.2 vs 11.0 なので ko_p=False を維持）。

再検証（実戦の戦略コード、各2回）: case4 の再キャプチャ局面（黒 K9 → 白 L11 の順で入った版）も
**N10 ×2 で正解**。他の5局面も変化なし。

### 殺す詰碁と生きる詰碁の両方に効く理由

`ko_p` の条件は `for_offense_p = xor(ko_p, xor(黒が攻め方か, 黒番か))` なので、**黒番である限り
`ko_p=True` は常に「黒（＝AI）にコウダテを渡す」**。黒が攻める詰碁なら攻め方パターン、
黒が守る詰碁なら守り方パターンが置かれ、受益者はどちらも黒になる。タイブレークの
「拮抗したら ko_p=True」はそのまま「迷ったら AI にコウダテを渡す」を意味する。
コウ勝ち評価も黒視点のスコアで見るので、殺すコウでも生きるコウでも同じくプラスとして出る。

コウ判定は「自分の**1子**が呼吸点1 → 守り側がそこを取る → 取り返しがコウで禁じられる」形を探す。
取られる1子は打った石自身とは限らないため（殺す詰碁では打った石自身、生きる詰碁では別の自石が
取られてコウになる形が普通）、打った石を最優先で試したうえで、他の1子アタリも
`_TSUMEGO_KO_MAX_ATARI_STONES`(6) 個まで調べる。

## 追記5（2026-07-30）: 黒番が ai:default に戻され、詰碁戦略が丸ごと無効だった

上記の対処を入れても実機で誤答が続いたため実行ログを取ったところ、決定的な行が出た:

```
Generate AI move called with mode: ai:default
Initializing DefaultStrategy with settings: {}
[DefaultStrategy] Top move: N8 ... [DefaultStrategy] Final decision: N8
```

**AI が `ai:tsumego` ではなく `ai:default` で着手していた**（＝KataGo の最善手をそのまま打つ）。
ownership 選択もコウ勝ち前提評価も一切効いていなかった。

原因は GUI の対局者ウィジェット。`update_player("B", PLAYER_AI, AI_TSUMEGO)` は
`PlayerSetupBlock.update_player_info` → `PlayerSetup.update_widget` を Clock で呼ぶが、そこでは

1. `player_type.select_key(PLAYER_AI)` → 種別が人間→AIに変わるので `setup_options` が発火し、
   `update_global_player_info()` が**ドロップダウンの現在値（ai:default）を KaTrain 側へ書き戻す**
2. `player_subtype_ai.select_key("ai:tsumego")` → `ai:tsumego` は選択肢
   （`AI_STRATEGIES_RECOMMENDED_ORDER` = spinner の `value_refs`）に無いので**選び直せず**、
   書き戻しも起きない

という順で進む。結果、黒番は `ai:default` で確定する。種別が既に AI の場合は 1 が発火しないため、
**起動後1回目のキャプチャだけ**この症状が出る（連続キャプチャしていた過去の検証では2回目以降が
`ai:tsumego` で動いていたため気づけなかった）。

**対処**:

- `AI_TSUMEGO` を `AI_STRATEGIES` と `AI_STRATEGIES_RECOMMENDED_ORDER` に登録（＝ウィジェットが
  値を保持できるようにする。書き戻しも `ai:tsumego` になる）。i18n（en/jp の `ai:tsumego` /
  `aihelp:tsumego`）を追加して `.mo` を再コンパイル
- `AI_OPTION_VALUES["min_visits"]` を `range(1, 10)` → `range(1, 31)`（既定10がスライダー範囲外だと
  AI設定画面を開いただけで値が変わりうるため。`ai:simple` と共有のキー）
- キャプチャ完了時（`finish_gui`、ウィジェット往復の後）に実効値を検証し、戻されていたら
  再設定して `OUTPUT_INFO` で警告する
- 回帰テスト `tests/test_tsumego_player_setup.py`

**教訓**: 「GUI の一覧に出さない戦略」をプログラムからだけ設定する設計は、GUI が値を保持できず
静かに巻き戻す。以後、`player_subtype` に使う値は必ず `AI_STRATEGIES_RECOMMENDED_ORDER` に入れる。

## 追記6（2026-07-30）: 枠外の代償地帯が gain の符号を反転させていた

5例目の誤答（13路左下、正解 A4。AI は **C3** を選び白5子が生きた）。枠の自動選択・visits・
コウ判定はすべて正常で、**gain の集計範囲**が原因だった。

`gain` は「盤上の**全**石」を合計していた（設計時の意図は「対象石群の同定を誤るより頑健」）。
しかし枠は `put_outside` で**リージョン外**を「守り側の代償地帯 `defense_area` ＋ 攻め方の地」に
配る設計で、その境目は未決着のまま残る。つまり**枠外の石の ownership は詰碁の成否と逆相関する
counterweight** になっており、gain に混ぜると符号が反転する。誤答局面の実測（1800 visits / wRN 0.04）:

```
move  visits  ptsLost   gain(全石)  gain(リージョン内)
C3      294    +1.91     +2.90  ←選択    -9.65   ← 正しく「白が生還」と出ている
A4      857    +0.06     -0.19           +0.33   ← 正解
B3      630    -0.06     +0.11           +0.24
```

C3 の内訳: リージョン内の白5子が +0.93 → −1.00（各 −1.93、計 **−9.65**）と正しく生還を示す一方、
**枠外の境目6子（M1 M2 M3 N2 N3 N4）が各 +1.94、計 +11.6** 動いて上回り、合計 +2.90 で候補中最大になった。
ノイズではなく構造的な逆相関なので、**枠あり詰碁では「守り側が生きる手」に系統的なバイアスが掛かっていた**。

6000 visits での事後確認（リージョン内の白7子の ownership、正 = 黒のもの = 白は死）:

| 手 | 白石の ownership | 判定 |
|---|---|---|
| C3（旧実装の選択） | 5子が **−0.996（生き）**、2子のみ死 | ✗ 失敗 |
| A4（正解） | 7子すべて +0.998 | ✓ |
| B3 | 7子すべて +0.998 | ✓（別解） |

**対処**: `tsumego_gain_stones(stones, region_of_interest)` で集計対象をリージョン内の石に限定する。
リージョンが無い（枠なしモード）場合は従来どおり全石。枠内に残る枠石（壁）は堅く生きていて
ownership が動かないので、混ぜても害はない＝リージョン境界が正しい切り口になる。

**検証**（同一の解析結果に対して集計範囲だけを差し替え、run 間分散を交絡させない A/B × 4 run）:

| 局面 | 旧（全石） | 新（リージョン内） |
|---|---|---|
| 初手 | C2 ×4 ✓ | C2 ×4 ✓ |
| 3手目 | D1 ×4 ✓ | D1 ×4 ✓ |
| **5手目** | **C3 ×4 ✗** | **B3 ×3 / A4 ×1**（どちらも白を殺す） |

旧実装の誤答は 4/4 で**決定的**（ノイズではない）。すでに正解していた局面の選択は変わらない。

**残る限界**: 5手目は A4 と B3 の 2 通りの正解があり、gain 飽和（root で白が既に死と読まれ、
どちらも gain ≈ 0）のため 0.12目差の目数タイブレークで B3 に寄る。アプリの解答木が A4 しか
受理しない場合は、追記3 の case1 初手と同じ**別解の受理問題**として残る。

## 追記7（2026-07-30）: コウ勝ち前提が「無条件に殺せる正解」を捨てていた

6例目の誤答（13路下辺、正解 **K1** で白を無条件に殺せる。AI は **L1** でコウにして不正解）。
追記6 の gain 修正は正しく効いており（`gain集計石41子`）、原因は **追記4 のコウ勝ち前提の
マージン `0.5目` が小さすぎた**こと。誤答局面の実測（region 1800 visits）:

```
move  visits   ptsLost  lead*sign  gainREG   ko?    koWin     gap
  K1    1776     +0.12     +11.44    +0.00     -            ← 無条件に殺す正解
  L1       1    +34.26     -22.70   -28.03    KO   +12.50   +1.06  ← 選ばれた
```

K1 は 1800 visits のうち 1776 を集める圧倒的な最善で、白は既に死んでいる（gain ±0.00）。
一方 L1 は **1 visit**・−34目・gain −28（白が生き返る）。それが「コウ勝ち前提 +12.50目 >
通常最善 +11.44目」で採用された。差はわずか **+1.06目**。

### なぜ +1.06目 出るのか（構造的なバイアス）

コウ勝ち前提のノードは (打つ → 守り側が取る → 取り返す) と進めた局面なので、**攻め方が
1手多く打ち、相手の石を1子取った状態**になっている。通常最善の局面と手数が揃っていないため、
比較は構造的にコウ側へ数目ぶん偏る。この +1.06目 はその「おまけ」であって、詰碁としての
成果の差ではない。**通常最善が既に無条件で殺しているとき、この偏りだけでコウが勝ってしまう。**

逆にコウが本当の正解である問題では、通常最善はセキ止まり等の失敗なので差は桁違いに大きい。
記録済みの実測値:

| ケース | 通常最善 | コウ勝ち前提 | 差 | 正しい判定 |
|---|---|---|---|---|
| **case E（本件・誤答）** | **+11.44**（無条件に殺す K1） | +12.50 | **+1.06** | 不採用 |
| case4（追記4・正解がコウ） | −12.4（セキの N8） | +3.1 | **+15.5** | 採用 |
| case4 再キャプチャ | −12〜−19 | +5.4 | +17〜+25 | 採用 |

### 採用した対処1（本命）: 成功しているならコウを検査しない

**詰碁の正解は目数ではなく結果の順序で決まる: 無条件に殺す（生きる） > コウ > セキ。**
目数はクラス内のタイブレークにすぎず、クラスを跨いだ比較には使えない。上の +1.06目 はまさに
「クラスを跨いで目数比較した」結果である。

そこでコウ勝ち前提の適用条件を、その本来の役目に絞る。この機構は追記4 で
「枠の中では攻め方のコウダテが乏しく、正解のコウ手がセキより悪く見える」局面の**救済**として
入れたものなので、**通常最善が既に成功しているなら適用してはいけない**。

成否の判定にはスコアの符号を使う。枠は「成功した側が `offence_to_win`(5)目勝ち」に調整される
設計なので、手番側から見た `best_normal` の符号がそのまま成否になる。`player_sign` 込みで見るので
**攻める詰碁・生きる詰碁のどちらでも成立**し、攻守の役（`black_to_attack_p`）を知る必要がない
（この値は枠生成時に `guess_black_to_attack` で決まるだけで game には残らない）。

記録済みの実測値すべてで一致する:

| ケース | 通常最善 | 判定 | 正しいか |
|---|---|---|---|
| case E（本件） | **+11.44**（K1 が無条件に殺す） | 既に成功 → コウ検査を省略 | ✓ |
| case4（正解がコウ） | −12.4（セキの N8） | 失敗 → 救済を試す | ✓ |
| case4 再キャプチャ | −12〜−19 | 失敗 → 救済を試す | ✓ |
| case4 ko_p=False の枠 | 負（コウ勝ちも −19.9） | 救済を試すが却下 | ✓ |

- `tsumego_already_succeeded()` / `TSUMEGO_SUCCESS_LEAD`(0.0)、設定 `ko_success_lead`
- コウ解析（800 visits）より**前**に判定するので、成功局面では解析1本ぶん速くなる

### 採用した対処2（保険）: マージンを 5.0目 に引き上げる

対処1 が主で、マージンは「救済が必要な局面で、構造的な偏りだけでコウが選ばれる」のを防ぐ保険。
実測の分離幅は +1.06 と +15.5 なので 5.0 は両者から 3倍以上の余裕がある。

- `TSUMEGO_KO_MARGIN` を 0.5 → **5.0**、設定 `ko_win_margin` で上書き可能
- 判定を純関数 `tsumego_ko_beats_normal()` に切り出してユニットテスト化（実測2ケースを固定）
- 採用/不採用の両方のログに**差と閾値**を出す（0.5 のときは差が見えずログから異常に気づけなかった）

**検証**（同一スクリプト・同一エンジンで新旧を比較、region 1800 visits、3 run）:

| 設定 | 選択 | コウ判定 |
|---|---|---|
| **新既定（成功ゲート + margin 5.0）** | **K1 ×3 ✓** | 「通常最善 +11.4目で既に成功」で省略 |
| 成功ゲートのみ（margin 0.5 に戻す） | **K1 ✓** | 同上（マージン値に依存しない） |
| 旧挙動（ゲート無効 + margin 0.5） | L1 ✗ | 採用（差 +1.04目、pointsLost +34.71） |

2行目が対処1 の価値を示している。**マージンという魔法の数字に頼らず、意味で決まっている。**

`min_visits` をコウ検査に課さない設計は**維持**した（探索分散で正解のコウ手が数 visits に沈む
ことがあり、そこで弾くと追記4 の慣習が効かなくなる）。1 visit の L1 は成功ゲートで落ちる。

**残る限界**: 成否の判定はスコアの符号なので、枠が大きく偏っている問題（`frame_balance_distance`
が大きいまま採用された枠）では誤判定しうる。その場合はマージンが保険として働く。
枠自体が壊れている問題は [[追記2]] の枠自動選択の領域。

---

## 追記8（2026-07-30）: gain は「探索の浅さ」を測っていた（case F）

7例目の誤答（13路右上、正解 **N8**。AI は **N7** を選び白が完全に生きた）。枠の自動選択・
リージョン限定・gain のリージョン絞り込みはすべて正しく働いており、原因は **1本の root 探索の
`movesOwnership` を候補間で比較していること**そのものだった。

**枠石は無罪**: gain 集計45子のうち枠石18子の寄与は全候補で **±0.00**（枠の壁は動かないという
`tsumego_gain_stones` の前提は正しい）。

### 測定（region 1800 visits を3 run + 同一局面を 8000 visits で深掘り）

| 手 | visits | 最善手比 | pointsLost | gain@1800 | 8000 visits では |
|---|---|---|---|---|---|
| **N8（正解）** | 780–890 | 1.00 | −0.60〜−0.79 | −0.45〜−0.55 | 2565visits で **−0.04** |
| N7（誤答） | 214–307 | 0.31–0.34 | +1.35〜+1.60 | **+2.70〜+9.10** | 637visits で **+0.06** |
| N6 | 89–90 | 0.11 | +1.44 | **+11.06〜+11.98** | 502visits で +0.66 |

visits を与えると gain が消える＝**死活の信号ではなく探索解像度の差**。この局面は root の
ownership が飽和（対象の黒石 −10.4/11、リージョン90点中 −87.3）しているため、gain は 0 方向への
**片側ノイズ**しか出せない。[[追記3]] で「root が決着していると信号が消える」限界は認識していたが、
**そのときのノイズ幅を ±0.03 と見積もったのが誤り**で、実測は +12（400倍）。case B / C の実信号
1.16 / 3.20 も飲み込むため `gain_epsilon` では原理的に止められず、`min_visits=10` も無力
（N7 は 214–307 visits ある）。

### 対処1: 深さゲート（`gain_min_visit_ratio`、既定 0.5）

gain で**目数最善手を覆せるのは、その手と探索の深さが比較できる候補だけ**に限る。
実測比は誤答 N7 0.31–0.34 / N6 0.11 に対し case D の正解 A4 は 1.00 なので 0.5 で分離できる。
追加クエリ 0・レイテンシ 0。

### 対処2: 同深さ検証（`gain_verify` / `gain_verify_visits` 800 / `gain_verify_margin` 0.3）

覆す判断が出たときだけ両者の**子局面を同 visits で解析し直し**、対象石の ownership を
**絶対値**で直接比較する（別クエリ同士は root 基準が揃わないので差分は使えない → 
`tsumego_absolute_ownership`）。実測（各1800visits、対象27子）: N8 **−26.60** > N7 −26.91 /
N6 −26.84 / M7 −26.89 で正解が残る。同深さでも ±0.3 程度は動くので margin 0.3 を要求する。

**検証**（同一スクリプト・region 1800 visits）:

| 設定 | case F(正解N8) | case D(正解A4) | case E(正解K1) |
|---|---|---|---|
| **新既定（ゲート+検証）** | **N8 3/3 ✓** | **A4 3/3 ✓** | **K1 3/3 ✓** |
| ゲート無効・検証のみ | **N8 2/2 ✓**（検証が却下: N6 −44.87 vs N8 −44.02） | — | — |
| 両方無効（旧挙動） | N7 / N6 **0/2 ✗** | — | — |

2層は独立に有効。旧挙動は誤答を再現する（＝A/B が成立している）。

### この問題自体について（選択ロジックとは別）

- 枠バランスは **−25目**（設計目標 ±5目、距離 20.5）。攻め方×コウダテの4通りを張り直しても
  最良が採用済みの枠（−25.5 / −33.4 / −48.3 / −73.7）。**リージョンが盤の53%（90/169点）を
  占める大型詰碁では、リージョン内の空き地がまるごと片側の地になり、この枠の設計が成立しない**。
  → `FRAME_BALANCE_WARN_DISTANCE`(8.0) 超過で警告を出すようにした（黙って進めない）
- 絶対スコアに依る判定（`ko_success_lead` の「既に成功」）は当問題では常に「失敗」側に張り付く
- KataGo は 6000–8000 visits で対象の黒石を**全ラインで死**と読む（app の正解手順を打ち切っても
  −10.96/11）。捕獲盤では W K10 の後、黒4子 K9/L9/M9/L10 が M10 の1呼吸点＝アタリで、これは
  枠石ではなくコア石だけで決まる。**app の問題と捕獲盤が一致しているかは別途確認が必要**
- それでも **KataGo の最善手は3手すべて app の正解と一致**（L12 → N8 → 3手目は N11 +0.22 と
  M12 +0.24 の 0.02目差だが、**app 側はどちらでも正解判定**とユーザー確認済み）。つまり本件の
  誤答要因は gain のみで、目数順位はどの手番でも正解を指していた

## 追記9（2026-07-30）: 枠が詰碁自体を消していた（case G / 攻め方の推定が反転）

13路左上、黒番、app の正解は **初手 A11**（A11 / W-A10 / C13 / W-E13 / A13 でコウ）。AI は初手
**B13** を打ち、白 D13 で殺すこともコウにすることも不可能になった。回帰データ
`calibration-data/tsumego/case-g-frame-role-20260730.sgf`（region `0,7,3,12`）。

### 実測（実際に出題された枠の盤）

| 指標 | 値 |
|---|---|
| 白の標的群（A12 B11 C11 D11 B10 B9）ownership | **+5.98/6** = 完全生存。正解手順を打ち切っても +5.97 |
| 黒の攻め石11子 ownership | **−10.86/11** = 全滅（−0.99/子） |
| 候補34手の目数差 | 1800v: B13 +0.00 / A11 +0.04、6000v: A11 +0.00 / B13 +0.05 |
| 候補の gain 絶対最大 | **0.07**（正常な局面は 9.7〜21.8） |

**どの手も同値**なので選択則では原理的に当たらない。B13 と A11 の差 0.05目は完全なノイズ。

### 原因

1. `guess_black_to_attack` が役割を反転した（白が攻め方と判定）。この問題は**図に白の外壁が
   描かれている**（F13/G12/E12/F11/F10/E9/E8/D7/D6/C5/B5 が問題本体の境界）ため、extrema
   ＝上下左右端の代表石が **4/4 とも白**になり、「守り側は端・攻め方は中央寄り」という
   ヒューリスティックの前提が逆転する。3層の入れ子（白の外壁 > 黒 > 白の標的）は枠の
   2層モデル（守り側の石＋攻め方の囲み）に収まらない
2. 壁が白で張られ（region が盤の47%＝80/169 なので `fit_margin` は margin 1 まで圧縮）、
   黒の攻め石が白の壁と白の外壁に封じ込められて死亡した

### 枠バランスではこの失敗を検出できない（原理）

枠は「**想定した**攻め方が成功したら `offence_to_win` 目勝ち」に代償地帯を調整する。役割の
推定が反転していると、想定攻め方（この場合は白）が実際に成功するので**バランスは完璧に見える**。

| 枠 | root lead | バランス距離 | 黒のコア石 |
|---|---|---|---|
| 13路 攻め方=白 ko=True（実際に採用された枠） | −7.06 | **2.06**（過去最良） | **−10.86/11** |
| 13路 攻め方=白 ko=False | −11.99 | 6.99 | −10.75/11 |
| 13路 攻め方=黒 ko=True | −43.72 | 38.72 | +3.76/11 |
| 19路に置き直し 攻め方=白 ko=True | +10.42 | 5.42 | −10.75/11 |

追記8で入れた `FRAME_BALANCE_WARN_DISTANCE`（8.0）の警告は距離 2.06 なので**鳴らない**。
19路に置き直しても距離 5.42 で鳴らない。**バランスは役割反転に対して構造的に不感**。

### 検討して却下した案

| 案 | 結果 |
|---|---|
| 役割だけ直す（13路 攻め方=黒） | ko=True で A11 が **+7.00目損**（最善は F9）。ko=False なら A11 が目数1位だが gain で B13 に競られる。距離 39〜43 |
| 19路に埋め込む＋役割修正 | A17(=A11) が v1390/1800・pt+0.00（次善 +10.75、B13 は +15.4）で正解するが lead −121 で設計外。しかも役割修正が前提で、正解の理由は「白が死ぬ」ではなく「黒自身が生きる」 |
| 枠なしで出題（採用） | A11 が pt+0.00・gain +1.03 で1位（次善 B13 +0.97） |

### 採用した判定: 「手番側の本体石が死んでいる枠は捨てる」

ユーザー指摘の不変条件 —— **必ず正解手がある詰碁で、開始時点から解く側の石が全滅している
判定はあり得ない**。これを枠の採否条件にする（`tsumego_frame.frame_destroys_problem` /
`solver_core_points`、判定は枠バランス試算 `frame_ko_trial_visits`=400 の ownership を流用）。

- 対象は**枠の壁・充填を除いた本体（コア）石のみ**。壁石は自明に生きているので混ぜると判定が
  埋もれる（実測 case D: 壁込み +25.00/25 で常に正常判定、本体だけなら +8.00/8）
- 閾値は **+0.5/子**（0 ではない）。実測で正常な枠は +1.00/子、壊れた枠の1つが −0.09/子 と
  0 付近に来たため（case F の ko=False 枠）、0 だと run ごとに採否が反転する
- どの枠候補も落ちたら**その回だけ枠なしで出題**する（`use_frame` の既定は枠ありのまま）

### 枠あり／枠なしの比較（4ケース、`select_tsumego_move` の選択手）

| ケース | 枠候補の本体石 ownership（ko=False / True） | 判定 | 枠あり | 枠なし |
|---|---|---|---|---|
| D（正解 A4/B3） | +1.00 / +1.00 /子 | 枠 ko=False | **OK A4** | OK A4 |
| E（正解 K1） | +1.00 / +1.00 /子 | 枠 ko=False | **OK K1** | OK K1 |
| F（正解 N8） | −0.09〜−0.19 / −0.94 /子 | 枠なし | OK N8 | **OK N8** |
| G（正解 A11） | −0.98 / −0.99 /子 | 枠なし | NG B13 | **OK A11** |

枠なしは4/4、枠ありは3/4。判定は D/E の枠には触れず（+1.00 は閾値から 0.5 離れている）、
F/G だけが枠なしに落ちる。枠なしを既定にはしない（過去の実機検証で「空いた盤面では地合いが
支配して詰碁を読む動機が消える／コミで均衡させると空点が最善手になる」という失敗が出ている）。

### 残る限界

- **非対称性**: 殺し問題では手番側の攻め石が壁と連絡して自明に +1.00 になるので判定は安全側。
  生き問題では手番側の石自体が戦いの対象なので、難問をエンジンが trial 400visits で読み切れ
  ないと ownership が下がり（生きと死のラインに探索が割れて +0.2 等）、有効な枠でも枠なしに
  落ちうる。実測ケースは4つとも殺し問題（コウ含む）で、生き問題の実例は未計測。枠なしは実測
  4ケース全て正解なので実害は小さい見込みだが、生き問題で枠なし側の既知の失敗（地合い支配）が
  再発したら、ここが最初の容疑箇所
- 枠なしでは絶対スコアが意味を持たない（実測 lead −82〜+111）ので `ko_success_lead` /
  `ko_win_margin` の絶対スコア判定は当てにならない。相対の `pointsLost` と gain は機能する
- `guess_black_to_attack` 自体は直していない。役割反転は「壊れた枠」として検出・回避される

## 追記10（2026-07-30）: 枠なし盤で目数ガードが正解を足切りした（case G 2手目）

追記9の枠なしフォールバック導入後、case G の再対局で **1手目 A11 は正解**になったが、
**2手目で正解 C13 を捨てて B13 を選び**再び不正解になった（実戦ログ:
`C13(v276 pt+2.26 g+5.94)` 足切り → `B13(pt-0.00 g-4.06)` 採用）。
回帰データ `calibration-data/tsumego/case-g2-frameless-guard-20260730.sgf`。

### 実測（枠なし盤・B-A11 W-A10 の後・1800visits ×3run + 実戦）

| run | C13（正解） | B13 | ガード帯（best+2.0） | 選択 |
|---|---|---|---|---|
| 実戦 | pt+2.26 g+5.94 v276 | pt−0.00 g−4.06 v266 | +2.00 | B13 ✗ |
| 0 | pt+1.69 g+6.60 v290 | pt−0.39 g−3.19 v323 | +1.61 | B13 ✗ |
| 1 | pt+1.56 g+6.25 v287 | pt−0.41 g−3.43 v288 | +1.59 | **C13 ○** |
| 2 | pt+1.60 g+5.79 v347 | pt−0.42 g−4.09 v305 | +1.58 | B13 ✗ |

C13 の pointsLost がガード帯 ±0.1 を挟んで揺れる＝**コイン投げ**。閾値の微調整では直らない。

### 原因

枠なし盤では「殺し損ねても外の空き地で取り返せる」ため KataGo の目数差が圧縮される
（枠なしの既知の病理が pointsLost に出た形）。一方 **gain はリージョン内の石だけの集計**
（追記6）なので汚染されず、C13 +5.79〜+6.60 vs B13 −3.19〜−4.09 と差約10で断トツ、
visits も同等（比 0.90〜1.14）だった。つまり「gain は正しいのに、目数ガードが gain 争いへの
参加を拒んだ」。同深さ検証（両手を800visitsで解析し直し絶対 ownership 比較）は
**C13 が +8.41〜+8.88 で3run とも安定**して勝つ。

### 採用した対処: 目数ガードの救済（`tsumego_rescue_candidate` / `gain_rescue_margin`）

ガードで足切りされた候補でも、次の全条件を満たす手は同深さ検証にかけ、**検証で確定した
場合だけ**採用する（検証なしでは絶対に採用しない。ガード外は本当に大損な手も含むため）:

1. gain が現在の選択手を `gain_rescue_margin`（既定 1.0）超えて上回る
2. `min_visits` 以上かつ選択手と探索の深さが比較できる（`gain_min_visit_ratio` の visit比）
3. `gain_verify` が有効（無効なら救済ごと停止）

margin 1.0 の根拠: 深さゲート通過後の偽 gain は ≦0.7（追記8）、本件の実信号は差約10。

### 検証（いずれも実 `generate_move`・1800visits・3run）

| ケース | 結果 |
|---|---|
| G2 2手目（枠なし） | **3/3 C13**（救済発動→同深さ検証 +8.4 で採用）＋モジュール再実行の3runも C13 |
| G 3手目（枠なし） | 3/3 A13（コウ取り。救済不要、通常選択 gain +1.1） |
| D / E / F（枠あり） | 9/9 正解（**救済は一度も発動せず**＝枠ありでは不活性。F の偽 gain N7/N6 は visit比 0.24/0.07 でゲートに弾かれる） |

### 残る限界

- 救済は「gain が大きく・深く探索され・同深さ検証で勝つ」手しか拾わない。枠なし盤で正解手の
  探索自体が痩せる局面（visit比 < 0.5）は救えない（そのときは深さゲートと同じ理由で、
  拾うほうが危険）→ **追記11 で撤回・修正**（visit比では本物と偽が分離できない実例が出た）

## 追記11（2026-07-30）: 深さゲートが目数ガード内の正解を足切りした（case H）

13路右下（枠なし）、黒番、正解手順 N2 / N1 / L2 / L1 / **N4**。AI は5手目で J7 を選び不正解。
回帰データ `calibration-data/tsumego/case-h-gate-cliff-20260730.sgf`（region `5,12,0,6`）。

### 実測（1800visits ×3run + 実戦）

- N4: **目数ガード内**（pt+5.96〜+6.19 ≤ best+2.0）、gain **+4.4 の断トツ1位**、しかし
  visit比 **0.46〜0.49 < 0.5** が深さゲートをわずかに下回り 3/3 で gain 争いから除外
- 追記10 の救済は「目数ガード外」しか対象にしていなかったため届かず、残った候補から
  J7/F7 等（gain −0.2〜−0.5）が選ばれた
- 同深さ検証（800visits）: **N4 +13.2〜+14.2 vs 代替 +8.9〜+9.7（差+4.3〜+4.6）**で完全に安定

### 原因と本質

深さゲートも救済の visit比条件も「浅い候補の gain は信用しない」という同じヒューリスティック
だが、**visit比では本物と偽が分離できない**: 本物 N4 の比 0.46（別の測定では 0.21）に対し、
case F の偽 N7 は 0.24〜0.36。重なっている。分離できるのは**同深さ検証だけ**
（本物: +4.5 / +8.4 で勝つ。偽: −0.24〜−0.58 で負ける）。

### 採用した対処: 救済の対象拡大と検証の厳格化

1. 救済の対象を「目数ガード外」から「**gain 争いに参加できなかった全候補**」
   （非 contenders ＝ ガード外＋深さゲート外）に拡大
2. 救済の visit比条件を**撤廃**（min_visits=10 の床のみ。深さの不信は検証そのものが解決する）
3. 代わりに救済の**採用マージンを gain_rescue_margin（1.0）に厳格化**（通常の覆しは 0.3）。
   偽 gain の候補も検証まで来るようになるが、偽は検証で −0.24〜−0.58 と負けるので
   +1.0 要求ならノイズ（±0.3）に対する安全域が広い

### 検証（実 `generate_move`・1800visits・各3run、計21run 全て正解）

| ケース | 結果 | 救済の挙動 |
|---|---|---|
| H 5手目（枠なし） | 3/3 N4 | run0-1 は探索分散でゲート内→通常経路、run2 は救済→検証+4.34採用。両経路とも N4 |
| G1 初手（安全確認） | 3/3 A11 | C13 が毎回トリガーするが検証 **−3.6 で毎回却下**＝正解の初手を守る |
| G2 2手目 | 3/3 C13 | 救済採用 +8.3 |
| G 3手目 | 3/3 A13 | トリガーなし |
| D / E（枠あり） | 6/6 | トリガーなし（D は別解 B3） |
| F（枠あり・偽gain耐性） | 3/3 N8 | 偽 N6 が毎回トリガーするが検証 **−0.5 で毎回却下**（コスト+2解析≈2秒） |

## 追記12（2026-07-30）: gain 1位のノイズ手が本物を検証から締め出した（case F2）

case F の枠なし盤・5手目（L12 / K10 / N8 / N7 の後、正解 **N11 または M12**）で AI が J11 を
選び不正解。回帰データ `calibration-data/tsumego/case-f2-rescue-shadow-20260730.sgf`
（region `5,12,6,12`）。

### 実測

- 枠なし盤の pointsLost は歪曲: J11（2子取りの小得）が best +0.34、白を殺す N11/M12 が
  +4.00/+4.13 で**ガード外**。gain は N9(v10) **+6.77** > N11 +5.41 > M12 +5.30
- 旧設計（トップ1指名）: 救済が gain 1位の **N9（v10 のノイズ手）だけ**を検証 →
  N9 −25.97 vs J11 −18.57 で正しく却下 → **そこで救済終了**。2位3位の本物は機会を失った
- 同深さ検証自体は毎回正しく序列化する（3run 安定）:
  **N11 −17.0〜−17.1 > M12 −17.2〜−17.3 > J11 −19.3〜−19.4 > N9 −26.8〜−26.9**。
  N11 は J11 を +2.2〜+2.4 上回り margin 1.0 を余裕で超える

### 構造的な結論

これまでの全誤答（F/G/G2/H/F2）で**同深さ検証は一度も間違えていない**。間違えるのは毎回
「検証の手前にある単一候補の指名」（ガード・ゲート・トップ1）。よって検証を最終裁定者にする:

- 救済は gain 降順の**トップ3**（`TSUMEGO_GAIN_RESCUE_MAX_CANDIDATES`）を全員検証し、
  incumbent を margin 超えて上回った中の**検証値最良**を採用（`_verified_choice`）
- コストは挑戦者1人あたり同深さ解析1本（≈1.3秒）。発動しない手番はゼロ
- あわせて潜在バグ修正: 検証が実行不能なとき（局面再現失敗等）に救済候補を未検証のまま
  採用し得た経路を、現在の選択（fallback）に戻すよう統一

### 検証（実 `generate_move`・各3run、計24run 全て正解）

| ケース | 結果 | 備考 |
|---|---|---|
| F2 5手目（枠なし） | 3/3（N11×2, M12×1） | run2 は N9 もトリガーに入り検証 −7.45 で却下、M12 採用 |
| H / G1 / G2 / Gm3 | 12/12 | G1 は C13/G13 が全run却下され A11 維持（安全確認） |
| D / E / F（枠あり） | 9/9 | F の偽 N6/N7 は救済トリガー圏でも検証が却下 |

## 追記13（2026-07-30）: 守り側の「無条件の生き > コウ」を選べない（case I・未対処の既知限界）

13路右下（枠なし、region `6,12,0,8`）、黒番。正解は **N2** / W-N3 / J1 / W-N7 / K1 —
一部の黒石を**捨てて**残りが無条件に生きる。AI は **J1** を先に打ち、白 N2 の咎めで
コウ生きしか残らず不正解。データ `calibration-data/tsumego/case-i-defender-ko-20260730.sgf`。

### 選択則の問題ではない（実測）

J1(v709 pt−0.13 g+0.25) vs N2(v488 pt+0.38 g−0.18) — 目数も gain も J1 を支持しており、
これはノイズではなく**評価の実態**。正解 N2 は捨て石が確実に死ぬのに対し、KataGo は J1 の
コウを黒有利と読むため、期待 ownership では J1 が上回る。詰碁の慣習（無条件 > コウの辞書式
順序）と期待値最大化の**目的関数の不一致**が根にある。

### どの層でも判別できないことを確認した実測

| 測定 | 結果 |
|---|---|
| J1/N2 の子局面 800/1800/6000visits | 全深さでほぼ同一（sum差 0.1〜0.4、lead は J1 が 0.7 有利） |
| コウの静的署名（ownership 飽和度） | 無い。unsettled はむしろ N2 の方が高い（7.5 vs 6.1） |
| J1 の咎め W-N2 の評価 | **6000visits でも v287 で「白の失着（黒視点 −52.9）」と誤読**。実際に打たれた後の再解析は −65.5 ＝ 12目超の読み違い。KataGo の PV は白の応手を N7 と読み咎めを含まない |
| 枠を張り直す（役割×コウダテ4通り） | 正しい役割（白攻め方）の枠2種でも J1 が loss+0.00 で1位（ko=False 枠は gain+18.2）。枠のコウダテ環境でも判別不能 |

J1 が「コウ生きしか残らない手」だと知るには白 N2 を正しく読む必要があり、エンジンがどの
深さ・どの盤面埋め込みでも読めない以上、その上のどんな選択ロジックでも判別できない。
「KataGo 探索崩壊」（正解手や咎め手を候補にすら挙げない族）×「多手先計画の壁」の交点。

### 対処

- **コード変更なし**（この層では修正不可能と測定で確定）
- 実用の復帰手段は既存の**アンドゥで次候補**（`tsumego_retry_candidate`）: J1 を除外すると
  N2 が候補2位（v488）に居るので、アンドゥ1回で正解が出る
- エンジン（モデル/バージョン）更新時にこの盤で再評価すること

### 副次発見: 捨て石のある生き問題は枠採否判定が偽陽性になる

黒本体 12子のうち生きるのは4子だけ（+0.99）で平均 +0.19/子 → `frame_destroys_problem`
（+0.5/子）が**正常な枠まで捨てて**枠なしになる。この問題では枠を残しても J1 を選ぶので
実害はなく、判定は変更しない（変更すると F の未検証枠が使われる等のリスクだけ増える）。
捨て石問題で「枠なら正解できたのに枠なしで誤答」という実例が出たら、判定を
「明確に生きている石が1子以上ある」等に緩める案を再検討する。

## 追記14（2026-07-30）: gain も目数も同着の「正しい別解」を選んで不正解（case J）

13路右上（枠あり、region `6,12,1,12`）、黒番の殺し。10手目まで解答樹どおりに正解し、
最後の11手目で **N11 を選び不正解**（アプリの正解は N10）。
データ `calibration-data/tsumego/case-j-points-tie-20260730.sgf`。

### 実測: N11 は本当に殺せている（探索崩壊ではない）

子局面を 800/1800/8000visits でリージョン解析（`probe` 計測 2026-07-30）:

| visits | N10: lead / absOwn | N11: lead / absOwn | 白11子 ownership |
|---|---|---|---|
| 800 | +5.61 / +43.97 | +5.45 / +43.92 | +10.98 / +10.94 |
| 8000 | +5.57 / +43.96 | +5.48 / +43.94 | +10.97 / +10.95 |

- 白の最善応手（8000visits）でも lead +5.4 維持、**パスが上位**（その場で死んでいる）
- 手読みでも裏付け: B N11 で白 N12 がアタリになり、白は黒の外ダメ N13 を**自殺手で
  永久に詰められない**（W N10 は自己アタリで9子抜かれ、W N7 の繋ぎも B M5 で攻め合い負け）
- 同深さ検証も差 0.05 で margin 0.3 に遠く及ばず**分離不能**（検証拡張は無効と実測）

### 原因: 目数タイブレークがノイズ差のコイン投げになる

最終手番: N10(v1175 pt−0.05 g+0.00) vs N11(v616 pt−0.07 g−0.02)。gain 差 0.02 は
gain_epsilon 内 → 目数委譲 → **0.02 目差**で N11。複数の手が同じ死活結果に到達する
局面では目数差もノイズで、コイン投げの先が解答樹に無い別解だと不正解になる。

### 対処: 目数も points_epsilon 内で並ぶなら visits 最多（KataGo の本命）を採る

解答樹の本線は KataGo の principal variation と一致しやすい（case J の正解10手は
すべて visits 最多手）。`TSUMEGO_POINTS_EPSILON = 0.25` はノイズ（〜0.07）と
目数タイブレークが守るべき最小の実信号（追記（2026-07-29）の C12/D12 = 0.64 目差）の中間。
**別解自体は原理的に防げない**ため、これは的中率を上げるヒューリスティック
（外れた場合の復帰は従来どおりアンドゥで次候補）。

### 検証（`points_tie_ab.py`: 1回の解析から新旧両方を計算、run 間分散を排除）

| 局面 | old（points_epsilon=0） | new（0.25） |
|---|---|---|
| 11手目（誤答局面） | **N11 3/3（誤答再現）** | **N10 3/3（正解）** |
| 1〜9手目の5決定 | 正解 3/3 | 同一選択 3/3（影響ゼロ） |

3run とも N10 の方が visits 多（v938〜985 vs v802〜852）なのに、旧則は毎回
0.02〜0.03 目のノイズ差で N11 を選んでいた（コイン投げではなく一貫して誤答側）。

### 再発と修正（同日 GUI 実測）: select 単体の A/B は generate_move の検証ゲートを見逃す

GUI で同じ問題を再実行すると再び N11。ログ上 `select_tsumego_move` は N10 を選んで
いたが、後段の「選択手 ≠ 目数最善」の**無条件 score_best 同深さ検証**が発動し、
等価な2手は margin 0.3 で分離できない（N10 +43.97 vs N11 +43.92、差 +0.05）ため
必ず却下 → 目数最善 N11 へ巻き戻り、同着タイブレークが丸ごと無効化されていた。
`points_tie_ab.py`（select 単体）はこの後段を通らないため検出できなかった。

対処は `tsumego_needs_score_best_verify`: 検証が要るのは「目数を本当に犠牲にして
gain で覆す」選択（目数差 > points_epsilon）だけ。同着バンド内の選択は「等価なので
PV に寄せる」判断なので検証しない（ムダな 800visits 解析2本の節約にもなる）。
実 generate_move E2E（`generate_move_e2e.py`、検証・救済経路込み）×3run で
誤答局面 N10 3/3・1手目 M11 3/3 を確認。**以後、選択則の変更は select A/B に加えて
必ずこの E2E も回すこと**。

## 追記15（2026-07-30）: 同着バンドの visits タイブレークがコウで殺す手を選んだ（case K）

13路左上（枠あり、region `0,8,3,12`）、黒番の殺し。初手で **A12 を選び不正解**
（正解 C13 → W D12 → A10 → W A13 → A11 で無条件に白死。A12 は W A11 → B B11 の
1子取りでコウにしかならない）。データ `calibration-data/tsumego/case-k-ko-route-20260730.sgf`。

### 実測: クラス差（無条件 vs コウ）はスコアに出ない

- 最終決定: A12(v822 pt+0.02 g+0.00) vs C13(v585 pt-0.05 g+0.00) — gain・目数とも
  points_epsilon 内の同着バンドに入り、**visits タイブレークがコウ側の A12 を選択**。
  旧目数タイブレークなら C13 だった（目数差 0.02〜0.13 は 4/4 観測で C13 側に符号一貫
  ＝無条件のわずかな期待値優位。case J では同じ大きさの差が解答樹と無関係だった —
  **スコアレベルの規則では J と K を同時に満たせない**）
- KataGo はコウも黒勝ちと読み切るため、A12 の後でも lead +11.2 を維持し、
  親局面の PV では**白が枠へ手抜き**（K12）してコウ自体が読み筋に現れない

### 検出: リージョン子局面解析の最善応手 PV にだけクラス差が現れる（3/3 run 安定）

各候補を1手進めて region 限定 800visits で解析（depth1 で守り方が局所応答を強制される）:

| 候補 | 守り方の最善応手 | 応手 PV | 判定 |
|---|---|---|---|
| A12 | A11 (v190-201) | `A11 B11 ...` | **ply2 B11 がコウ形**（リージョン内） |
| C13 | D12 (v130-256) | `D12 A10 A13 A11 ...` | clean（正解手順そのもの） |
| A10 | A11 (v173-234) | `A11 C13 ...` | clean（正解手順に合流する別解） |

コウ形の構造判定は「1子取り・取った石が単独呼吸点1・相手の取り返しが KaTrain の
着手判定でコウ禁止」（`tsumego_pv_reaches_region_ko`）。**リージョン外のコウ形は
数えない** — 枠格子内の偶発コウ（実測 probe: ply7 の L5）を拾うと誤検出になる。

### 対処: 同着バンドの第一キーを「コウ経路でない」にする（無条件 > コウ）

バンドが2手以上のときだけ各候補（visits 降順トップ4）に子局面解析1本を撃ち、
コウ経路の候補を格下げする（`tie_ko_screen`、既定 true。visits は gain_verify_visits を
流用）。バンド内の選択順は **(1) コウ経路でない → (2) visits 最多 → (3) 目数 → (4) gain**。
全員コウ経路なら従来どおり visits（クラスが同じならクラス内タイブレーク）。

### 検証（実 generate_move E2E ×3run）

| ケース | 結果 |
|---|---|
| K 初手 | **C13 3/3（修正前は A12 3/3 で誤答）** |
| J 1手目/11手目（回帰） | M11 3/3 / N10 3/3（影響なし） |

## 追記16（2026-07-30）: 候補手自身がコウを開始する形をコウ検査が素通しした（case L）

13路右下（枠あり、region `4,12,0,9`）、黒番の殺し。初手で **L5 を選び不正解**
（正解 J6 → W H6（2子取り）→ J6 の打ち直し＝石の下で無条件殺し）。
データ `calibration-data/tsumego/case-l-immediate-ko-20260730.sgf`。

### 原因: コウ検査が「応手 PV」しか歩いていなかった

白 L6 は呼吸点1（L5）のアタリ石で、**B L5 は L6 を1子取りして自身が孤立呼吸点1になる
「打った瞬間にコウを開始する手」**。守り方は次にコウ禁止で取り返せないため、
リージョン子局面の最善応手は L3 になり、**応手 PV にはコウ形が現れない**。
追記15 の検査は応手 PV だけを歩いていたので L5 を「無条件」と誤判定し、
visits タイブレーク（L5 v1162 > J6 v635）でコウ側を選んだ。

構造チェック（エンジン不要）: L5 単体 → ko_route=True / 正解 J6 は K6 と繋いで
2子で捨てる手筋なので「単独1子」条件に掛からず、正解手順（J6 H6 J6）込みでも False。

### 対処: 検査シーケンスを [候補手] + 応手 PV に拡張

`tsumego_candidate_reaches_region_ko` — 検査は必ず候補手自身（ply0）から始める。
候補自身がコウ形の1子取りなら**解析クエリ不要で確定**（コスト減にもなる）。
応手 PV 側のコウ形（case K の A11 → B11）は従来どおり同じ歩きで拾う。

### 検証（実 generate_move E2E ×3run）

| ケース | 結果 |
|---|---|
| L 初手 | **J6 3/3（修正前は L5 で誤答）** |
| J 1手目/11手目（回帰） | M11 3/3 / N10 3/3（影響なし） |
| K 初手（回帰） | C13 2/3・A10 1/3（下記） |

case K の A10 はバンドプローブ 3/3 でコウ判定が全て正しいこと（A12=コウ経路、
C13/A10=無条件）を確認済み。A10 は正解手順に合流する**手順前後の無条件別解**
（A10 → W A11 → C13）で、A12 格下げ後に残る clean な2手 {C13, A10} の visits が
接近している（C13 520-600 vs A10 369-443）ため稀に入れ替わる。アプリの解答樹が
A10 始まりを認めない場合は**別解限界の残余**（復帰はアンドゥで次候補）。

## 追記17（2026-07-30）: コウ手の gain が実信号になり同着バンドのコウ検査を素通りした（case M）

13路右下（枠あり、region `4,12,0,8`）、黒番の生き。5手目（B K2 → W L4 → B L3 → W M3 の後）で
**M2 を選び不正解**（正解 K1 → W M4 → B M2 の順なら無条件生き。M2 先行だと W K1 で
K 列4子が呼吸点 L2 だけのアタリになり、B M4 の1子取り（M3）がコウ＝コウ生きに転落）。
データ `calibration-data/tsumego/case-m-capture-gain-ko-20260730.sgf`。

### 原因: クラス混同はスコア系メトリックの全部に出ない — gain も同深さ検証もコウ側を追認する

- GUI 実測: M2(v191/比0.12 pt-0.01 g+1.92) が**目数首位かつ gain 首位** → gain 差 1.94 >
  gain_epsilon で同着バンドが形成されず、追記15 のコウ検査（バンド2手以上が条件）が走らない。
  選択手＝目数最善なので score_best 検証も発動せず、gain 首位なので救済も発動しない＝無検証で採用。
- 再現 probe（2/2 run）: M2 は pt+0.56〜0.75 で目数首位ですらない（GUI の pt-0.01 は run 分散）が
  gain +1.76 は不変 → 救済がトリガーし、**同深さ検証（800visits）が M2 を +1.29 で追認**
  （rescue margin 1.0 超え）。つまりどの経路を通っても M2 が選ばれる。
- +1.29 は偽 gain ではなく**実信号**: M2 のラインは「コウに勝つ前提」で白 L2/M3 を取り切るので
  子局面の ownership が本当に高い。「検証は一度も間違えない」（追記8〜12）はスコアの真偽の話で
  あり、**クラス（無条件 > コウ）の裁定には検証も原理的に使えない**ことが初めて実測で出た。
- 一方、構造検出は分離する: リージョン子局面解析で M2 への白の最善応手は **K1**（アプリの反撃と
  同一）で、その PV の B M4（1子取り・呼吸点1）がコウ形（`tsumego_candidate_reaches_region_ko`）。

### 対処1: コウ検査を「同着バンド」から「選択パイプライン最後のクラス裁定」に一般化

- `tsumego_class_screen_pool` — 検査対象 = 最終選択手＋目数ガード内の対抗馬（visits 降順、計4手）。
- `tsumego_declass_choice` — 選択手がコウ経路で clean な対抗馬がいれば visits 最多の clean へ格下げ。
- generate_move はバンド事前検査をやめ、バンド → score_best 検証 → 救済の**後**に1回だけ裁定する
  （どの経路で選ばれてもクラスを通る）。選択手が clean なら検査1本で終わり、対抗馬の検査は
  選択手がコウ経路のときだけ。`select_tsumego_move` の ko_routes 引数は撤去（バンド内は
  visits → 目数 → gain に戻る。旧 in-band 検査と同着ケースの結果は同一）。

### 対処2（F2 で実測した誤爆の防止）: ガード外の救済採用手は検査しない

対処1 を素朴に全選択手へ適用すると case F2 が回帰した（3run 中 1 で正解 N11 → 失敗手 J10）。
枠なし盤の救済採用手 N11(pt+3.85) は**目数ガード外**で、応手が N9 に振れた run の PV の偶発
コウ形で格下げされ、ガード内の clean な J10 — **同深さ検証 -18.8 で N11 -17.0 に負けている
失敗手** — に差し替わった。クラス裁定（無条件 > コウ）が意味を持つのは「スコアが同じ成功と
見なす帯（目数ガード）」の中だけ。帯の外から ownership 検証で拾った手はスコアが嘘をつく局面
（追記10 の圧縮）にいるので、検証の実測をスコアの嘘で上書きしない。実装は
`tsumego_class_screen_pool` が選択手ガード外なら pool を選択手のみにして検査を成立させない。

### 対処3（case M で実測した見逃しの防止）: 拮抗している応手は全部歩く

白のコウ仕掛け K1(v144) と穏健な M4(v103) は比 0.72 で拮抗し、800visits の子局面解析では top が
入れ替わる。top 1本だけ歩く実装は M4 が top の run（3run 中 2）でコウを見逃した。
`tsumego_competitive_replies`（visits 比 0.5=`TSUMEGO_KO_REPLY_RATIO` 以上、最大3本
=`TSUMEGO_KO_REPLY_MAX`）で拮抗応手の PV を全部歩く。守り方が選べる競争力のある抵抗のどれかに
コウがあるなら、その候補はコウ経路（doomed な抵抗は比で沈む: K1 子局面の白 M2 取り返しは
比 0.12 で walk 対象外＝正解 K1 は安定して clean）。歩き自体はエンジン不要なのでコスト増なし。

### 検証（実 generate_move E2E）

| ケース | 結果 |
|---|---|
| M 5手目 | **K1 5/5（修正前は M2 で誤答。M2 はコウ経路検出 5/5・K1 は clean 5/5）** |
| M 1手目/3手目 | K2 3/3 / L3 3/3（影響なし） |
| J 1手目/11手目（回帰） | M11 3/3 / N10 3/3（N10 は拮抗応手3本 walk でも clean） |
| K 初手（回帰） | **C13 3/3**（A12 格下げが事後検査でも同一に働く。追記16 時点の A10 混入なし） |
| L 初手（回帰） | J6 3/3（L5 の ply0 検出は不変） |
| F2 5手目（回帰） | N11 2/3・M12 1/3（追記12 と同一。ガード外ゲートで誤爆消滅） |
| H 5手目（回帰） | N4 3/3 |
| G2 2手目（回帰） | A10 3/3 — **選択則起因ではない**（stash A/B で新旧同一を確認）。現在の解析は
A10 を v1729〜1752 の本命として読み gain が飽和、救済トリガー（追記10 の C13 +6）が消えた。
A10 が解答樹の別解かは GUI で要観察 |

pytest 全体は 407 passed（test_ai.py の 2 failed は本変更と無関係の既存環境要因。
stash で変更前コードでも同一に失敗することを確認済み）。

## 追記18（2026-07-30）: 生き問題の枠が浅い読みで捨てられ、枠なし盤で詰碁が消えた（case N）

13路左下（**枠なし**、region `0,5,0,9`）、黒番の生き。初手で **D10 を選び即不正解**（正解は
B3 → W A4 → B A1 → W B8 → B C1 → W C7 → B A3 の無条件生き。A1/C1 は順序入替可）。D10 は
死活と無関係な点で、選択則の問題ではない。データ
`calibration-data/tsumego/case-n-live-frame-drop-20260730.sgf`（保存 SGF は認識盤そのもの
＝枠なしで出題された回のもの。枠付きキャプチャと違い枠を剥がして復元することはできない）。

### 原因1: 枠なし盤では詰碁が消えている（選択則では救えない）

枠なしフォールバック（追記9）が発動していた。その盤の root は **-74〜-75目**（リージョン外が
丸ごと白の地）で、手番側コア10子は **-0.73〜-0.80/子 = 死**（6サンプル安定）。GUI 実測:

| 手 | visits/比 | pointsLost | gain | |
|---|---|---|---|---|
| D10（選択） | 248/1.00 | +0.46 | +2.28 | 目数首位かつ gain 首位 |
| B3（正解） | 232/0.94 | +1.37 | **-1.37** | ガード内・visit比 0.94 で深さゲートも救済も通った上での gain 負け |

gain_probe で内訳を見ると、この盤の gain は死活の信号ではなく**追記8 の「探索の浅さ」ノイズ**
そのものだった: gain 上位は D5(8visits) +3.60 → D8(6v) +3.19 → A8(**1v**) +2.64 … と visits の
少ない順に並び、選択手 D10(253v) は +1.73、正解 B3 は負。石別の内訳では |delta|≥0.15 の石が
1つも無く（相手石15子は既に -0.83〜-0.99 で決着済み）、**どの手も盤面を動かしていない**。
リージョン外の地が支配的で、詰碁の成否がスコアにもオーナーシップにも現れていない。

実 generate_move: **枠なし D10 2/2 NG ／ 枠あり B3 2/2 OK（ko どちらの枠でも）**。
つまり枠を捨てた判断そのものが誤答の原因で、選択則側の対処は存在しない。

### 原因2: 枠の採否判定が浅すぎた — 生き問題は「解けたら生き」なので浅い読みでは死ぬ

`frame_destroys_problem`（追記9）は trial 400visits の root ownership で下していた。case N の
有効な枠（ko=False）の1子平均を**プロセスを分けた独立サンプル**で測ると:

| 深さ | 実測（1子平均） | 判定 |
|---|---|---|
| 400 | -0.69 / -0.80 / -0.98 | 全部「死」→ 枠を捨てる（＝誤答） |
| 1800 | +0.95 +0.93 +0.78 +0.92 +0.42 -0.02 -0.08 -0.23 -0.95 | **二峰性**。閾値 0.5 をまたぎコイン投げ |
| 6000 | +0.96 / +0.98 / +0.99 / +1.00 | 生きで安定 |

`frame_destroys_problem` の docstring が予告していた非対称性（「殺し問題では手番側の攻め石が
壁と連絡して自明に +1.00 になるが、生き問題では手番側の石自体が戦いの対象なので、読み切れないと
有効な枠でも枠なしに落ちうる」）が、初めて実害として出た形。**生き問題でこの判定は「エンジンが
その詰碁を解けたか」を聞いているのと同じ**なので、浅い読みでは必ず死側に倒れる。

一方、本当に壊れている枠は深くすると**より明確に死ぬ**（case F ko=False: 400 で -0.09、
1800 で -0.72〜-0.85、6000 で -0.92〜-0.95。case G: どの深さでも -0.98）。深さは正常な枠だけを救う。

**注意（測り方）**: 同一プロセスで同じ局面を測り直しても独立サンプルにならない（探索木が
再利用され 0.2 秒で返る）。分散を見るときは engine 起動を挟むこと。

### 対処1: 枠を捨てる前に `frame_validity_visits`(6000) で読み直す

`frame_validity_verdicts` / `frame_solver_verdict`（新規）。浅い読み（`frame_ko_trial_visits`=400）
で死と出た枠だけ読み直し、**読めた中で最も深い読みだけで**裁定する（平均すると浅いノイズが
復活する）。深い読みが取れなければ浅い読みの判定＝従来動作（枠を捨てる）。生きている枠は
読み直さないので、追加コスト（1候補あたり約4〜8秒）を払うのは捨てる寸前の枠だけ。

### 対処2（バックストップ）: 捨てる先の枠なし盤を測ってから捨てる

`frame_over_frameless`（新規）。全枠が壊れ判定のときだけ枠なし盤を同じ深さで読み、手番側コアの
1子平均が `FRAME_OVER_FRAMELESS_MARGIN`(0.5) 以上上回る枠があればそれを使う。**枠なしは安全側の
フォールバックではない**（リージョン外が丸ごと相手の地になる）ことを判断に入れる。

実測（1子平均。枠は ko の2通り／枠なしは `frameless_region`）:

| ケース | 枠の攻め方推定 | 枠 ko=False | 枠 ko=True | 枠なし | 結論 |
|---|---|---|---|---|---|
| D | 正（黒=攻め） | +1.00 | +1.00 | +0.99 | 枠（400 で生き＝比較に来ない） |
| E | 正（黒=攻め） | +1.00 | +1.00 | +0.92 | 枠（同上） |
| F | **反転**（黒=攻めなのに守り側の席） | -0.92〜-0.95 | -0.94〜-0.95 | **-0.65** | 枠なし（従来どおり） |
| G | **反転**（同上） | -0.98 | -0.99 | **-0.63** | 枠なし（従来どおり） |
| N | 正（黒=守り＝生き問題） | **+0.96〜+1.00** | -0.99 | -0.75 | 枠 ko=False |

この並びが判定の意味を説明している: 枠が壊れるのは**攻め方推定が反転したとき**で、そのとき
手番側の石は相手の壁の中に置かれるので深く読むほど確実に死ぬ（構造的な死）。一方 case N は
推定が正しく、死と出ていたのは単にエンジンがまだ解けていなかったから（読みの浅さ）。
深さはこの2つを分離する。

対処1（6000visits）を入れた後は case N が「生き」で安定するため、この比較が結論を変える
実測ケースは無い。ただし 1800visits 時点の実測では -0.23 / -0.08 の run をこの比較が拾って
正解にしていた（3run 中 2）ので、深い読みでもなお死と出る難問への保険として残す。

### 検証

`frame_validity_probe.py` は本番と同じ判定手順（読み直し＋枠なしとの比較）を通すよう更新し、
枠なしで出題された回の SGF も受け取れるよう一般化した。

| ケース | 結果 |
|---|---|
| N 初手（枠の採否） | **枠 ko=False を保持 3/3**（+0.80〜+0.99/子、ko=True は -0.98/-0.99 で正しく落選）。修正前は枠なしに落ちて D10 |
| N 初手（枠あり盤の着手） | **実 generate_move で B3 5/5**（gain +3.87〜+11.92）。枠なし盤の 2/2 D10 と対照的で、同じ選択則が枠さえ正しければ解けている |
| F 2手目（回帰） | 枠なし維持（枠 -0.94〜-0.96 vs 枠なし -0.64）・N8 |
| G 初手（回帰） | 枠なし維持（枠 -0.98/-0.99 vs 枠なし -0.68）・A11 |

pytest 全体は 421 passed（test_ai.py の 2 failed は本変更と無関係の既存環境要因。
`git stash` で katrain/・tests/ の変更を外しても同一に失敗することを確認済み）。

**注**: `select_tsumego_move` 単体（probe の表示）は枠あり盤で 3run 中 1 回 A1 に振れた。
A1 は正解手順の3手目だが初手では W C1 で2子取られる。実 generate_move は後段（同深さ検証・
救済・クラス裁定）を通るので 5/5 B3 で、この振れは表面化していない。select 単体の出力を
着手の結論として読まないこと。

## 影響範囲

- `katrain/core/constants.py` — `AI_TSUMEGO` の定義と各リストへの登録
- `katrain/core/ai.py` — `TsumegoOwnershipStrategy` の追加
- `katrain/__main__.py` — キャプチャ時の黒番プレイヤー種別、枠の採否判定と枠なしフォールバック
- `katrain/core/tsumego_frame.py` — 枠バランス距離、枠の採否判定（`frame_destroys_problem` /
  `frame_solver_verdict` / `frame_validity_verdicts` / `frame_over_frameless`）
- `katrain/config.json` および `C:\Users\iwaki\.katrain\config.json` — 戦略設定と `_enable_ownership`
- `tests/` — 戦略のユニットテスト

既存の戦略・枠ロジック・リージョン算出は変更しない。
